"use client"

import type { Dataset } from "@/lib/types/files"

export function FileUploadZone({
  jobId,
  userId,
  existingFiles,
}: {
  jobId: string
  userId: string
  existingFiles: Dataset[]
}) {
  return <div>Upload zone placeholder</div>
}
