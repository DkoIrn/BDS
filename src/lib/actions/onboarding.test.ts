import { describe, it } from "vitest"

describe("onboarding server actions", () => {
  it.todo("getOnboardingStatus returns completed status")
  it.todo("completeOnboarding sets flag to true")
  it.todo("resetOnboarding clears flag")
})
