---
phase: 24-branded-client-reports
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/lib/actions/branding.ts
  - src/app/(dashboard)/settings/page.tsx
  - src/components/files/export-buttons.tsx
  - src/app/api/reports/pdf/route.ts
autonomous: false
requirements: [RPTX-01, RPTX-02, RPTX-03, RPTX-04]

must_haves:
  truths:
    - "User can upload a company logo in settings and see a preview"
    - "User can pick a brand colour in settings using a native colour picker"
    - "Branding settings persist in the profiles table and survive page reload"
    - "User sees a pre-generation dialog with section toggles and commentary fields before generating a PDF"
    - "Generated PDF reflects the uploaded logo, brand colour, section selection, and commentary"
    - "Both Executive and Technical report modes respect branding and section toggles"
  artifacts:
    - path: "src/lib/actions/branding.ts"
      provides: "Server actions for logo upload, branding save, and branding fetch"
      exports: ["uploadBrandingLogo", "saveBrandColor", "getBrandingSettings"]
    - path: "src/app/(dashboard)/settings/page.tsx"
      provides: "Report Branding card in settings with logo upload and colour picker"
    - path: "src/components/files/export-buttons.tsx"
      provides: "Pre-generation dialog with section toggles and commentary textareas"
    - path: "src/app/api/reports/pdf/route.ts"
      provides: "POST proxy route that fetches branding from DB and forwards to FastAPI"
  key_links:
    - from: "src/components/files/export-buttons.tsx"
      to: "src/app/api/reports/pdf/route.ts"
      via: "POST fetch with sections and commentary in JSON body"
      pattern: "fetch.*api/reports/pdf.*method.*POST"
    - from: "src/app/api/reports/pdf/route.ts"
      to: "profiles table"
      via: "Supabase query for logo_storage_path and brand_color"
      pattern: "from.*profiles.*select.*logo_storage_path.*brand_color"
    - from: "src/lib/actions/branding.ts"
      to: "Supabase Storage datasets bucket"
      via: "upload to {user_id}/branding/logo.png path"
      pattern: "storage.*from.*datasets.*upload"
---

<objective>
Build the frontend branding settings UI (logo upload + colour picker in Settings page) and the pre-generation report dialog (section toggles + commentary fields in ExportButtons). Update the Next.js PDF proxy route to POST with branding data fetched from the user's profile.

Purpose: Users can customise their QC reports with company branding and control which sections appear, producing client-ready deliverables directly from TruQC.
Output: Branding card in settings, pre-generation dialog, updated proxy route.
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-branded-client-reports/24-RESEARCH.md
@.planning/phases/24-branded-client-reports/24-01-SUMMARY.md

@src/app/(dashboard)/settings/page.tsx
@src/components/files/export-buttons.tsx
@src/app/api/reports/pdf/route.ts
</context>

<interfaces>
<!-- Key contracts from Plan 01 that this plan consumes -->

From backend/app/routers/reports.py (created in Plan 01):
```python
# POST endpoint accepts JSON body:
class ReportRequest(BaseModel):
    mode: str = "technical"
    triage_accepted: int | None = None
    triage_rejected: int | None = None
    triage_deferred: int | None = None
    sections: dict[str, bool] | None = None
    commentary: dict[str, str] | None = None
    brand_color: str | None = None
    logo_base64: str | None = None  # Base64 logo bytes
```

From supabase/migrations/00011_branding_columns.sql (created in Plan 01):
```sql
-- profiles table now has:
-- logo_storage_path TEXT (nullable, path in datasets bucket)
-- brand_color TEXT DEFAULT '#14B8A6' (hex colour)
```

Section toggle keys (Technical): severity_pie, severity_breakdown, issues_by_category, issues_by_column, kp_density, methodology, soq, detailed_issues
Section toggle keys (Executive): severity_pie, top_issues, soq
Commentary keys: executive_summary, soq
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Branding server actions and settings UI</name>
  <files>
    src/lib/actions/branding.ts,
    src/app/(dashboard)/settings/page.tsx
  </files>
  <action>
1. Create `src/lib/actions/branding.ts` with three server actions:

   a. `uploadBrandingLogo(formData: FormData)`: "use server". Create Supabase client, getUser() auth check. Extract file from formData.get("logo") as File. Validate: must be image/png or image/jpeg, max 2MB. Upload to datasets bucket at path `${user.id}/branding/logo.png` with `{ upsert: true }`. On success, update profiles table: `supabase.from("profiles").update({ logo_storage_path: path }).eq("id", user.id)`. Return `{ success: true }` or `{ error: string }`.

   b. `saveBrandColor(formData: FormData)`: "use server". Auth check. Get "brand_color" from formData. Validate hex format with regex `/^#[0-9A-Fa-f]{6}$/`. Update profiles: `supabase.from("profiles").update({ brand_color }).eq("id", user.id)`. Return `{ success: true }` or `{ error: string }`.

   c. `getBrandingSettings()`: "use server". Auth check. Query profiles for `logo_storage_path, brand_color` where id = user.id. If logo_storage_path exists, generate a signed URL (300s expiry) via `supabase.storage.from("datasets").createSignedUrl(logo_storage_path, 300)`. Return `{ logoUrl: string | null, brandColor: string }`.

2. Update `src/app/(dashboard)/settings/page.tsx`:

   a. Import `uploadBrandingLogo`, `saveBrandColor`, `getBrandingSettings` from `@/lib/actions/branding`.
   Import `Upload`, `Palette` from lucide-react.

   b. Add state: `brandColor` (string, default "#14B8A6"), `logoUrl` (string | null), `logoFile` (File | null), `brandingPending` (useTransition).

   c. In the existing `loadUser` useEffect, also call `getBrandingSettings()` and set `brandColor` and `logoUrl` from the result.

   d. Add a new "Report Branding" Card between the Profile card and Plan & Billing card. Structure:
   ```
   Card (rounded-2xl)
     CardHeader
       CardTitle with Palette icon (teal-600, teal-50 bg) "Report Branding"
       CardDescription "Customise your QC reports with company branding"
     CardContent
       - Logo upload area: If logoUrl exists, show a 120x60 img preview with rounded-xl border. Below it, a "Change Logo" button. If no logo, show a dashed-border drop zone with Upload icon and "Upload your company logo" text + "(PNG or JPEG, max 2MB)" hint. Use a hidden file input (accept="image/png,image/jpeg") triggered by button click. On file select, call uploadBrandingLogo via FormData, show toast on success/error, refresh logoUrl.
       - Brand colour row: Label "Brand Colour" with a flex row containing an `<input type="color">` (h-10 w-14, rounded-lg border, cursor-pointer) and a text Input showing the hex value (w-28, font-mono, text-sm). On colour change, update state. A "Save" button calls saveBrandColor with the hex value, shows toast.
       - Small info text: "Your logo and colour will appear on all generated PDF reports."

3. Styling: Follow existing card patterns (rounded-2xl, rounded-xl inputs, bg-foreground buttons). The logo preview should have a subtle slate-100 background to show transparency.
  </action>
  <verify>
    <automated>cd C:/Users/danie/OneDrive/Desktop/BDS && npx next lint --quiet 2>&1 | head -20</automated>
  </verify>
  <done>
    - Settings page shows a "Report Branding" card with logo upload and colour picker
    - Logo upload stores file at {user_id}/branding/logo.png in datasets bucket
    - Brand colour saves to profiles.brand_color
    - Logo preview appears after upload
    - Colour picker uses native browser input + hex text field
    - Both settings persist across page reload
  </done>
</task>

<task type="auto">
  <name>Task 2: Pre-generation dialog and POST proxy route</name>
  <files>
    src/components/files/export-buttons.tsx,
    src/app/api/reports/pdf/route.ts
  </files>
  <action>
1. Update `src/app/api/reports/pdf/route.ts`:

   a. Add a POST handler alongside the existing GET. The POST handler:
      - Reads JSON body: `{ mode, triage_accepted, triage_rejected, triage_deferred, sections, commentary }`
      - Auth check (same as GET: getUser, fetch run, ownership check)
      - Fetches branding from profiles: `supabase.from("profiles").select("logo_storage_path, brand_color").eq("id", user.id).single()`
      - If logo_storage_path exists, downloads logo bytes: `supabase.storage.from("datasets").download(logo_storage_path)`
      - Converts logo bytes to base64: `Buffer.from(logoBytes).toString("base64")`
      - Builds FastAPI POST body: `{ mode, triage_accepted, triage_rejected, triage_deferred, sections, commentary, brand_color, logo_base64 }`
      - POSTs to `${fastApiUrl}/api/v1/report/pdf/${runId}` with JSON body and `Content-Type: application/json` header
      - Returns PDF response (same headers as GET)

   b. Keep the existing GET handler unchanged for backwards compatibility.

2. Update `src/components/files/export-buttons.tsx`:

   a. Replace the simple dropdown menu with a pre-generation dialog flow. When user clicks a PDF mode (Executive or Technical) from the dropdown, instead of immediately downloading, open a dialog.

   b. Add state: `dialogOpen` (boolean), `selectedMode` ("executive" | "technical" | null), `sectionToggles` (Record<string, boolean>), `commentaryText` (Record<string, string>), `generating` (boolean).

   c. Define section configs as constants:
      ```typescript
      const TECHNICAL_SECTIONS = [
        { key: "severity_pie", label: "Severity Chart" },
        { key: "severity_breakdown", label: "Severity Breakdown" },
        { key: "issues_by_category", label: "Issues by Category" },
        { key: "issues_by_column", label: "Issues by Column" },
        { key: "kp_density", label: "KP Density" },
        { key: "methodology", label: "Methodology" },
        { key: "soq", label: "Statement of Quality" },
        { key: "detailed_issues", label: "Detailed Issues Table" },
      ]
      const EXECUTIVE_SECTIONS = [
        { key: "severity_pie", label: "Severity Chart" },
        { key: "top_issues", label: "Top Issues" },
        { key: "soq", label: "Statement of Quality" },
      ]
      const COMMENTARY_FIELDS = [
        { key: "executive_summary", label: "Executive Summary Notes" },
        { key: "soq", label: "Statement of Quality Notes" },
      ]
      ```

   d. When a PDF mode is selected from dropdown:
      - Set selectedMode
      - Initialize sectionToggles with all keys set to true
      - Clear commentaryText
      - Open dialog

   e. Dialog content (use a simple div overlay with backdrop, same pattern as existing modals in the app -- or use a `<dialog>` element with Tailwind styling since base-ui Dialog may not be available):
      ```
      Overlay (fixed inset-0, bg-black/50, z-50)
        Dialog panel (fixed top-1/2 left-1/2 -translate, bg-white, rounded-2xl, shadow-xl, max-w-lg, max-h-[80vh] overflow-y-auto, p-6)
          Title: "Configure {Executive|Technical} Report" (text-lg font-semibold)

          Sections heading (text-sm font-medium text-muted-foreground, mt-4)
          Section toggles: map over relevant sections array. Each as a flex row with:
            - Checkbox (native or styled) with checked={sectionToggles[key]}
            - Label text
          All default to checked.

          Commentary heading (text-sm font-medium text-muted-foreground, mt-4)
          Commentary textareas: map over COMMENTARY_FIELDS. Each as:
            - Label (text-sm)
            - Textarea (rounded-xl, h-20, resize-none, placeholder "Add notes for this section...")

          Footer (flex justify-end gap-2, mt-6, border-t, pt-4):
            - Cancel button (variant outline, rounded-xl)
            - "Generate Report" button (bg-foreground text-background, rounded-xl, with Loader2 spinner when generating)
      ```

   f. On "Generate Report" click:
      - Set generating = true
      - Build sections dict from sectionToggles (send all keys with their boolean values)
      - Build commentary dict from commentaryText (only include non-empty values)
      - POST to `/api/reports/pdf?runId=${runId}` with JSON body: `{ mode: selectedMode, sections, commentary }` and `Content-Type: application/json` header
      - On success: create blob, trigger download via anchor element (same pattern as current handleDownload)
      - On error: console.error
      - Close dialog, reset generating

   g. Keep the CSV and Excel buttons unchanged. The dropdown still shows Executive/Technical options but they now open the dialog instead of directly downloading.
  </action>
  <verify>
    <automated>cd C:/Users/danie/OneDrive/Desktop/BDS && npx next lint --quiet 2>&1 | head -20</automated>
  </verify>
  <done>
    - Clicking Executive or Technical in PDF dropdown opens a pre-generation dialog
    - Dialog shows section toggles (all on by default) appropriate to the selected mode
    - Dialog shows commentary textareas for executive_summary and soq
    - Generate Report button POSTs to proxy route with sections and commentary
    - Proxy route fetches branding from profiles, downloads logo, forwards all to FastAPI as POST
    - Generated PDF reflects branding, sections, and commentary
    - GET proxy route still works (backwards compat)
    - CSV and Excel export buttons unchanged
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify branded client reports end-to-end</name>
  <action>Human verifies the complete branded reports feature works end-to-end across settings, dialog, and PDF output.</action>
  <what-built>
    Complete branded client reports feature:
    1. Settings page with Report Branding card (logo upload + colour picker)
    2. Pre-generation dialog with section toggles and commentary fields
    3. Backend report builder with parameterised branding, sections, commentary
    4. End-to-end flow from settings to PDF generation
  </what-built>
  <how-to-verify>
    1. Go to Settings page. Verify "Report Branding" card appears between Profile and Plan sections.
    2. Upload a company logo (PNG or JPEG). Verify preview appears after upload.
    3. Pick a brand colour using the colour picker. Save it. Refresh page -- verify both logo and colour persist.
    4. Navigate to a dataset with validation results. Click "PDF Report" dropdown.
    5. Select "Technical Report". Verify a configuration dialog opens with:
       - 8 section toggles (all checked by default)
       - 2 commentary textareas
    6. Uncheck "Methodology" and "Issues by Column". Add text to "Executive Summary Notes".
    7. Click "Generate Report". Verify PDF downloads with:
       - Your uploaded logo in the header (left side)
       - Your brand colour on header bar and section underlines
       - No "Methodology" or "Issues by Column" sections
       - Commentary text visible in the executive summary area
    8. Repeat with "Executive Report" to verify it also shows logo, colour, and commentary.
    9. Verify CSV and Excel export buttons still work normally.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Settings branding card visible and functional (logo + colour)
- Pre-generation dialog appears for both Executive and Technical modes
- Section toggles control what appears in generated PDF
- Commentary text renders in PDF sections
- Logo and colour from settings reflected in all generated reports
- Backwards compatibility: direct API calls without branding still work
</verification>

<success_criteria>
- User can upload logo in settings and see it on generated PDF reports
- User can set brand colour and see it on report headers/accents
- User can toggle sections on/off before generating a report
- User can add commentary notes that appear in the PDF
- Branding persists per user account across sessions
- Both Executive and Technical reports respect all branding settings
</success_criteria>

<output>
After completion, create `.planning/phases/24-branded-client-reports/24-02-SUMMARY.md`
</output>
