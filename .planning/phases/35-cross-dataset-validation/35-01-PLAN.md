---
phase: 35-cross-dataset-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/validators/cross_dataset.py
  - backend/app/services/cross_validation_presets.py
  - backend/app/services/validation.py
  - backend/app/models/schemas.py
  - backend/app/routers/validation.py
  - supabase/migrations/20260414_cross_dataset_validation.sql
  - backend/tests/validators/test_cross_dataset.py
  - backend/tests/fixtures/cross_dataset/dob_sample.csv
  - backend/tests/fixtures/cross_dataset/doc_sample.csv
autonomous: true
requirements:
  - XVAL-01
  - XVAL-02
  - XVAL-04

must_haves:
  truths:
    - "Cross-dataset validator compares two DataFrames using KP-based alignment and returns ValidationIssue list"
    - "DOB vs DOC preset detects burial/cover inconsistencies with configurable tolerance"
    - "Position vs Event preset detects position mismatches at event KPs"
    - "Column auto-detection matches columns between datasets using synonym lookup"
    - "Coverage gap check flags KP ranges present in one dataset but not the other"
    - "API endpoint accepts two dataset IDs and returns cross-validation issues alongside single-dataset issues"
  artifacts:
    - path: "backend/app/validators/cross_dataset.py"
      provides: "Cross-dataset comparison logic with KP alignment"
      min_lines: 100
    - path: "backend/app/services/cross_validation_presets.py"
      provides: "Preset definitions for DOB vs DOC and Position vs Event"
      exports: ["PRESETS", "CrossValidationPreset", "auto_match_columns", "COLUMN_SYNONYMS"]
    - path: "backend/app/routers/validation.py"
      provides: "Extended /validate endpoint accepting secondary_dataset_id"
    - path: "supabase/migrations/20260414_cross_dataset_validation.sql"
      provides: "Adds secondary_dataset_id column to validation_runs"
      contains: "secondary_dataset_id"
    - path: "backend/tests/validators/test_cross_dataset.py"
      provides: "Unit tests for cross-dataset validator"
      min_lines: 50
  key_links:
    - from: "backend/app/validators/cross_dataset.py"
      to: "backend/app/services/cross_validation_presets.py"
      via: "import CrossValidationPreset, PRESETS"
      pattern: "from app\\.services\\.cross_validation_presets import"
    - from: "backend/app/routers/validation.py"
      to: "backend/app/validators/cross_dataset.py"
      via: "run_cross_dataset_checks call"
      pattern: "run_cross_dataset_checks"
    - from: "backend/app/validators/cross_dataset.py"
      to: "backend/app/validators/base.py"
      via: "ValidationIssue, Severity imports"
      pattern: "from app\\.validators\\.base import"
---

<objective>
Build the complete backend for cross-dataset validation: the validator module, preset definitions, column auto-detection, DB migration, and API endpoint extension.

Purpose: Enable the system to compare two related datasets (DOB vs DOC, Position vs Event) using KP-based row alignment with configurable tolerances, producing ValidationIssue records that merge into the standard issue stream.

Output: Working backend that accepts two dataset IDs, runs cross-validation checks, and stores issues with rule_type="cross_dataset" in the existing validation_issues table.
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-cross-dataset-validation/35-CONTEXT.md
@.planning/phases/35-cross-dataset-validation/35-RESEARCH.md

<interfaces>
<!-- Existing contracts the executor needs -->

From backend/app/validators/base.py:
```python
class Severity(str, Enum):
    CRITICAL = "critical"
    WARNING = "warning"
    INFO = "info"

@dataclass
class ValidationIssue:
    row_number: int
    column_name: str
    rule_type: str
    severity: Severity
    message: str
    expected: str | None = None
    actual: str | None = None
    kp_value: float | None = None

class Validator(Protocol):
    def validate(self, df: pd.DataFrame, column_mappings: list[dict], config: dict) -> list[ValidationIssue]: ...
```

From backend/app/models/schemas.py:
```python
class ValidateRequest(BaseModel):
    dataset_id: str
    config: ProfileConfig | None = None

class ValidateResponse(BaseModel):
    run_id: str
    total_issues: int
    critical_count: int
    warning_count: int
    info_count: int
    pass_rate: float
    status: str
```

From backend/app/services/validation.py:
```python
def run_validation_pipeline(
    df: pd.DataFrame,
    column_mappings: list[dict],
    config: dict,
    enabled_checks: dict | None = None,
) -> list[ValidationIssue]:
```

From backend/app/validators/consistency.py (pattern to follow):
```python
# Uses CONSISTENCY_RULES list, iterates rows, creates ValidationIssue with rule_type="cross_column_consistency"
# Uses _get_kp_value helper for KP extraction
def check_cross_column_consistency(df, column_mappings, kp_column=None) -> list[ValidationIssue]:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-dataset validator module, presets, and column auto-detection</name>
  <files>
    backend/app/validators/cross_dataset.py,
    backend/app/services/cross_validation_presets.py,
    backend/tests/validators/test_cross_dataset.py,
    backend/tests/fixtures/cross_dataset/dob_sample.csv,
    backend/tests/fixtures/cross_dataset/doc_sample.csv
  </files>
  <behavior>
    - Test: align_by_kp merges two DataFrames by nearest KP within tolerance, unmatched rows get NaN
    - Test: check_value_delta flags rows where column difference exceeds tolerance
    - Test: check_relationship flags rows where DOC > DOB (a_gte_b check)
    - Test: check_coverage_gaps flags KP ranges in one dataset but not the other
    - Test: run_cross_dataset_checks with dob_vs_doc preset returns expected issues for sample data
    - Test: run_cross_dataset_checks with position_vs_event preset returns expected issues
    - Test: auto_match_columns correctly pairs columns using synonym lookup (e.g., "Burial Depth" matches "DOB")
    - Test: all cross-dataset issues have rule_type="cross_dataset", row_number=0, and populated kp_value
    - Test: tolerance overrides from config dict take precedence over preset defaults
  </behavior>
  <action>
    Create test fixtures first:
    - `dob_sample.csv`: ~20 rows with columns KP, DOB, Easting, Northing. Include rows at KP 0.0-1.0 with known values.
    - `doc_sample.csv`: ~20 rows with same KP range but DOC values. Include 2-3 intentional mismatches (DOC > DOB), 1 coverage gap (KP range missing), 1 easting delta > tolerance.

    Create `backend/app/services/cross_validation_presets.py`:
    - `CrossValidationPreset` dataclass: id, name, dataset_a_type, dataset_b_type, column_pairs (list of dicts with a, b, check, tolerance, description), kp_alignment_tolerance
    - `PRESETS` dict with two entries: "dob_vs_doc" and "position_vs_event" (use values from RESEARCH.md Pattern 2)
    - `COLUMN_SYNONYMS` dict for column name normalization (kp, dob, doc, easting, northing, depth, top, event, event_code, event_description)
    - `auto_match_columns(headers_a, headers_b) -> list[dict]` using synonym lookup
    - `get_preset_for_types(type_a, type_b) -> CrossValidationPreset | None` that auto-selects preset from dataset type label pair

    Create `backend/app/validators/cross_dataset.py`:
    - `align_by_kp(df_a, df_b, kp_col_a, kp_col_b, tolerance)` using pd.merge_asof with direction="nearest"
    - `check_value_delta(merged, col_a, col_b, tolerance, preset)` flags rows where abs(col_a - col_b) > tolerance
    - `check_relationship(merged, col_a, col_b, op, tolerance, preset)` for a_gte_b type checks
    - `check_coverage_gaps(df_a, df_b, kp_col, preset)` flags KP ranges in one but not the other (use min/max KP comparison per dataset)
    - `run_cross_dataset_checks(df_a, df_b, preset, column_mapping, config)` orchestrator function
    - All issues use rule_type="cross_dataset", row_number=0, populate kp_value
    - Issue messages include both values, delta, and which dataset each value comes from (per user decision: KP-based references, show both values)
    - column_name format: "col_a(TypeA)/col_b(TypeB)" to indicate cross-dataset origin

    Write tests in test_cross_dataset.py covering all behaviors listed above. Run RED first, then implement GREEN.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/validators/test_cross_dataset.py -x -q</automated>
  </verify>
  <done>Cross-dataset validator passes all tests: KP alignment, delta checks, relationship checks, coverage gaps, preset selection, column auto-detection, tolerance overrides</done>
</task>

<task type="auto">
  <name>Task 2: Database migration and API endpoint extension</name>
  <files>
    supabase/migrations/20260414_cross_dataset_validation.sql,
    backend/app/models/schemas.py,
    backend/app/routers/validation.py
  </files>
  <action>
    Create migration `supabase/migrations/20260414_cross_dataset_validation.sql`:
    - Add `secondary_dataset_id UUID REFERENCES datasets(id)` nullable column to `validation_runs` table
    - Add index on secondary_dataset_id for query performance
    - Add `cross_validation_config JSONB` nullable column to `validation_runs` for storing preset/tolerance config snapshot

    Extend `backend/app/models/schemas.py`:
    - Add `CrossDatasetConfig(BaseModel)`: preset_id (str|None), dataset_type_a (str), dataset_type_b (str), column_mapping (list[dict]|None for manual overrides), tolerances (dict[str, float]|None for user overrides)
    - Extend `ValidateRequest`: add `secondary_dataset_id: str | None = None` and `cross_dataset_config: CrossDatasetConfig | None = None`

    Extend `backend/app/routers/validation.py`:
    - In `_legacy_validation_background`, after existing single-dataset validation:
      - If the validation_run has a secondary_dataset_id, fetch + parse the second dataset
      - Auto-detect column mapping if not provided, using auto_match_columns
      - Look up preset using get_preset_for_types if preset_id not specified
      - Run run_cross_dataset_checks with both DataFrames
      - Append cross-dataset issues to the same issue_records batch insert
      - Store secondary_dataset_id and cross_validation_config in the validation_runs record
    - In the `validate_dataset` endpoint handler:
      - Pass secondary_dataset_id through to the validation run record
      - Pass cross_dataset_config to the background task
    - The existing single-file flow MUST remain unchanged when secondary_dataset_id is None (backward compatible)
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/ -x -q --timeout=30</automated>
  </verify>
  <done>Migration file exists with secondary_dataset_id column. ValidateRequest accepts secondary_dataset_id. Validation endpoint runs cross-dataset checks when secondary_dataset_id is provided, single-dataset flow unchanged when it is null.</done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/validators/test_cross_dataset.py -x -q` passes
- `cd backend && python -m pytest tests/ -x -q` full suite passes (no regressions)
- Migration SQL is syntactically valid
- ValidateRequest model accepts secondary_dataset_id=None without breaking existing callers
</verification>

<success_criteria>
- Cross-dataset validator correctly aligns rows by KP and detects delta/relationship/coverage issues
- Two domain presets (DOB vs DOC, Position vs Event) ship with sensible defaults
- Column auto-detection matches common survey column name variations
- API endpoint accepts two dataset IDs and produces merged issue list
- All cross-dataset issues use rule_type="cross_dataset" with KP-based references
- Existing single-dataset validation is completely unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/35-cross-dataset-validation/35-01-SUMMARY.md`
</output>
