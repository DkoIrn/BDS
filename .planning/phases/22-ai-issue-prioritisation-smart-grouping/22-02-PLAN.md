---
phase: 22-ai-issue-prioritisation-smart-grouping
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/app/api/ai-summary/route.ts
  - src/components/files/ai-summary-panel.tsx
  - src/components/files/issue-cluster.tsx
  - src/components/files/results-dashboard.tsx
  - src/app/(dashboard)/pipeline/components/stage-validate.tsx
autonomous: false
requirements:
  - AIFR-01
  - AIFR-04

must_haves:
  truths:
    - "Results dashboard shows Top 3 Blockers cards above the issues table"
    - "Issues are displayed as expandable clusters instead of individual rows when clustering is active"
    - "AI generates a plain-English summary of validation results"
    - "System shows an accept/reject recommendation with confidence percentage"
    - "AI summary loads asynchronously -- clusters display immediately, AI narrative appears after"
    - "Prioritisation works on both project results dashboard and pipeline validate stage"
  artifacts:
    - path: "src/app/api/ai-summary/route.ts"
      provides: "Anthropic API endpoint for narrative summary + recommendation"
      exports: ["POST"]
    - path: "src/components/files/ai-summary-panel.tsx"
      provides: "Top blockers, narrative, accept/reject badge UI"
      exports: ["AISummaryPanel"]
    - path: "src/components/files/issue-cluster.tsx"
      provides: "Expandable cluster row component"
      exports: ["IssueClusterRow"]
    - path: "src/components/files/results-dashboard.tsx"
      provides: "Updated dashboard with clustering + AI summary"
    - path: "src/app/(dashboard)/pipeline/components/stage-validate.tsx"
      provides: "Updated pipeline validate with clustering + AI summary"
  key_links:
    - from: "src/components/files/ai-summary-panel.tsx"
      to: "/api/ai-summary"
      via: "fetch POST with clustered data"
      pattern: "fetch.*api/ai-summary"
    - from: "src/components/files/results-dashboard.tsx"
      to: "src/lib/ai/cluster-issues.ts"
      via: "import clusterIssues, adaptServerIssues"
      pattern: "import.*cluster-issues"
    - from: "src/app/(dashboard)/pipeline/components/stage-validate.tsx"
      to: "src/lib/ai/cluster-issues.ts"
      via: "import clusterIssues, adaptPipelineIssues"
      pattern: "import.*cluster-issues"
---

<objective>
Build the AI summary API route and integrate clustering + AI narrative into both the project results dashboard and pipeline validate stage.

Purpose: This wires the clustering engine from Plan 01 into the user-facing UI, adds the Anthropic-powered narrative summary and accept/reject recommendation, and surfaces top blockers prominently so engineers see what matters first.

Output: AI summary endpoint, reusable UI components, updated results dashboard and pipeline validate stage.
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ai-issue-prioritisation-smart-grouping/22-RESEARCH.md
@.planning/phases/22-ai-issue-prioritisation-smart-grouping/22-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (will exist when this executes) -->
From src/lib/ai/types.ts:
```typescript
export interface ClusterInput {
  id: string
  rule_type: string
  severity: 'critical' | 'warning' | 'info'
  row_number: number
  column_name: string
  message: string
  kp_value: number | null
}

export interface IssueCluster {
  id: string
  rule_type: string
  column_name: string
  severity: 'critical' | 'warning' | 'info'
  count: number
  label: string
  kp_range: { min: number; max: number } | null
  row_range: { min: number; max: number }
  issues: ClusterInput[]
  priorityScore: number
}

export interface AISummaryResponse {
  summary: string
  topBlockers: string[]
  recommendation: 'accept' | 'reject'
  confidence: number
  reasoning: string
}
```

From src/lib/ai/cluster-issues.ts:
```typescript
export function clusterIssues(issues: ClusterInput[]): IssueCluster[]
export function getTopBlockers(clusters: IssueCluster[], n?: number): IssueCluster[]
export function adaptServerIssues(issues: ServerValidationIssue[]): ClusterInput[]
export function adaptPipelineIssues(issues: PipelineValidationIssue[]): ClusterInput[]
```

<!-- Existing AI route pattern -->
From src/app/api/ai-clean/route.ts:
- Auth via createClient() + getUser()
- Anthropic SDK instantiation with process.env.ANTHROPIC_API_KEY
- JSON extraction via regex: /```json\s*([\s\S]*?)```/
- Graceful fallback if JSON parsing fails

<!-- Existing results dashboard -->
From src/components/files/results-dashboard.tsx:
- Client component with issues state (ValidationIssue[])
- Renders ResultsStatCards, IssuesTable, RunSwitcher, ExportButtons
- Issues fetched via getValidationIssues(runId) server action

<!-- Pipeline validate stage -->
From src/app/(dashboard)/pipeline/components/stage-validate.tsx:
- Client component with ValidationResult state (issues + summary)
- Issues via validateClientSide() function
- Renders IssuesList inline component
- Has onIssuesFound callback prop
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AI summary API route and reusable UI components</name>
  <files>src/app/api/ai-summary/route.ts, src/components/files/ai-summary-panel.tsx, src/components/files/issue-cluster.tsx</files>
  <action>
    1. Create src/app/api/ai-summary/route.ts (POST):
       - Auth check via createClient() + getUser() (same pattern as ai-clean)
       - Check process.env.ANTHROPIC_API_KEY, return 503 if missing
       - Accept body: { clusters: { severity, label, count }[], passRate: number, totalIssues: number, rowCount: number, fileName: string }
       - Build prompt: "You are a survey data QC specialist. Analyze these validation results and provide: 1) A 2-3 sentence plain-English summary of the dataset quality. 2) The top 3 most impactful issues (blockers) as short phrases. 3) An accept/reject recommendation with confidence 0-100. Respond in JSON only: { summary, topBlockers: string[], recommendation: accept|reject, confidence: number, reasoning: string }"
       - Include in prompt: file name, row count, pass rate, then cluster list formatted as "- [SEVERITY] label (count instances)"
       - Cap clusters sent to 30 max
       - Use claude-sonnet-4-20250514, max_tokens: 1500
       - Extract JSON via regex (same ```json pattern as ai-clean), fallback to raw parse
       - Validate response shape: summary is string, topBlockers is array of strings, recommendation is accept|reject, confidence is number 0-100
       - Return AISummaryResponse JSON, or { error } on failure
       - Wrap in try/catch, return 500 with error message on failure

    2. Create src/components/files/ai-summary-panel.tsx:
       - Props: { clusters: IssueCluster[], passRate: number, totalIssues: number, rowCount: number, fileName: string }
       - On mount, if clusters.length > 0, call /api/ai-summary with cluster summaries (map to {severity, label, count})
       - State: loading (boolean), aiSummary (AISummaryResponse | null), error (string | null)
       - Layout (card with rounded-2xl, p-6):
         a) Top 3 Blockers section: 3 mini-cards in a row (grid cols-1 sm:cols-3 gap-3). Each card shows severity-colored left border (red/amber/blue), the blocker text from aiSummary.topBlockers, and the cluster's count if matched. If AI not loaded yet, show top 3 clusters from getTopBlockers() deterministically.
         b) AI Summary section: narrative text (aiSummary.summary) in a muted paragraph. Show Skeleton while loading.
         c) Recommendation badge: Pill showing "Accept" (green, CheckCircle icon) or "Reject" (red, XCircle icon) with confidence percentage. Only show when AI loaded.
         d) Reasoning: small muted text below recommendation (aiSummary.reasoning)
       - If AI call fails, show clusters-only mode with a subtle "AI analysis unavailable" note. Do NOT block the entire panel.
       - Use sonner toast for AI errors (non-blocking).

    3. Create src/components/files/issue-cluster.tsx:
       - Props: { cluster: IssueCluster }
       - Expandable row: shows cluster label, severity badge, count badge, KP range if present
       - Click to expand/collapse individual issues list
       - Expanded state shows each issue's row_number, column_name, message in a compact list (max-h-60 overflow-y-auto)
       - Severity colors matching SEVERITY_CONFIG pattern from stage-validate.tsx (red/amber/blue backgrounds)
       - Use ChevronDown/ChevronUp icons for expand toggle
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>API route authenticates and returns AI summary JSON. AISummaryPanel shows top blockers immediately and loads AI narrative async. IssueClusterRow renders expandable cluster with individual issues.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate into results dashboard and pipeline validate</name>
  <files>src/components/files/results-dashboard.tsx, src/app/(dashboard)/pipeline/components/stage-validate.tsx</files>
  <action>
    1. Update src/components/files/results-dashboard.tsx:
       - Import clusterIssues, adaptServerIssues, getTopBlockers from @/lib/ai/cluster-issues
       - Import AISummaryPanel from @/components/files/ai-summary-panel
       - Import IssueClusterRow from @/components/files/issue-cluster
       - After issues are loaded (in the useEffect for selectedRunId), compute clusters: clusterIssues(adaptServerIssues(issues))
       - Add clusters state: useState<IssueCluster[]>([])
       - Render AISummaryPanel between ResultsStatCards and IssuesTable, passing clusters, passRate (from selectedRun.pass_rate ?? 0), totalIssues (selectedRun.total_issues), rowCount (issues.length as proxy or total from run), fileName (datasetId as fallback label)
       - Add a toggle above IssuesTable: "Clustered view" / "Individual view" (default: clustered)
       - In clustered view: render clusters.map(c => <IssueClusterRow cluster={c} />) instead of <IssuesTable>. Apply activeSeverity filter to clusters too.
       - In individual view: render existing <IssuesTable> as-is (preserve full backward compat)
       - Keep all existing functionality (RunSwitcher, ExportButtons, severity filter)

    2. Update src/app/(dashboard)/pipeline/components/stage-validate.tsx:
       - Import clusterIssues, adaptPipelineIssues from @/lib/ai/cluster-issues
       - Import AISummaryPanel from @/components/files/ai-summary-panel
       - Import IssueClusterRow from @/components/files/issue-cluster
       - After validation completes (result is set), compute clusters from result.issues
       - Replace the inline IssuesList component with clustered view:
         a) AISummaryPanel with clusters, passRate (computed as (rowCount - criticalCount) / rowCount * 100), totalIssues (result.summary.total), rowCount (from state.parsedData length), fileName (from state.file?.name ?? "Pipeline dataset")
         b) Render clusters as IssueClusterRow components below the summary panel
       - Keep the existing "Show all N issues" expand pattern but apply it to clusters (show first 5 clusters, expand to show all)
       - Preserve the skip validation flow, back/continue buttons, and all existing stage navigation
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Results dashboard shows AISummaryPanel + clustered issues with toggle. Pipeline validate stage shows AI panel + clusters after validation. Both flows use deterministic clusters immediately with async AI narrative.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify AI prioritisation and clustering</name>
  <what-built>AI-powered issue prioritisation and smart grouping across both project results dashboard and pipeline validate stage</what-built>
  <how-to-verify>
    1. Navigate to a project dataset that has validation results. Open the Results tab.
    2. Verify "Top 3 Blockers" cards appear above the issues table, showing the highest-priority clusters.
    3. Verify issues are displayed in clustered view by default (e.g. "23 KP gaps in DOB between KP 102-118").
    4. Click a cluster to expand and see individual issues listed within.
    5. Toggle to "Individual view" to confirm the original issue table still works.
    6. Verify AI narrative summary loads (may take 2-5 seconds) with accept/reject recommendation and confidence %.
    7. Navigate to /pipeline, upload a test CSV, run validation in the Validate stage.
    8. Verify clusters and AI summary panel appear in the pipeline validate results too.
    9. If ANTHROPIC_API_KEY is not set, verify the panel gracefully shows clusters without AI (no crash).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
npx tsc --noEmit
npx vitest run src/lib/ai/
</verification>

<success_criteria>
- Top 3 Blockers render immediately from deterministic clusters on both dashboard and pipeline
- AI narrative and accept/reject badge load asynchronously without blocking cluster display
- Clusters are expandable to show individual issues
- Both project and pipeline flows use the shared clustering library
- Graceful degradation when ANTHROPIC_API_KEY is missing
</success_criteria>

<output>
After completion, create `.planning/phases/22-ai-issue-prioritisation-smart-grouping/22-02-SUMMARY.md`
</output>
