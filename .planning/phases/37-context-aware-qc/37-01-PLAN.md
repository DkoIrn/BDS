---
phase: 37-context-aware-qc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260417_context_zones.sql
  - backend/app/models/schemas.py
  - backend/app/services/context_zones.py
  - backend/app/services/context_zone_presets.py
  - backend/app/routers/context_zones.py
  - backend/app/main.py
  - backend/tests/test_context_zones.py
autonomous: true
requirements: [CTXQ-01, CTXQ-02, CTXQ-03, CTXQ-04]

must_haves:
  truths:
    - "Context zones can be created, read, updated, and deleted via API"
    - "Zone-aware validation splits DataFrame by KP range and applies modified thresholds per zone"
    - "Event-conditional zones match rows by event column value instead of KP range"
    - "Preset zones for shore approach, trench crossing, J-tube, and span are loadable"
    - "Overlapping zones use first-match-wins with sort_order priority"
    - "When no zones are provided, validation runs unchanged with base config"
  artifacts:
    - path: "supabase/migrations/20260417_context_zones.sql"
      provides: "context_zones table with RLS policies"
      contains: "CREATE TABLE public.context_zones"
    - path: "backend/app/services/context_zones.py"
      provides: "Zone-aware validation dispatch"
      exports: ["apply_context_zones", "load_zones"]
    - path: "backend/app/services/context_zone_presets.py"
      provides: "Pre-configured domain zone templates"
      contains: "PRESET_ZONES"
    - path: "backend/app/routers/context_zones.py"
      provides: "CRUD API for zones"
      contains: "router"
    - path: "backend/tests/test_context_zones.py"
      provides: "Unit tests for zone execution and CRUD"
      min_lines: 80
  key_links:
    - from: "backend/app/services/context_zones.py"
      to: "backend/app/services/validation.py"
      via: "run_validation_pipeline import"
      pattern: "from app.services.validation import run_validation_pipeline"
    - from: "backend/app/routers/context_zones.py"
      to: "backend/app/main.py"
      via: "router registration"
      pattern: "context_zones"
---

<objective>
Build the complete backend for context-aware QC: database table, Pydantic models, zone-aware validation service, CRUD API router, domain preset zones, and unit tests.

Purpose: Enable validators to apply different thresholds based on pipeline context (KP zones, events), reducing false positives in complex survey environments.
Output: Working backend API for zone CRUD and a zone-aware validation dispatch service ready for pipeline integration.
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-context-aware-qc/37-RESEARCH.md

@.planning/phases/36-custom-rule-builder/36-01-SUMMARY.md (same CRUD + RLS pattern)

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/app/models/schemas.py:
```python
class ProfileConfig(BaseModel):
    # Existing — has range thresholds, enabled checks

class ValidateRequest(BaseModel):
    dataset_id: str
    config: ProfileConfig | None = None
    secondary_dataset_id: str | None = None
    cross_dataset_config: CrossDatasetConfig | None = None
    custom_rule_ids: list[str] | None = None
    # Will add: context_zone_ids: list[str] | None = None
```

From backend/app/services/validation.py:
```python
def run_validation_pipeline(
    df: pd.DataFrame,
    column_mappings: list[dict],
    flat_config: dict,
    enabled_checks: dict,
) -> list[ValidationIssue]:
```

From backend/app/routers/custom_rules.py (pattern to replicate):
```python
router = APIRouter(prefix="/api/v1/rules", tags=["custom-rules"])
# CRUD: POST /, GET /, GET /{id}, PUT /{id}, DELETE /{id}
# Test: POST /test
```

From backend/app/routers/validation.py:
```python
def _legacy_validation_background(
    dataset_id: str,
    config: ProfileConfig | None,
    secondary_dataset_id: str | None = None,
    cross_dataset_config: CrossDatasetConfig | None = None,
    custom_rule_ids: list[str] | None = None,
    # Will add: context_zone_ids parameter
) -> None:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Database migration, Pydantic models, zone service, preset zones, and tests</name>
  <files>
    supabase/migrations/20260417_context_zones.sql,
    backend/app/models/schemas.py,
    backend/app/services/context_zones.py,
    backend/app/services/context_zone_presets.py,
    backend/tests/test_context_zones.py
  </files>
  <behavior>
    - Test: ContextZoneDefinition validates kp_range requires kp_start <= kp_end
    - Test: ContextZoneDefinition validates event_match requires non-empty event_value
    - Test: apply_context_zones with a single KP zone modifies thresholds for rows in range
    - Test: apply_context_zones with rows outside all zones uses base config
    - Test: Event-conditional zone matches rows containing event_value in event column
    - Test: Overlapping zones use first-match-wins (earlier zone claims the row)
    - Test: Empty zones list returns same results as run_validation_pipeline with base config
    - Test: _modify_config applies multipliers correctly (1.5 on 3.0 = 4.5)
    - Test: PRESET_ZONES contains shore-approach, trench-crossing, j-tube, span entries
  </behavior>
  <action>
    1. Create `supabase/migrations/20260417_context_zones.sql` with the context_zones table exactly as specified in RESEARCH.md. Include UUID PK, profile_id FK, user_id FK, org_id FK, zone_type CHECK constraint, valid_kp_range CHECK, valid_event CHECK, threshold_modifiers JSONB, enabled/is_preset/preset_id/sort_order columns, indexes on profile_id and org_id, RLS policies mirroring custom_rules (org members SELECT, reviewers/admins INSERT, owner UPDATE, owner/admin DELETE), and updated_at trigger.

    2. Add `ContextZoneDefinition` Pydantic model to `backend/app/models/schemas.py` with zone_type literal, optional kp_start/kp_end/event_value, threshold_modifiers dict, and model_validator for zone type constraints. Add `context_zone_ids: list[str] | None = None` to `ValidateRequest`.

    3. Create `backend/app/services/context_zones.py` with:
       - `ContextZone` dataclass (id, name, zone_type, kp_start, kp_end, event_value, threshold_modifiers, sort_order)
       - `load_zones(supabase, zone_ids: list[str]) -> list[ContextZone]` — fetches from DB, returns sorted by sort_order
       - `apply_context_zones(df, column_mappings, base_config, enabled_checks, zones, kp_column) -> list[ValidationIssue]` — splits DataFrame by zones using first-match-wins with covered_indices tracking, runs run_validation_pipeline per zone with modified config, tags issues with zone name, runs base config on uncovered rows
       - `_get_zone_mask(df, zone, kp_column) -> pd.Series` — KP range mask via pd.to_numeric with errors=coerce, or event match using column_mappings to find event column (not hard-coded column names)
       - `_modify_config(base_config, modifiers) -> dict` — multiplier-based config modification

    4. Create `backend/app/services/context_zone_presets.py` with PRESET_ZONES dict containing shore-approach, trench-crossing, j-tube, and span entries as specified in RESEARCH.md. Include a `get_preset_zones() -> dict` function and a `create_zones_from_preset(preset_id) -> ContextZoneDefinition | None` function.

    5. Create `backend/tests/test_context_zones.py` with tests for all behaviors listed above. Use sample DataFrames with kp, depth, dob columns and event_listing column. Mock run_validation_pipeline where needed for unit isolation.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_context_zones.py -x -v</automated>
  </verify>
  <done>All zone service tests pass. ContextZoneDefinition model validates correctly. apply_context_zones correctly splits DataFrames by KP/event zones, applies modified thresholds, and merges results with zone tags. Preset zones are defined for 4 pipeline scenarios.</done>
</task>

<task type="auto">
  <name>Task 2: CRUD API router and main.py registration</name>
  <files>
    backend/app/routers/context_zones.py,
    backend/app/main.py
  </files>
  <action>
    1. Create `backend/app/routers/context_zones.py` following the exact pattern from `backend/app/routers/custom_rules.py`:
       - Router prefix: `/api/v1/zones`, tags: `["context-zones"]`
       - Request models: `CreateZoneRequest` (profile_id, user_id, org_id, name, description, zone_type, kp_start, kp_end, event_value, threshold_modifiers, sort_order), `UpdateZoneRequest` (all optional fields)
       - POST `/` — create zone, validate ContextZoneDefinition, insert to Supabase, return 201
       - GET `/` — list zones, requires query param `profile_id`, returns zones sorted by sort_order
       - GET `/{zone_id}` — get single zone
       - PUT `/{zone_id}` — update zone fields, validate if rule_definition changed
       - DELETE `/{zone_id}` — delete zone
       - GET `/presets` — return PRESET_ZONES from context_zone_presets module
       - POST `/presets/apply` — accept preset_id + profile_id + user_id + org_id, create zone from preset template with user-provided KP overrides

    2. Register the router in `backend/app/main.py` — add `from app.routers.context_zones import router as context_zones_router` and `app.include_router(context_zones_router)`, following the existing custom_rules registration pattern.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_context_zones.py -x -v && python -c "from app.routers.context_zones import router; print('Router OK:', router.prefix)"</automated>
  </verify>
  <done>CRUD router registered at /api/v1/zones with create, list, get, update, delete, presets, and preset apply endpoints. Router loads without import errors.</done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/test_context_zones.py -x -v` — all zone tests green
- `cd backend && python -m pytest tests/ -x` — full backend suite green (no regressions)
- `python -c "from app.services.context_zones import apply_context_zones; print('OK')"` — service importable
- `python -c "from app.routers.context_zones import router; print('OK')"` — router importable
</verification>

<success_criteria>
- Context zone Pydantic models validate KP range and event match constraints
- apply_context_zones correctly segments DataFrames by zone and applies modified thresholds
- Event-conditional zones match on event column values via column_mappings
- CRUD API at /api/v1/zones with full create/read/update/delete
- Preset zones for 4 pipeline scenarios (shore approach, trench crossing, J-tube, span)
- All tests pass, no regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/37-context-aware-qc/37-01-SUMMARY.md`
</output>
