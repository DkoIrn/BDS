---
phase: 14-data-transform-tools
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/app/(dashboard)/tools/transform/page.tsx
  - src/app/(dashboard)/tools/transform/crs/page.tsx
  - src/app/(dashboard)/tools/transform/crs/crs-tool.tsx
  - src/app/(dashboard)/tools/transform/merge/page.tsx
  - src/app/(dashboard)/tools/transform/merge/merge-tool.tsx
  - src/components/transform/output-preview-table.tsx
  - src/app/api/transform/crs/route.ts
  - src/app/api/transform/merge/route.ts
autonomous: true
requirements:
  - XFRM-07
  - XFRM-08
  - XFRM-09

must_haves:
  truths:
    - "Landing page cards link to tool sub-pages with Auto-Clean showing Coming Soon badge"
    - "CRS tool lets user upload a file, auto-detects source CRS, select target CRS, and download converted file"
    - "Merge tool lets user upload multiple files, preview file list, and download merged output"
    - "CRS and merge done states show preview table of first ~20 rows of output"
    - "Output format defaults to same-as-input when no explicit selection is made"
  artifacts:
    - path: "src/app/(dashboard)/tools/transform/page.tsx"
      provides: "Landing page with clickable capability cards"
      contains: "Link"
    - path: "src/app/(dashboard)/tools/transform/crs/crs-tool.tsx"
      provides: "CRS conversion tool UI"
      contains: "useDropzone"
    - path: "src/app/(dashboard)/tools/transform/merge/merge-tool.tsx"
      provides: "Merge datasets tool UI"
      contains: "useDropzone"
    - path: "src/components/transform/output-preview-table.tsx"
      provides: "Shared lightweight preview table for transform output (~20 rows)"
      contains: "OutputPreviewTable"
    - path: "src/app/api/transform/crs/route.ts"
      provides: "Auth proxy for CRS endpoint"
      exports: ["POST"]
    - path: "src/app/api/transform/merge/route.ts"
      provides: "Auth proxy for merge endpoint"
      exports: ["POST"]
  key_links:
    - from: "src/app/(dashboard)/tools/transform/crs/crs-tool.tsx"
      to: "/api/transform/crs"
      via: "fetch POST with FormData"
      pattern: "fetch.*api/transform/crs"
    - from: "src/app/api/transform/crs/route.ts"
      to: "FASTAPI_URL/api/v1/transform/crs"
      via: "raw body forwarding"
      pattern: "fastApiUrl.*transform/crs"
    - from: "src/app/(dashboard)/tools/transform/merge/merge-tool.tsx"
      to: "/api/transform/merge"
      via: "fetch POST with FormData"
      pattern: "fetch.*api/transform/merge"
    - from: "src/app/(dashboard)/tools/transform/crs/crs-tool.tsx"
      to: "src/components/transform/output-preview-table.tsx"
      via: "import OutputPreviewTable"
      pattern: "OutputPreviewTable"
    - from: "src/app/(dashboard)/tools/transform/merge/merge-tool.tsx"
      to: "src/components/transform/output-preview-table.tsx"
      via: "import OutputPreviewTable"
      pattern: "OutputPreviewTable"
---

<objective>
Build the transform landing page, CRS conversion tool, merge datasets tool, and a shared output preview table component.

Purpose: Delivers 2 of 3 user-facing tools. CRS conversion is the highest-value transform for UK survey data. Merge is the simplest tool UI-wise but needs multi-file upload (new pattern). The shared preview table shows first ~20 rows of transform output per user decision.
Output: Updated landing page with clickable cards, CRS tool at /tools/transform/crs, merge tool at /tools/transform/merge, shared OutputPreviewTable component, API proxy routes for both.
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-data-transform-tools/14-CONTEXT.md
@.planning/phases/14-data-transform-tools/14-RESEARCH.md
@.planning/phases/14-data-transform-tools/14-01-SUMMARY.md

<interfaces>
<!-- Converter UI pattern to follow -->

From src/app/(dashboard)/tools/convert/converter.tsx (state machine pattern):
```typescript
type ConvertState =
  | { step: "upload" }
  | { step: "format"; file: File }
  | { step: "converting"; file: File; targetFormat: TargetFormat }
  | { step: "done"; file: File; targetFormat: TargetFormat; blobUrl: string; ... }
  | { step: "error"; file: File; message: string }
```

From src/app/api/convert/route.ts (auth proxy pattern):
```typescript
export async function POST(request: Request) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Not authenticated' }, { status: 401 })
  const fastApiUrl = process.env.FASTAPI_URL
  const rawBody = await request.arrayBuffer()
  const contentType = request.headers.get('Content-Type') || ''
  const response = await fetch(`${fastApiUrl}/api/v1/convert`, {
    method: 'POST', headers: { 'Content-Type': contentType }, body: rawBody,
  })
  // ... relay response
}
```

From existing transform landing page (current placeholder):
```typescript
const capabilities = [
  { icon: Globe, title: "CRS Conversion", ... },
  { icon: Merge, title: "Merge Datasets", ... },
  { icon: Scissors, title: "Split by Region", ... },
  { icon: Sparkles, title: "Auto-Clean", ... },
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing page update, API proxy routes, and shared preview table component</name>
  <files>
    src/app/(dashboard)/tools/transform/page.tsx,
    src/components/transform/output-preview-table.tsx,
    src/app/api/transform/crs/route.ts,
    src/app/api/transform/merge/route.ts
  </files>
  <action>
    1. Update src/app/(dashboard)/tools/transform/page.tsx:
       - Remove the "Coming Soon" dashed border placeholder section.
       - Make capability cards clickable: CRS -> `/tools/transform/crs`, Merge -> `/tools/transform/merge`, Split -> `/tools/transform/split`.
       - Use Next.js Link component for navigation.
       - Auto-Clean card: add a "Coming Soon" Badge (from shadcn/ui) overlaying the card, no link, slightly muted/opacity styling.
       - Keep existing icons and descriptions. Update Split description to "Split by KP range or column value".
       - Add hover effect on clickable cards (e.g., hover:border-primary/50 transition).

    2. Create src/components/transform/output-preview-table.tsx ("use client"):
       - A lightweight, read-only preview table component for transform output. NOT reusing data-preview-table.tsx (per RESEARCH.md: too coupled to mapping workflow).
       - Props: { csvText: string; maxRows?: number } where csvText is the raw CSV/text response body, maxRows defaults to 20.
       - Parse the csvText: split by newlines, first row = headers, remaining rows = data. Use a simple CSV split (handle quoted commas if straightforward, otherwise split on comma).
       - Render using shadcn Table component (Table, TableHeader, TableRow, TableHead, TableBody, TableCell).
       - Show first `maxRows` data rows. If more rows exist, show a muted note below: "Showing first {maxRows} of {totalRows} rows".
       - Compact styling: text-xs, px-2 py-1 cells, overflow-x-auto wrapper for wide tables.
       - Truncate cell values longer than 30 chars with ellipsis and title attribute for hover.
       - Export as named export: `export function OutputPreviewTable(...)`.
       - This component will be used by CRS tool, merge tool (this plan), and split tool (Plan 03).

    3. Create src/app/api/transform/crs/route.ts:
       - Copy auth proxy pattern from src/app/api/convert/route.ts exactly.
       - Set maxDuration = 60.
       - Auth check with getUser(). Forward raw body to `${fastApiUrl}/api/v1/transform/crs`.
       - Forward response headers: Content-Type, Content-Disposition, X-Transform-Warnings, X-Row-Count, X-Source-CRS, X-Target-CRS, X-Input-Format.
       - On error, parse JSON detail and return error message.

    4. Create src/app/api/transform/merge/route.ts:
       - Same auth proxy pattern. Forward to `${fastApiUrl}/api/v1/transform/merge`.
       - Forward same headers plus X-Merge-File-Count, X-Input-Format if present.
  </action>
  <verify>
    <automated>cd "C:/Users/danie/OneDrive/Desktop/BDS" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Landing page cards are clickable links to tool sub-pages. Auto-Clean shows "Coming Soon" badge. Two API proxy routes created following established auth proxy pattern. OutputPreviewTable component created as shared reusable component.</done>
</task>

<task type="auto">
  <name>Task 2: CRS conversion and merge tool pages</name>
  <files>
    src/app/(dashboard)/tools/transform/crs/page.tsx,
    src/app/(dashboard)/tools/transform/crs/crs-tool.tsx,
    src/app/(dashboard)/tools/transform/merge/page.tsx,
    src/app/(dashboard)/tools/transform/merge/merge-tool.tsx
  </files>
  <action>
    1. Create src/app/(dashboard)/tools/transform/crs/page.tsx:
       - Simple server component wrapper: import CrsTool, render with page title "CRS Conversion" and breadcrumb-style back link to /tools/transform.

    2. Create src/app/(dashboard)/tools/transform/crs/crs-tool.tsx ("use client"):
       - State machine with steps: upload -> configure -> transforming -> done -> error (same pattern as converter).
       - Step 1 (Upload): Inline react-dropzone accepting all survey file extensions (.csv, .xlsx, .xls, .geojson, .json, .zip, .kml, .kmz, .xml, .dxf). 50MB limit. Show file info after selection.
       - Step 2 (Configure): Show auto-detected source CRS (call a local detection endpoint or parse response headers). If not detected, show warning and force manual selection. Source CRS dropdown: WGS84 (4326), OSGB36 (27700), UTM Zone 29N (32629), UTM Zone 30N (32630), UTM Zone 31N (32631). Target CRS dropdown: same list minus source. Output format selector: CSV, GeoJSON, KML.
         DEFAULT OUTPUT FORMAT: Read the X-Input-Format response header from the backend (added in Plan 01). Pre-select the output format dropdown to match the uploaded file's format. If the input format is not in the dropdown options (e.g., xlsx, dxf), default to GeoJSON. Per user decision: "same as input, fallback to GeoJSON."
       - Step 3 (Transforming): Spinner with "Converting coordinates..." status text. POST to /api/transform/crs with FormData (file, source_epsg, target_epsg, target_format). If user has not changed the output format dropdown, send target_format as empty string so the backend applies the same-as-input default.
       - Step 4 (Done): Show stats from response headers (row count, source/target CRS). Import and render OutputPreviewTable from src/components/transform/output-preview-table.tsx — pass the response body text and maxRows=20 to show a preview of the transformed data. Download button using blob URL + hidden anchor pattern. "Transform Another" reset button.
       - Error state: Inline error message with red AlertCircle icon and "Try Again" button.
       - CRS list as const array with { epsg: number, label: string } objects for reuse in dropdowns.
       - NOTE: For preview, the response body is read as text (not just blob). Store both blob URL (for download) and text (for preview). If the output format is not text-based (e.g., KML is XML-based but still text), show the preview. For binary formats, skip preview and show stats only.

    3. Create src/app/(dashboard)/tools/transform/merge/page.tsx:
       - Server component wrapper with title "Merge Datasets" and back link.

    4. Create src/app/(dashboard)/tools/transform/merge/merge-tool.tsx ("use client"):
       - State machine: upload -> preview -> merging -> done -> error.
       - Step 1 (Upload): Multi-file dropzone. Accept same extensions. Allow both drag-multiple and "Add more files" button. Show file list as added (filename, size, remove button). Minimum 2 files to proceed.
       - Step 2 (Preview): Show file list summary (N files, total rows if known). Output format selector (CSV, GeoJSON, KML).
         DEFAULT OUTPUT FORMAT: Detect the extension of the first uploaded file. If it matches a supported output format, pre-select it. Otherwise default to GeoJSON. Per user decision: "same as input, fallback to GeoJSON."
         "Merge Files" button. If user has not changed format, send target_format as empty string for backend default.
       - Step 3 (Merging): Spinner with "Merging N files..." status text. POST to /api/transform/merge with FormData containing all files and target_format.
       - Step 4 (Done): Show stats (total rows, columns, warnings from X-Transform-Warnings header). Import and render OutputPreviewTable — pass the response body text and maxRows=20 to show a preview of the merged output. Download button. "Merge More" reset button. Same text/blob dual-read approach as CRS tool.
       - Error state: Same pattern as CRS tool.
       - Multi-file FormData: append each file as `files` field (FastAPI expects List[UploadFile] with same field name).
  </action>
  <verify>
    <automated>cd "C:/Users/danie/OneDrive/Desktop/BDS" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>CRS tool page renders with upload, CRS selection, transform, preview of first ~20 rows, and download flow. Output format defaults to same-as-input (fallback GeoJSON). Merge tool page renders with multi-file upload, preview, merge, output preview table, and download flow. Both follow the converter's three-step UI pattern.</done>
</task>

</tasks>

<verification>
npx next build succeeds with no errors. CRS and merge tool pages are accessible at /tools/transform/crs and /tools/transform/merge. Both done states show preview table of first ~20 rows.
</verification>

<success_criteria>
- Landing page cards link to tool sub-pages; Auto-Clean shows Coming Soon badge
- CRS tool: upload file, select source/target CRS, transform, preview first ~20 rows, download converted file
- Merge tool: upload multiple files, select output format, merge, preview first ~20 rows, download merged file
- Output format defaults to same-as-input with GeoJSON fallback
- API proxy routes forward to FastAPI with auth check
- All UI follows established converter three-step pattern
- OutputPreviewTable shared component created for reuse by split tool
</success_criteria>

<output>
After completion, create `.planning/phases/14-data-transform-tools/14-02-SUMMARY.md`
</output>
