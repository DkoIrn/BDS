---
phase: 08-results-dashboard
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/components/files/file-detail-view.tsx
  - src/components/jobs/job-results-table.tsx
  - src/app/(dashboard)/projects/[projectId]/jobs/[jobId]/page.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, PROJ-05]

must_haves:
  truths:
    - "File detail page has Mapping | Results | Data Preview tabs"
    - "Results tab auto-activates when dataset status is 'validated'"
    - "Results tab shows the full ResultsDashboard component with issues and stat cards"
    - "Job detail page Results tab shows a summary table of all datasets with verdict, issue count, pass rate"
    - "Job-level aggregate header shows count of validated datasets, total issues, PASS/FAIL breakdown"
    - "Clicking a dataset row in the job results table navigates to the file detail page"
  artifacts:
    - path: "src/components/files/file-detail-view.tsx"
      provides: "Tabbed file detail view with Mapping, Results, Data Preview tabs"
      exports: ["FileDetailView"]
    - path: "src/components/jobs/job-results-table.tsx"
      provides: "Job-level results summary table with aggregate header"
      exports: ["JobResultsTable"]
    - path: "src/app/(dashboard)/projects/[projectId]/jobs/[jobId]/page.tsx"
      provides: "Job page with wired Results tab"
  key_links:
    - from: "src/components/files/file-detail-view.tsx"
      to: "src/components/files/results-dashboard.tsx"
      via: "ResultsDashboard in Results tab"
      pattern: "<ResultsDashboard"
    - from: "src/components/jobs/job-results-table.tsx"
      to: "src/lib/actions/validation.ts"
      via: "getJobValidationSummary"
      pattern: "getJobValidationSummary"
    - from: "src/app/(dashboard)/projects/[projectId]/jobs/[jobId]/page.tsx"
      to: "src/components/jobs/job-results-table.tsx"
      via: "JobResultsTable component"
      pattern: "<JobResultsTable"
---

<objective>
Wire the results dashboard into existing pages: restructure FileDetailView with tabs and integrate ResultsDashboard, build job-level results table, and replace the placeholder in the job detail page.

Purpose: Connect the dashboard components from Plan 01 into the application so users can access results from both file-level and job-level views.
Output: 3 files modified/created -- tabbed FileDetailView, job results table component, updated job page.
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-results-dashboard/08-CONTEXT.md
@.planning/phases/08-results-dashboard/08-RESEARCH.md
@.planning/phases/08-results-dashboard/08-01-SUMMARY.md

@src/components/files/file-detail-view.tsx
@src/app/(dashboard)/projects/[projectId]/jobs/[jobId]/page.tsx
@src/lib/types/validation.ts

<interfaces>
<!-- From Plan 01 outputs -->

From src/components/files/results-dashboard.tsx:
```typescript
interface ResultsDashboardProps {
  datasetId: string
}
export function ResultsDashboard({ datasetId }: ResultsDashboardProps)
```

From src/lib/actions/validation.ts (new in Plan 01):
```typescript
export async function getJobValidationSummary(jobId: string): Promise<{ data: JobDatasetSummary[] } | { error: string }>
```

From src/lib/types/validation.ts (new in Plan 01):
```typescript
export interface JobDatasetSummary {
  id: string
  fileName: string
  verdict: 'PASS' | 'FAIL' | null
  issueCount: number
  passRate: number | null
  lastRunAt: string | null
  isValidated: boolean
}
```

From src/lib/utils/severity.ts (Plan 01):
```typescript
export function getVerdict(criticalCount: number): 'PASS' | 'FAIL'
export function formatRunDate(dateStr: string): string
```

Existing FileDetailView props:
```typescript
interface FileDetailViewProps {
  dataset: Dataset
  jobSurveyType: SurveyType
  projectId: string
  jobId: string
}
```

Existing Tabs API (base-ui via shadcn):
```typescript
// For controlled tabs, use value= and onValueChange=
<Tabs value={activeTab} onValueChange={setActiveTab}>
  <TabsList>
    <TabsTrigger value="mapping">Mapping</TabsTrigger>
  </TabsList>
  <TabsContent value="mapping">...</TabsContent>
</Tabs>
```

Pitfall from research: Use controlled value prop (not defaultValue) so Realtime subscription can auto-switch to Results tab when validation completes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FileDetailView tab restructure with ResultsDashboard</name>
  <files>src/components/files/file-detail-view.tsx</files>
  <action>
Restructure the existing FileDetailView component to use a tabbed layout with three tabs: Mapping | Results | Data Preview.

1. Add Tabs, TabsList, TabsTrigger, TabsContent imports from @/components/ui/tabs.
2. Import ResultsDashboard from @/components/files/results-dashboard.

3. Add controlled tab state:
   ```typescript
   const [activeTab, setActiveTab] = useState<string>(
     dataset.status === 'validated' ? 'results' : 'mapping'
   )
   ```

4. In the Realtime subscription handler (existing useEffect), when `newStatus === 'validated'`, add `setActiveTab('results')` to auto-switch to the Results tab. This addresses pitfall #1 from research.

5. Wrap the entire return JSX in a Tabs component:
   - **Mapping tab content:** Move existing content into this tab:
     - ParsingWarningBanner
     - Column Mappings section (header + edit/confirm buttons + ColumnMappingTable)
     - Validation Profile selector (ProfileSelector)
     - Run QC button section
     - ValidationProgress (validating state)
     - Validation error display
   - **Results tab content:**
     - Show `<ResultsDashboard datasetId={dataset.id} />` when dataset has been validated (datasetStatus === 'validated' or validationRun exists).
     - Show empty state when no validation run exists: centered message "No validation results yet. Run QC from the Mapping tab to see results."
     - Also show a "Re-run QC" button at the bottom of Results tab (calls handleRerun). This replaces the old ValidationSummary rerun button.
   - **Data Preview tab content:** Move existing DataPreviewTable section here.

6. Remove the inline ValidationSummary rendering (`datasetStatus === "validated" && validationRun && <ValidationSummary ...>`) since ResultsDashboard replaces it entirely.

7. Remove the ValidationSummary import. Keep the import of ValidationProgress (still used in Mapping tab during validating state).

8. The loading and error return blocks at the top of the component remain unchanged (they show before any tabs).

Key detail: The Tabs component should only render when `!loading && dataset.status !== 'error'` (the existing conditions that gate the main return). The loading/error returns at the top already handle those cases.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>FileDetailView renders 3 tabs. Mapping tab contains all column mapping + profile + validation controls. Results tab shows ResultsDashboard. Data Preview tab shows DataPreviewTable. Tab auto-switches to Results on validation complete via Realtime. ValidationSummary removed.</done>
</task>

<task type="auto">
  <name>Task 2: Job-level results table and page wiring</name>
  <files>src/components/jobs/job-results-table.tsx, src/app/(dashboard)/projects/[projectId]/jobs/[jobId]/page.tsx</files>
  <action>
1. Create `src/components/jobs/job-results-table.tsx` -- "use client" component:
   Props: `{ jobId: string, projectId: string }`.

   - On mount, fetch data via `getJobValidationSummary(jobId)`.
   - State: `summaries` (JobDatasetSummary[]), `loading` (boolean).

   - **Aggregate header** above the table:
     Compute from summaries: validatedCount (isValidated === true), totalDatasets, totalIssues (sum issueCount), failCount (verdict === 'FAIL'), passCount (verdict === 'PASS').
     Display: "{validatedCount} of {totalDatasets} datasets validated -- {totalIssues} total issues -- {failCount} FAIL, {passCount} PASS"
     Show this only when validatedCount > 0.

   - **Summary table** with columns: Dataset Name | Verdict | Issues | Pass Rate | Last Run.
     - Dataset Name: file name as link. Clicking navigates to `/projects/${projectId}/jobs/${jobId}/files/${summary.id}`.
     - Verdict: Badge -- green "PASS" or red "FAIL". Show dash if not validated.
     - Issues: number. Show dash if not validated.
     - Pass Rate: percentage with 1 decimal. Show dash if null.
     - Last Run: formatted date using `formatRunDate`. Show "Not run" if null.

   - Use Table, TableHeader, TableRow, TableHead, TableBody, TableCell from shadcn.
   - Use Badge with appropriate variant for verdict (green outline for PASS, destructive for FAIL).
   - Use Link from next/link for navigation.
   - Loading state: show Skeleton rows.
   - Empty state (no datasets): "No datasets in this job yet."
   - All-unvalidated state (datasets exist but none validated): Show the table with dashes in verdict/issues/rate columns.

2. Update `src/app/(dashboard)/projects/[projectId]/jobs/[jobId]/page.tsx`:
   - Import `JobResultsTable` from `@/components/jobs/job-results-table`.
   - Replace the Results tab placeholder (the BarChart3 empty state div) with `<JobResultsTable jobId={jobId} projectId={projectId} />`.
   - Remove the `BarChart3` import from lucide-react (no longer used).
   - Keep the existing Tabs with "files" and "results" TabTriggers unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Job detail page Results tab shows JobResultsTable with aggregate header and per-dataset summary rows. Clicking a dataset row navigates to the file detail page. Verdict, issue count, pass rate, and last run date display correctly. Empty and loading states handled.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- FileDetailView has 3 tabs and auto-switches to Results on validation complete
- Job detail page Results tab renders JobResultsTable instead of placeholder
- Navigation from job results table to file detail page works via Link
- No import errors between Plan 01 and Plan 02 components
</verification>

<success_criteria>
- File detail page shows Mapping | Results | Data Preview tabs
- Results tab auto-activates when validation completes via Realtime
- Results tab displays the full ResultsDashboard (stat cards, issues table, run switcher)
- Job detail page Results tab shows aggregate header with dataset counts and PASS/FAIL breakdown
- Job detail page shows per-dataset summary table with verdict, issues, pass rate, last run
- Clicking a dataset in the job results table navigates to its file detail page
- Old ValidationSummary component is no longer rendered in FileDetailView
</success_criteria>

<output>
After completion, create `.planning/phases/08-results-dashboard/08-02-SUMMARY.md`
</output>
