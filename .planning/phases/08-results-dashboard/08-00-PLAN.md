---
phase: 08-results-dashboard
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/components/issues-table.test.tsx
  - tests/components/results-stat-cards.test.tsx
  - tests/components/issue-row-detail.test.tsx
  - tests/components/run-switcher.test.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, PROJ-05]

must_haves:
  truths:
    - "Test stub files exist for all 4 dashboard components before implementation begins"
    - "Each test file has describe blocks matching the requirement it covers"
    - "All test stubs pass (placeholder expects) so the suite stays green"
  artifacts:
    - path: "tests/components/issues-table.test.tsx"
      provides: "DASH-01 test stubs for severity filtering and sorting"
    - path: "tests/components/results-stat-cards.test.tsx"
      provides: "DASH-02 test stubs for stat card rendering and color thresholds"
    - path: "tests/components/issue-row-detail.test.tsx"
      provides: "DASH-03 test stubs for expanded detail display"
    - path: "tests/components/run-switcher.test.tsx"
      provides: "PROJ-05 test stubs for run selection behavior"
  key_links: []
---

<objective>
Create Wave 0 test stub files for all 4 dashboard components so that implementation plans (08-01, 08-02) have test targets to verify against.

Purpose: The Validation Architecture requires these 4 test files to exist before implementation begins. Each plan's tasks will run vitest against these files as their verification step.
Output: 4 test stub files in tests/components/ with placeholder tests that pass.
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-results-dashboard/08-CONTEXT.md
@.planning/phases/08-results-dashboard/08-RESEARCH.md
@.planning/phases/08-results-dashboard/08-VALIDATION.md

@tests/components/file-upload-zone.test.tsx

<interfaces>
<!-- Types the test stubs will reference -->

From src/lib/types/validation.ts:
```typescript
export type ValidationSeverity = 'critical' | 'warning' | 'info'

export interface ValidationRun {
  id: string
  dataset_id: string
  run_at: string
  total_issues: number
  critical_count: number
  warning_count: number
  info_count: number
  pass_rate: number | null
  completeness_score: number | null
  status: string
  created_at: string
  config_snapshot: ProfileConfig | null
  profile_id: string | null
}

export interface ValidationIssue {
  id: string
  run_id: string
  dataset_id: string
  row_number: number
  column_name: string
  rule_type: string
  severity: ValidationSeverity
  message: string
  expected: string | null
  actual: string | null
  kp_value: number | null
  created_at: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 test stub files for dashboard components</name>
  <files>tests/components/issues-table.test.tsx, tests/components/results-stat-cards.test.tsx, tests/components/issue-row-detail.test.tsx, tests/components/run-switcher.test.tsx</files>
  <action>
Create 4 test stub files following the exact pattern from `tests/components/file-upload-zone.test.tsx` (vi.mock, describe blocks, placeholder expects).

1. **`tests/components/issues-table.test.tsx`** (DASH-01):
   - Mock `@/lib/actions/validation` (getValidationIssues)
   - Mock `@/lib/utils/severity` (getSeverityColor, getSeverityIcon)
   - `describe('IssuesTable')` with nested describes:
     - `describe('severity filtering')`: stubs for "shows All tab with total count", "filters to critical issues", "filters to warning issues", "filters to info issues", "tab counts match filtered results"
     - `describe('sorting')`: stubs for "sorts by row number ascending by default", "toggles sort direction on column click", "sorts severity by ordinal (critical > warning > info)"
     - `describe('expandable rows')`: stubs for "expands row on click", "collapses expanded row on second click"

2. **`tests/components/results-stat-cards.test.tsx`** (DASH-02):
   - Mock `@/lib/utils/severity` (getPassRateColor, getVerdict, getSeverityColor)
   - `describe('ResultsStatCards')` with nested describes:
     - `describe('verdict banner')`: stubs for "shows PASS when no critical issues", "shows FAIL when critical issues exist"
     - `describe('stat cards')`: stubs for "displays total issues count", "displays pass rate percentage", "displays data completeness percentage", "shows N/A when pass rate is null"
     - `describe('color coding')`: stubs for "green border when pass rate >= 90", "yellow border when pass rate 70-89", "red border when pass rate < 70"
     - `describe('severity badges')`: stubs for "clicking critical badge calls onSeverityClick", "badges show correct counts"

3. **`tests/components/issue-row-detail.test.tsx`** (DASH-03):
   - Mock `@/lib/actions/validation` (getIssueContext)
   - `describe('IssueRowDetail')` with nested describes:
     - `describe('loading state')`: stubs for "shows skeleton while loading context"
     - `describe('detail display')`: stubs for "shows full explanation message", "shows expected vs actual values", "shows rule type badge", "shows KP value when present"
     - `describe('surrounding context')`: stubs for "displays surrounding rows table", "highlights flagged row", "shows correct number of context rows"
     - `describe('error handling')`: stubs for "shows error message when context fetch fails"

4. **`tests/components/run-switcher.test.tsx`** (PROJ-05):
   - Mock `@/lib/utils/severity` (formatRunDate)
   - `describe('RunSwitcher')` with nested describes:
     - `describe('run display')`: stubs for "shows formatted date for each run", "shows issue count for each run", "shows profile name from config_snapshot"
     - `describe('selection')`: stubs for "calls onRunChange when run selected", "shows selected run in trigger"
     - `describe('edge cases')`: stubs for "handles single run (no dropdown needed)"

Each stub test should use the pattern: `it('description', () => { expect(true).toBe(true) })` as placeholder.
All stubs import from vitest: `import { describe, it, expect, vi } from 'vitest'`.
  </action>
  <verify>
    <automated>cd "C:/Users/danie/OneDrive/Desktop/BDS" && npx vitest run tests/components/issues-table.test.tsx tests/components/results-stat-cards.test.tsx tests/components/issue-row-detail.test.tsx tests/components/run-switcher.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 test stub files exist in tests/components/. All placeholder tests pass. Stubs cover DASH-01 (severity filtering, sorting, expandable rows), DASH-02 (verdict, stat cards, color coding, severity badges), DASH-03 (loading, detail display, surrounding context, error handling), and PROJ-05 (run display, selection, edge cases).</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/components/ --reporter=verbose` passes with all 4 new test files
- Each test file has describe blocks matching its requirement coverage
- Existing tests are not broken
</verification>

<success_criteria>
- 4 test stub files exist before Plan 01 execution begins
- All stubs pass (green suite)
- Test structure matches the behaviors that Plan 01 and Plan 02 components must satisfy
</success_criteria>

<output>
After completion, create `.planning/phases/08-results-dashboard/08-00-SUMMARY.md`
</output>
