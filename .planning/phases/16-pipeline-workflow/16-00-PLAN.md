---
phase: 16-pipeline-workflow
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/pipeline/pipeline-state.test.ts
  - tests/pipeline/pipeline-store.test.ts
  - tests/pipeline/pipeline-stepper.test.ts
  - tests/pipeline/stage-dispatch.test.ts
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04
  - PIPE-05
  - PIPE-07

must_haves:
  truths:
    - "Test stubs exist for all unit-testable pipeline behaviors"
    - "All test files run and report pending/skipped status without errors"
  artifacts:
    - path: "tests/pipeline/pipeline-state.test.ts"
      provides: "Test stubs for reducer actions and gating logic"
    - path: "tests/pipeline/pipeline-store.test.ts"
      provides: "Test stubs for sessionStorage save/load/clear round-trip"
    - path: "tests/pipeline/pipeline-stepper.test.ts"
      provides: "Test stubs for stepper visual state rendering"
    - path: "tests/pipeline/stage-dispatch.test.ts"
      provides: "Test stubs for stage panel dispatch assertions"
  key_links: []
---

<objective>
Create test stub files (Wave 0) for all unit-testable pipeline behaviors, establishing the test contracts that Plans 01 and 02 must satisfy.

Purpose: Nyquist compliance requires automated verification for every task. These stubs define expected behaviors before implementation begins.

Output: Four test files with descriptive test cases that initially fail or skip, ready to go green as Plans 01 and 02 implement the production code.
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-pipeline-workflow/16-CONTEXT.md
@.planning/phases/16-pipeline-workflow/16-RESEARCH.md
@.planning/phases/16-pipeline-workflow/16-VALIDATION.md
@vitest.config.ts
@tests/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for pipeline state, store, stepper, and stage dispatches</name>
  <files>
    tests/pipeline/pipeline-state.test.ts,
    tests/pipeline/pipeline-store.test.ts,
    tests/pipeline/pipeline-stepper.test.ts,
    tests/pipeline/stage-dispatch.test.ts
  </files>
  <action>
Create `tests/pipeline/` directory and four test files. All tests should use `describe` and `it` blocks with descriptive names. Use `it.todo()` for tests that depend on components not yet built (stepper rendering), and use `it.skip()` or real assertions for pure-logic tests that can be written now with placeholder imports.

**pipeline-state.test.ts** (PIPE-01, PIPE-02):

```typescript
import { describe, it, expect } from 'vitest'
// Will import from '@/app/(dashboard)/pipeline/lib/pipeline-state'
```

Describe blocks:
1. `describe('pipelineReducer')`:
   - `it('IMPORT_FILE resets state and advances to inspect')`
   - `it('IMPORT_EXISTING sets datasetId and advances to inspect')`
   - `it('INSPECT_COMPLETE stores parsed data and advances to validate')`
   - `it('SKIP_VALIDATE marks validate as skipped and advances to clean')`
   - `it('VALIDATE_START keeps current stage as validate')`
   - `it('VALIDATE_COMPLETE stores run data and advances to clean')`
   - `it('SKIP_CLEAN marks clean as skipped and advances to export')`
   - `it('CLEAN_COMPLETE stores cleaned data and advances to export')`
   - `it('SET_EXPORT_FORMAT updates export format')`
   - `it('GO_TO_STAGE transitions when canNavigateTo returns true')`
   - `it('GO_TO_STAGE does NOT transition when canNavigateTo returns false')`
   - `it('RESET returns initial state')`

2. `describe('canNavigateTo')`:
   - `it('always allows navigation to import')`
   - `it('allows inspect only when import is completed')`
   - `it('allows validate only when inspect is completed')`
   - `it('allows clean when inspect is completed (validate can be skipped)')`
   - `it('allows export when import is completed')`
   - `it('blocks inspect when import is not completed')`
   - `it('blocks validate when inspect is not completed')`

All tests should be `it.todo()` stubs initially. The executor of Plan 01 will fill in the assertions after creating pipeline-state.ts.

**pipeline-store.test.ts** (PIPE-03, PIPE-07):

```typescript
import { describe, it, expect, beforeEach } from 'vitest'
// Will import from '@/app/(dashboard)/pipeline/lib/pipeline-store'
```

Describe block `describe('pipeline-store')`:
- `it('savePipelineState writes to sessionStorage')`
- `it('loadPipelineState reads back saved state')`
- `it('save then load round-trips serializable fields correctly')`
- `it('loadPipelineState returns null when no saved state')`
- `it('loadPipelineState returns null on corrupted JSON')`
- `it('clearPipelineState removes state from sessionStorage')`
- `it('omits parsedData and cleanedData from serialized state')`

All `it.todo()` stubs. These test pure logic (sessionStorage mock is available in jsdom) and will be filled in by Plan 01 executor.

**pipeline-stepper.test.ts** (PIPE-04):

```typescript
import { describe, it, expect } from 'vitest'
// Will import PipelineStepper and render with @testing-library/react
```

Describe block `describe('PipelineStepper')`:
- `it.todo('renders all 5 stage labels')`
- `it.todo('marks current stage with current visual style')`
- `it.todo('marks completed stages with check icon')`
- `it.todo('marks skipped stages with warning icon')`
- `it.todo('calls onStageClick when a navigable stage is clicked')`
- `it.todo('does not call onStageClick for non-navigable stages')`

These are component tests requiring @testing-library/react. Use `it.todo()` since the component does not exist yet.

**stage-dispatch.test.ts** (PIPE-05):

```typescript
import { describe, it, expect } from 'vitest'
// Tests that stage panels dispatch correct actions
```

Describe blocks:
1. `describe('StageImport dispatch')`:
   - `it.todo('dispatches IMPORT_FILE with fileName on file drop')`
   - `it.todo('dispatches IMPORT_EXISTING with datasetId on dataset select')`
   - `it.todo('dispatches RESET when re-import button is clicked')`

2. `describe('StageInspect dispatch')`:
   - `it.todo('dispatches INSPECT_COMPLETE after parsing uploaded file')`

These are component integration tests. Use `it.todo()` since stage panels do not exist yet.

IMPORTANT: Each test file must be syntactically valid TypeScript that vitest can discover. Use only `it.todo('description')` for stubs -- do NOT write assertion bodies that reference non-existent imports.
  </action>
  <verify>
    <automated>npx vitest run tests/pipeline/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Four test files exist under tests/pipeline/. Running vitest discovers all test stubs and reports them as todo/skipped with zero failures. Test file count: pipeline-state (12+ stubs), pipeline-store (7 stubs), pipeline-stepper (6 stubs), stage-dispatch (4 stubs).</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/pipeline/ --reporter=verbose` discovers all 4 test files
2. All tests report as todo/skipped (not failing)
3. No import errors (stubs do not import non-existent modules)
</verification>

<success_criteria>
- tests/pipeline/ directory contains 4 test files
- vitest discovers and lists all test stubs
- Zero test failures (all todo/skipped)
- Test names match the behaviors specified in VALIDATION.md and RESEARCH.md
</success_criteria>

<output>
After completion, create `.planning/phases/16-pipeline-workflow/16-00-SUMMARY.md`
</output>
