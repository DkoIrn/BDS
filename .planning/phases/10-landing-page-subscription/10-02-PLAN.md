---
phase: 10-landing-page-subscription
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/landing/landing-navbar.tsx
  - src/components/landing/landing-page.tsx
  - src/app/page.tsx
  - src/app/globals.css
autonomous: false
requirements:
  - UIDE-03
  - SUBS-01
  - SUBS-02

must_haves:
  truths:
    - "Unauthenticated user visiting / sees the full landing page with all sections"
    - "Authenticated user visiting / is redirected to /dashboard"
    - "Sticky navbar shows anchor links to Features, How it works, Pricing"
    - "Clicking anchor links smooth-scrolls to the correct section"
    - "Navbar shows Login and Get Started buttons linking to /login and /signup"
    - "Mobile layout stacks content vertically and shows hamburger menu"
    - "Landing page looks professional with brand palette and clean typography"
  artifacts:
    - path: "src/components/landing/landing-navbar.tsx"
      provides: "Sticky header with anchor links, logo, Login/Signup buttons, mobile menu"
    - path: "src/components/landing/landing-page.tsx"
      provides: "Full page composition of all landing sections"
    - path: "src/app/page.tsx"
      provides: "Auth-gated root page: landing for unauth, redirect for auth"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/landing/landing-page.tsx"
      via: "import and render"
      pattern: "import.*LandingPage"
    - from: "src/components/landing/landing-page.tsx"
      to: "src/components/landing/hero-section.tsx"
      via: "import and compose"
      pattern: "import.*HeroSection"
    - from: "src/components/landing/landing-page.tsx"
      to: "src/components/landing/pricing-section.tsx"
      via: "import and compose"
      pattern: "import.*PricingSection"
    - from: "src/app/page.tsx"
      to: "/dashboard"
      via: "redirect for authenticated users"
      pattern: "redirect.*dashboard"
---

<objective>
Wire landing page sections into a complete page with navigation, auth-gated routing, and smooth scrolling.

Purpose: Compose all section components into a full landing page, add the landing-specific navbar with anchor navigation and mobile menu, update root page.tsx to serve the landing page for unauthenticated visitors, and verify the complete experience.
Output: Working landing page at / for unauthenticated users, redirect to /dashboard for authenticated users.
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-landing-page-subscription/10-CONTEXT.md
@.planning/phases/10-landing-page-subscription/10-RESEARCH.md
@.planning/phases/10-landing-page-subscription/10-01-SUMMARY.md

<interfaces>
<!-- Components created in Plan 01 that this plan composes -->
From src/components/landing/hero-section.tsx: export function HeroSection()
From src/components/landing/features-section.tsx: export function FeaturesSection()
From src/components/landing/how-it-works-section.tsx: export function HowItWorksSection()
From src/components/landing/pricing-section.tsx: export function PricingSection()
From src/components/landing/cta-section.tsx: export function CtaSection()
From src/components/landing/landing-footer.tsx: export function LandingFooter()

<!-- Existing auth pattern from current page.tsx -->
From src/lib/supabase/server.ts: export async function createClient()
<!-- Usage: const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser() -->

<!-- shadcn Sheet component available for mobile menu -->
From src/components/ui/sheet.tsx: Sheet, SheetTrigger, SheetContent, SheetHeader, SheetTitle

<!-- Important project decisions -->
<!-- Button does NOT support asChild in shadcn v4 -- use styled Link elements -->
<!-- Use getUser() not getSession() for server-side auth checks -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create landing navbar, page composition, root page update, and smooth scroll</name>
  <files>src/components/landing/landing-navbar.tsx, src/components/landing/landing-page.tsx, src/app/page.tsx, src/app/globals.css</files>
  <action>
**landing-navbar.tsx** (client component -- needs scroll state):
- "use client" directive
- Sticky header: `sticky top-0 z-50 transition-colors`
- Scroll detection via useEffect: when scrollY > 10, add `border-b bg-background/95 backdrop-blur`, otherwise `bg-transparent`
- Left: Logo -- "SQ" box (size-8 rounded-md bg-primary text-primary-foreground font-bold) + "SurveyQC" text. Link using anchor `href="#"` (scrolls to top)
- Center: Anchor links for Features (#features), How it works (#how-it-works), Pricing (#pricing). Style: `text-sm text-muted-foreground hover:text-foreground transition-colors`. Hide on mobile with `hidden md:flex`
- Right: Login link to /login (`text-sm font-medium`), "Get Started" styled Link to /signup with accent colors (`inline-flex h-9 items-center justify-center rounded-md bg-accent px-4 text-sm font-medium text-accent-foreground hover:bg-accent/90 transition-colors`)
- Mobile hamburger: Show Menu icon button on `md:hidden`. Use shadcn Sheet component (SheetTrigger, SheetContent) for slide-out menu containing anchor links + Login + Get Started. Import from @/components/ui/sheet
- Max-width container: `mx-auto flex h-16 max-w-6xl items-center px-4 sm:px-6`

**landing-page.tsx** (server component -- no "use client"):
- Import and compose all 6 sections in order: LandingNavbar, HeroSection, FeaturesSection, HowItWorksSection, PricingSection, CtaSection, LandingFooter
- Wrap in a single `<div className="min-h-screen">` or fragment
- Export as default function LandingPage

**page.tsx** (update existing root page):
- Keep the existing auth check pattern: `createClient()` -> `getUser()`
- If user exists: `redirect("/dashboard")` (keep current behavior)
- If no user: render `<LandingPage />` instead of `redirect("/login")`
- Import LandingPage from "@/components/landing/landing-page"

**globals.css** (add smooth scroll):
- Add `scroll-behavior: smooth;` to the existing `html` rule, or add a new rule if none exists:
```css
html {
  scroll-behavior: smooth;
}
```
  </action>
  <verify>
    <automated>npx vitest run tests/components/landing-page.test.tsx tests/components/pricing-section.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Root page.tsx renders LandingPage for unauthenticated visitors. Landing navbar has anchor links and Login/Signup buttons. All sections compose correctly in order. Smooth scroll CSS is added. All landing page tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete landing page experience</name>
  <action>
Human verifies the complete landing page visually and functionally.

What was built: Complete landing page with hero, features, how-it-works, pricing tiers, CTA, footer, and sticky navbar with anchor navigation.

How to verify:
1. Run `npm run dev` and visit http://localhost:3000 in an incognito/private browser window (unauthenticated)
2. Verify the landing page renders with:
   - Sticky navbar with SQ logo, anchor links (Features, How it works, Pricing), Login, Get Started
   - Hero section with pain-point headline, subtext, CTA button, product mockup
   - Features section with 6 cards and Lucide icons
   - How-it-works section with 4 numbered steps
   - Pricing section with 3 tiers (Starter $49, Professional $149 highlighted, Enterprise Contact Us)
   - Final CTA section
   - Footer with copyright and links
3. Click each navbar anchor link -- verify smooth scroll to correct section
4. Click "Get Started" button in hero -- verify navigates to /signup
5. Click "Log in" in navbar -- verify navigates to /login
6. Resize browser to mobile width (~375px) -- verify:
   - Cards stack vertically
   - Hamburger menu appears with anchor links
   - Pricing cards stack vertically
7. Log in to the app, then visit http://localhost:3000 -- verify redirect to /dashboard

Resume signal: Type "approved" or describe issues.
  </action>
  <verify>Human visual and functional verification</verify>
  <done>Landing page approved by user: professional appearance, working navigation, responsive layout, correct pricing tiers, all CTAs functional</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/components/landing-page.test.tsx tests/components/pricing-section.test.tsx --reporter=verbose` -- all tests pass
- `npx vitest run` -- full test suite passes (no regressions)
- Unauthenticated visit to / shows landing page
- Authenticated visit to / redirects to /dashboard
- Anchor links smooth-scroll to correct sections
- Mobile layout is responsive with hamburger menu
</verification>

<success_criteria>
- Landing page visible at / for unauthenticated users
- Auth redirect preserved for logged-in users
- Sticky navbar with working anchor links and smooth scroll
- Mobile-responsive layout with hamburger menu
- All 3 subscription tiers clearly displayed with feature breakdowns
- Professional tier highlighted as "Most Popular"
- All CTA buttons link to /signup
- Brand palette consistently applied (Deep Blue, Teal, Orange)
- Clean, professional design matching Datadog/Linear aesthetic
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-landing-page-subscription/10-02-SUMMARY.md`
</output>
