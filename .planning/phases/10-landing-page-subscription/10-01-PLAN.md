---
phase: 10-landing-page-subscription
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/components/landing-page.test.tsx
  - tests/components/pricing-section.test.tsx
  - src/components/landing/hero-section.tsx
  - src/components/landing/features-section.tsx
  - src/components/landing/how-it-works-section.tsx
  - src/components/landing/pricing-section.tsx
  - src/components/landing/cta-section.tsx
  - src/components/landing/landing-footer.tsx
autonomous: true
requirements:
  - UIDE-03
  - SUBS-01
  - SUBS-02

must_haves:
  truths:
    - "Landing page renders hero section with pain-point headline and CTA to /signup"
    - "Landing page renders features section with 3-6 feature cards using Lucide icons"
    - "Landing page renders how-it-works section with Upload/Detect/Validate/Report steps"
    - "Pricing section displays 3 tiers (Starter, Professional, Enterprise) with feature lists"
    - "Professional tier is highlighted as recommended with a visual distinction"
    - "Enterprise tier shows Contact Us instead of a price"
    - "Final CTA section links to /signup"
  artifacts:
    - path: "src/components/landing/hero-section.tsx"
      provides: "Hero with headline, subtext, CTA button, product mockup"
    - path: "src/components/landing/features-section.tsx"
      provides: "Feature cards grid with Lucide icons"
    - path: "src/components/landing/how-it-works-section.tsx"
      provides: "Step-by-step pipeline visual"
    - path: "src/components/landing/pricing-section.tsx"
      provides: "3-tier pricing cards with feature breakdowns"
    - path: "src/components/landing/cta-section.tsx"
      provides: "Final call-to-action with signup link"
    - path: "src/components/landing/landing-footer.tsx"
      provides: "Minimal footer with copyright and links"
    - path: "tests/components/landing-page.test.tsx"
      provides: "Tests for landing page section rendering"
    - path: "tests/components/pricing-section.test.tsx"
      provides: "Tests for pricing tier display"
  key_links:
    - from: "src/components/landing/pricing-section.tsx"
      to: "/signup"
      via: "CTA button links"
      pattern: "href.*signup"
    - from: "src/components/landing/hero-section.tsx"
      to: "/signup"
      via: "Primary CTA link"
      pattern: "href.*signup"
---

<objective>
Create all landing page section components and test stubs for the SurveyQC public-facing page.

Purpose: Build the individual content sections (hero, features, how-it-works, pricing, CTA, footer) that compose the landing page, plus test coverage for key requirements.
Output: 6 section components in src/components/landing/ and 2 test files.
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-landing-page-subscription/10-CONTEXT.md
@.planning/phases/10-landing-page-subscription/10-RESEARCH.md
@.planning/phases/10-landing-page-subscription/10-VALIDATION.md

<interfaces>
<!-- Existing UI components available for use -->

From src/components/ui/card.tsx: Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter
From src/components/ui/button.tsx: Button (note: does NOT support asChild -- use styled Link instead)
From src/components/ui/badge.tsx: Badge
From src/components/ui/separator.tsx: Separator

<!-- Brand colors from globals.css -->
Primary: Deep Blue #1E3A8A (--primary)
Secondary: Teal #14B8A6 (--secondary)
Accent: Orange #F97316 (--accent)
Background: #F8FAFC (--background)

<!-- Important: Button does NOT support asChild per project decision -->
<!-- Use styled Link elements for navigation buttons: -->
<!-- <Link href="/signup" className="inline-flex h-10 items-center ...">Get Started</Link> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for landing page and pricing section</name>
  <files>tests/components/landing-page.test.tsx, tests/components/pricing-section.test.tsx</files>
  <action>
Create two test files that cover the three phase requirements:

**tests/components/landing-page.test.tsx** (covers UIDE-03):
- Test that LandingPage component renders all expected sections: hero (with heading text), features (with "Features" heading or feature card text), how-it-works, pricing, CTA, and footer
- Test that hero section contains a link to /signup
- Test that the page contains anchor-target ids: "features", "how-it-works", "pricing"
- Import LandingPage from "@/components/landing/landing-page" (will be created in Plan 02)

**tests/components/pricing-section.test.tsx** (covers SUBS-01, SUBS-02):
- Test that PricingSection renders exactly 3 tier names: "Starter", "Professional", "Enterprise"
- Test that Starter and Professional tiers display dollar prices
- Test that Enterprise tier displays "Contact Us" (not a dollar price)
- Test that Professional tier has a visual highlight (check for a distinguishing class or "Most Popular" badge)
- Test that each tier has a list of features (at least 3 items per tier)
- Test that all CTA buttons link to /signup
- Import PricingSection from "@/components/landing/pricing-section"

Use render() from @testing-library/react, screen queries (getByText, getAllByText, getByRole, etc.). Follow existing test patterns in tests/components/ (see issues-table.test.tsx for reference).
  </action>
  <verify>
    <automated>npx vitest run tests/components/landing-page.test.tsx tests/components/pricing-section.test.tsx --reporter=verbose 2>&1 | head -5</automated>
  </verify>
  <done>Test files exist and are syntactically valid (they will fail until components are built -- that is expected for Wave 0)</done>
</task>

<task type="auto">
  <name>Task 2: Build all landing page section components</name>
  <files>src/components/landing/hero-section.tsx, src/components/landing/features-section.tsx, src/components/landing/how-it-works-section.tsx, src/components/landing/pricing-section.tsx, src/components/landing/cta-section.tsx, src/components/landing/landing-footer.tsx</files>
  <action>
Create 6 server components (no "use client" needed -- these are purely presentational) in src/components/landing/. Use a consistent Section wrapper pattern for spacing: `scroll-mt-16 py-20 sm:py-28` with `mx-auto max-w-6xl px-4 sm:px-6`.

**hero-section.tsx:**
- Pain-point led headline (e.g., "Stop Spending Hours Manually Checking Survey Data")
- Subtext explaining automated QC with explainable flags, minutes instead of hours
- Primary CTA: styled Link to /signup with orange accent background (`bg-accent text-accent-foreground hover:bg-accent/90`). Do NOT use Button asChild (not supported in shadcn v4) -- use a styled Link element
- Secondary visual: a placeholder product mockup div styled as a browser window frame (rounded corners, dot-row title bar, gradient/skeleton content inside) with a subtle shadow. Use `next/image` if a real screenshot is placed in /public, otherwise use a styled placeholder div
- Responsive: text left / mockup right on lg, stacked on mobile
- Section does NOT need an id (it is the top of the page)

**features-section.tsx:**
- id="features" with scroll-mt-16
- Section heading: "Everything you need for survey QC"
- 3-6 feature cards in a responsive grid (grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6)
- Each card: Lucide icon (teal/secondary color), feature title, 1-2 sentence description
- Features to highlight (pick 6): Column Auto-Detection, Anomaly Flagging, Explainable Reports, Validation Profiles, Async Processing, Dataset Export
- Use Card component from shadcn/ui for each feature card
- Icons to use from lucide-react: Search (auto-detection), AlertTriangle (anomaly), FileText (reports), SlidersHorizontal (profiles), Zap (async), Download (export)

**how-it-works-section.tsx:**
- id="how-it-works" with scroll-mt-16
- Section heading: "How it works"
- 4 numbered steps in a horizontal row (lg) / vertical stack (mobile):
  1. Upload -- "Drop your CSV or Excel files"
  2. Auto-detect -- "Columns are automatically identified"
  3. Validate -- "QC checks run in the background"
  4. Report -- "Download your annotated results"
- Each step: large number (text-4xl font-bold text-primary/20), step title, short description
- Connect steps with a subtle line/arrow on desktop (use border or a connecting element between steps)
- Icons from lucide-react: Upload, Scan, CheckCircle, FileDown

**pricing-section.tsx:**
- id="pricing" with scroll-mt-16
- Section heading: "Simple, transparent pricing"
- Subtext: "Choose the plan that fits your survey operation"
- 3 cards in grid-cols-1 lg:grid-cols-3 gap-8
- Define PricingTier interface and tiers data array inline (not a separate file):
  - Starter: $49/mo, 5 projects, 10 uploads/mo, 25MB files, basic validators, PDF reports
  - Professional: $149/mo, unlimited projects, 100 uploads/mo, 50MB files, all validators + profiles, PDF + dataset export, priority processing. Highlighted with `border-secondary ring-2 ring-secondary` and "Most Popular" Badge
  - Enterprise: price null -> "Contact Us", everything in Professional + unlimited uploads, 100MB files, API access, custom validators, dedicated support, SSO
- Each card: tier name, price (or "Contact Us"), description, feature list with check icons, CTA button linking to /signup (styled Link, not Button asChild)
- Use Card, CardHeader, CardContent, CardFooter from shadcn/ui
- Use Check icon from lucide-react for feature list items

**cta-section.tsx:**
- Final call-to-action section with a gradient or tinted background (bg-primary text-primary-foreground)
- Heading: "Ready to automate your survey QC?"
- Subtext: short sentence about getting started in minutes
- CTA button: styled Link to /signup with accent colors (or white on dark bg)
- Centered layout

**landing-footer.tsx:**
- Minimal footer with subtle top border
- Left: "SQ" logo + "SurveyQC" text + copyright year
- Right: 3 links -- Login (/login), Privacy (#), Terms (#)
- Use text-sm text-muted-foreground styling
- Max-width consistent with other sections (max-w-6xl)
  </action>
  <verify>
    <automated>npx vitest run tests/components/pricing-section.test.tsx --reporter=verbose</automated>
  </verify>
  <done>All 6 section components render correctly. Pricing section test passes confirming 3 tiers with correct names, prices, features, and CTA links. Feature cards display with Lucide icons. All sections use consistent spacing and responsive grid layouts.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/components/landing-page.test.tsx tests/components/pricing-section.test.tsx --reporter=verbose` -- pricing tests pass (landing-page tests may fail until Plan 02 creates the composition component)
- All 6 component files exist in src/components/landing/
- Pricing section renders 3 tiers: Starter ($49), Professional ($149, highlighted), Enterprise (Contact Us)
- Feature section renders 6 cards with Lucide icons
- No new npm dependencies added
</verification>

<success_criteria>
- 6 section components created and rendering correctly
- 2 test files created covering UIDE-03, SUBS-01, SUBS-02
- Pricing tests pass with correct tier names, prices, and features
- All CTA buttons link to /signup
- Responsive grid layouts (stack on mobile, multi-column on desktop)
- Brand palette applied (Deep Blue primary, Teal accents, Orange CTAs)
</success_criteria>

<output>
After completion, create `.planning/phases/10-landing-page-subscription/10-01-SUMMARY.md`
</output>
