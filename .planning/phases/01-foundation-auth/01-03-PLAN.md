---
phase: 01-foundation-auth
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/(dashboard)/layout.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/(dashboard)/settings/page.tsx
  - src/components/app-sidebar.tsx
  - src/components/top-bar.tsx
autonomous: false
requirements:
  - AUTH-03
  - UIDE-01
  - UIDE-02

must_haves:
  truths:
    - "Authenticated user sees sidebar with Dashboard and Settings navigation items"
    - "Sidebar collapses from full labels to icon-only mode"
    - "Sidebar auto-collapses to icon-only on tablet breakpoints"
    - "Top bar shows breadcrumb trail on left and user avatar dropdown on right"
    - "User can log out from the user dropdown in the top bar"
    - "Dashboard page shows a welcome message"
    - "Settings page allows viewing profile info and changing password"
    - "Brand colors are visibly applied throughout the shell"
  artifacts:
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Dashboard layout with SidebarProvider, AppSidebar, TopBar"
      min_lines: 15
    - path: "src/components/app-sidebar.tsx"
      provides: "Collapsible sidebar with Dashboard and Settings nav items"
      min_lines: 40
    - path: "src/components/top-bar.tsx"
      provides: "Top bar with breadcrumbs, notification bell placeholder, user dropdown"
      min_lines: 30
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Welcome dashboard page"
      min_lines: 10
    - path: "src/app/(dashboard)/settings/page.tsx"
      provides: "Settings page with profile and password sections"
      min_lines: 40
  key_links:
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/app-sidebar.tsx"
      via: "imports and renders AppSidebar"
      pattern: "AppSidebar"
    - from: "src/components/top-bar.tsx"
      to: "src/lib/actions/auth.ts"
      via: "logout action in user dropdown"
      pattern: "logout"
    - from: "src/components/app-sidebar.tsx"
      to: "@/components/ui/sidebar"
      via: "uses shadcn/ui Sidebar components"
      pattern: "SidebarMenu"
---

<objective>
Build the authenticated app shell: collapsible sidebar, top bar with breadcrumbs and user dropdown, dashboard welcome page, and settings page.

Purpose: This delivers the application shell that all future phases build inside. The sidebar, top bar, and layout establish the UX patterns for the entire product. The logout action in the user dropdown completes AUTH-03. The brand design system (UIDE-01) and responsive layout (UIDE-02) are demonstrated in the shell.
Output: Complete app shell with sidebar navigation, top bar, dashboard page, and settings page.
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app shell layout with collapsible sidebar and top bar</name>
  <files>
    src/app/(dashboard)/layout.tsx,
    src/components/app-sidebar.tsx,
    src/components/top-bar.tsx
  </files>
  <action>
    1. Create `src/components/app-sidebar.tsx` using shadcn/ui Sidebar component:
       - Use `Sidebar` with `collapsible="icon"` prop for icon-only collapse mode
       - **Header section**: App logo/name "SurveyQC AI" -- shows full name when expanded, icon/initials when collapsed
       - **Content section** with `SidebarMenu`:
         - Dashboard item: LayoutDashboard icon from lucide-react, label "Dashboard", links to `/dashboard`
         - Settings item: Settings icon from lucide-react, label "Settings", links to `/settings`
         - Only these two items in Phase 1 (per user decision: no disabled/greyed-out future features)
       - **Footer section**: User info with avatar and name (get user from Supabase server client), using `SidebarMenuButton` that shows user email/name when expanded and avatar when collapsed
       - Use `SidebarGroup`, `SidebarGroupContent`, `SidebarMenuItem`, `SidebarMenuButton` from shadcn/ui
       - Highlight active nav item based on current pathname (use `usePathname` from next/navigation)
       - Apply Deep Blue (#1E3A8A) as the sidebar background or use a subtle brand-colored sidebar style. The sidebar should feel distinctly branded.
       - This component needs to be a Client Component ('use client') for pathname detection. Pass user data as props from the layout (which is a Server Component).

    2. Create `src/components/top-bar.tsx`:
       - Left side: `SidebarTrigger` button (hamburger/collapse toggle from shadcn/ui Sidebar), then a `Separator`, then `Breadcrumb` component showing current page name
       - Right side: Notification bell icon (Bell from lucide-react) as a disabled/placeholder button (no functionality until Phase 7 -- just the icon for visual consistency per user's specific idea), then user avatar with `DropdownMenu`:
         - Dropdown items: user email (display only), separator, "Settings" (links to /settings), "Log out" (calls logout server action)
       - Use `SidebarInset` header pattern: `<header className="flex h-16 items-center gap-2 px-4 border-b">`
       - This is a Client Component for dropdown interaction. Receive user data as props.

    3. Create `src/app/(dashboard)/layout.tsx` following Pattern 6 from research:
       - This is a Server Component
       - Fetch the current user using the Supabase server client (`getUser()`)
       - Wrap children in `SidebarProvider` > `AppSidebar` + `SidebarInset` > `TopBar` + `<main>`
       - Pass user data (email, full_name) to AppSidebar and TopBar as props
       - Main content area: `<main className="flex-1 p-6">` wrapping `{children}`
       - If no user found (shouldn't happen due to middleware, but defensive), redirect to /login

    Responsive behavior (UIDE-02):
    - On desktop (lg+): sidebar shows full labels, can be toggled to icon-only
    - On tablet (md): sidebar auto-collapses to icon-only mode (set `defaultOpen={false}` or use the `open` cookie-based state from shadcn/ui Sidebar)
    - On mobile (sm): sidebar becomes a Sheet (built into shadcn/ui Sidebar component automatically)
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -10</automated>
  </verify>
  <done>Dashboard layout renders with sidebar and top bar. Sidebar has Dashboard and Settings items. Sidebar collapses to icon-only. Top bar shows breadcrumbs and user dropdown. User dropdown has logout option.</done>
</task>

<task type="auto">
  <name>Task 2: Create dashboard welcome page and settings page</name>
  <files>
    src/app/(dashboard)/dashboard/page.tsx,
    src/app/(dashboard)/settings/page.tsx
  </files>
  <action>
    1. Create `src/app/(dashboard)/dashboard/page.tsx`:
       - Server Component that fetches user profile from Supabase
       - Display a welcome message: "Welcome back, {full_name || 'there'}!" using a heading with brand typography
       - Below the welcome: a brief description like "Your survey data quality control dashboard" in muted text
       - Use a Card component to contain the welcome content
       - This is intentionally minimal -- no project-related UI (per user decision: "Phase 1 dashboard shows a welcome message only")
       - Apply brand colors: Deep Blue for headings, background card on #F8FAFC base

    2. Create `src/app/(dashboard)/settings/page.tsx`:
       - Server Component that fetches user profile
       - Page heading: "Settings"
       - **Profile section** (Card):
         - Display: Full Name (editable input), Email (read-only display)
         - "Save changes" button that calls a server action to update the profile in Supabase (update `profiles` table for full_name)
         - Create an `updateProfile` server action in `src/lib/actions/auth.ts` (or a new `src/lib/actions/profile.ts`) that updates the profiles table
       - **Password section** (Card):
         - Fields: New Password, Confirm New Password
         - "Update password" button that calls the `updatePassword` server action from `src/lib/actions/auth.ts`
         - Inline error if passwords don't match
       - Both sections use shadcn/ui Card, Input, Label, Button
       - Sections separated by spacing, each in its own Card
       - Show success/error feedback inline or via sonner toast for server errors (Claude's discretion -- toast for success, inline for validation errors)
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -10</automated>
  </verify>
  <done>Dashboard shows welcome message with user's name. Settings page shows profile section (name editable, email read-only) and password change section. Both pages render within the app shell layout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 1 application</name>
  <files>none</files>
  <action>
    Human verifies the complete Phase 1 application: auth pages (login, signup, forgot password, update password) with split-screen layout, and app shell (collapsible sidebar, top bar with breadcrumbs and user dropdown, dashboard welcome page, settings page). All styled with brand palette (Deep Blue, Teal, Orange).

    NOTE: To test this you need:
    1. A Supabase project with the profiles migration applied (run the SQL from supabase/migrations/00001_profiles.sql in the SQL editor)
    2. .env.local file with your Supabase URL and anon key
    3. Redirect URL configured in Supabase Auth settings (http://localhost:3000/auth/callback)

    Verification steps:
    1. Run `npm run dev` and visit http://localhost:3000 -- should redirect to /login
    2. On /login: verify split-screen layout (form left, brand visual right on desktop)
    3. Click "Don't have an account? Sign up" -- navigate to /signup
    4. Create an account with email and password -- should redirect to login with "check email" message
    5. Verify email (check Supabase Dashboard > Auth > Users if rate-limited, manually confirm user)
    6. Log in with the account -- should redirect to /dashboard
    7. Verify dashboard shows welcome message with your name
    8. Verify sidebar shows "Dashboard" and "Settings" items with icons
    9. Click the sidebar collapse toggle -- verify it collapses to icon-only mode
    10. Resize browser to tablet width -- verify sidebar auto-collapses
    11. Click user avatar dropdown in top bar -- verify "Log out" option exists
    12. Navigate to Settings -- verify profile info and password change sections
    13. Click "Log out" -- verify redirect to /login
    14. Verify brand colors are applied: Deep Blue primary, Teal for success elements, Orange for accents
  </action>
  <verify>User types "approved" or describes issues to fix</verify>
  <done>User confirms all auth flows work, app shell looks professional with brand colors, sidebar collapses properly, and responsive layout works on tablet.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- All dashboard routes render within the app shell
- Sidebar collapses between full and icon-only modes
- Top bar shows breadcrumbs and user dropdown
- Logout from dropdown redirects to /login
- Dashboard shows welcome message
- Settings page has profile and password sections
- Brand colors visible throughout the shell
</verification>

<success_criteria>
- Authenticated users see the full app shell with sidebar and top bar
- Sidebar has exactly 2 items: Dashboard and Settings
- Sidebar toggles between full labels and icon-only
- Sidebar auto-collapses on tablet breakpoints
- Top bar has breadcrumbs (left), notification bell placeholder (right), user dropdown (right)
- Logout works from user dropdown on any page
- Dashboard shows welcome message only
- Settings allows profile name edit and password change
- Brand palette (Deep Blue, Teal, Orange) applied consistently
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>
