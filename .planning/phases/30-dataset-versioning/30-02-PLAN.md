---
phase: 30-dataset-versioning
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - src/lib/types/versions.ts
  - src/lib/actions/versions.ts
  - src/app/api/versions/route.ts
  - src/app/api/version-diff/route.ts
  - src/components/files/version-timeline.tsx
  - src/components/files/version-diff-view.tsx
  - src/components/files/file-detail-view.tsx
  - src/components/realtime-provider.tsx
autonomous: false
requirements: [DVER-02, DVER-03, DVER-04]

must_haves:
  truths:
    - "User sees a Versions tab on the dataset detail page between Preview and Audit"
    - "Versions tab shows a vertical timeline of all versions, newest first, with metadata per card"
    - "Trend summary at top shows total versions, issue trend, and row count change"
    - "User can select exactly 2 versions via checkboxes and click Compare to see a diff"
    - "Diff view shows summary card first, then expandable sections for added/removed/modified rows"
    - "Modified rows display inline before/after format for changed columns only"
    - "New versions appear in real-time without page refresh"
  artifacts:
    - path: "src/lib/types/versions.ts"
      provides: "DatasetVersion, VersionDiff, DiffSummary TypeScript types"
      exports: ["DatasetVersion", "VersionDiff", "DiffSummary", "DiffRow", "ModifiedRow"]
    - path: "src/components/files/version-timeline.tsx"
      provides: "Versions tab content with timeline and trend header"
      min_lines: 100
    - path: "src/components/files/version-diff-view.tsx"
      provides: "Comparison results with summary card and expandable row sections"
      min_lines: 80
  key_links:
    - from: "src/components/files/file-detail-view.tsx"
      to: "src/components/files/version-timeline.tsx"
      via: "TabsContent with VersionTimeline component"
      pattern: "TabsContent.*versions"
    - from: "src/components/files/version-timeline.tsx"
      to: "src/app/api/versions/route.ts"
      via: "fetch call to load version list"
      pattern: "fetch.*api/versions"
    - from: "src/components/files/version-diff-view.tsx"
      to: "src/app/api/version-diff/route.ts"
      via: "fetch call to compute diff"
      pattern: "fetch.*api/version-diff"
    - from: "src/components/realtime-provider.tsx"
      to: "dataset_versions table"
      via: "Supabase Realtime subscription"
      pattern: "dataset_versions"
---

<objective>
Frontend UI for dataset versioning: TypeScript types, API proxy routes, Versions tab with vertical timeline and trend summary, checkbox-based version comparison selection, and paginated diff view with summary-first drill-down.

Purpose: Users need to see how their data changed over time, compare any two versions, and see linked issue counts. The UI must update in real-time when new versions are created after validation.
Output: Versions tab in dataset detail page, version comparison UI, Realtime integration
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-dataset-versioning/30-CONTEXT.md
@.planning/phases/30-dataset-versioning/30-RESEARCH.md
@.planning/phases/30-dataset-versioning/30-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 backend that the frontend consumes -->

From backend/app/routers/versions.py (Plan 01 output):
```
GET /api/v1/datasets/{dataset_id}/versions
  Response: { "versions": DatasetVersion[] }

POST /api/v1/datasets/{dataset_id}/diff
  Request: { "version_a": number, "version_b": number, "page": number, "page_size": number }
  Response: {
    "summary": { rows_before, rows_after, added_count, removed_count, modified_count, columns_before, columns_after },
    "added": DiffRow[],
    "removed": DiffRow[],
    "modified": ModifiedRow[],
    "pagination": { page, page_size, total_added, total_removed, total_modified, has_more }
  }
```

DatasetVersion record shape (from dataset_versions table):
```
{
  id: string (UUID),
  dataset_id: string,
  version_number: number,
  storage_path: string,
  row_count: number,
  column_count: number,
  file_size: number,
  validation_run_id: string | null,
  issue_count: number,
  severity_breakdown: { critical: number, warning: number, info: number },
  created_at: string (ISO timestamp)
}
```

From src/components/files/file-detail-view.tsx (tab system):
```typescript
// Existing tabs: mapping, results, preview, audit
// Add "versions" tab between "preview" and "audit"
<TabsTrigger value="versions">Versions</TabsTrigger>
<TabsContent value="versions">
  <VersionTimeline datasetId={dataset.id} />
</TabsContent>
```

From src/components/realtime-provider.tsx (multi-table subscription pattern):
```typescript
// Already subscribes to "datasets" and "job_runs" tables
// Extend to also subscribe to "dataset_versions" table
// Pattern: channel.on('postgres_changes', { event: '*', schema: 'public', table: 'dataset_versions' }, callback)
```

From src/app/api/compare/route.ts (proxy pattern):
```typescript
// Auth check -> forward to FASTAPI_URL -> return response
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
const response = await fetch(`${process.env.FASTAPI_URL}/api/v1/...`)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types, API routes, Versions tab with timeline, and Realtime</name>
  <files>
    src/lib/types/versions.ts,
    src/lib/actions/versions.ts,
    src/app/api/versions/route.ts,
    src/app/api/version-diff/route.ts,
    src/components/files/version-timeline.tsx,
    src/components/files/file-detail-view.tsx,
    src/components/realtime-provider.tsx
  </files>
  <action>
    1. Create `src/lib/types/versions.ts` with TypeScript types:
       - `DatasetVersion`: id, dataset_id, version_number, storage_path, row_count, column_count, file_size, validation_run_id (string|null), issue_count, severity_breakdown ({critical, warning, info}), created_at
       - `DiffSummary`: rows_before, rows_after, added_count, removed_count, modified_count, columns_before, columns_after
       - `DiffRow`: { row: number, values: Record<string, string> }
       - `ModifiedRow`: { row: number, changes: Record<string, { old: string, new: string }> }
       - `VersionDiff`: { summary: DiffSummary, added: DiffRow[], removed: DiffRow[], modified: ModifiedRow[], pagination: { page, page_size, total_added, total_removed, total_modified, has_more } }

    2. Create `src/lib/actions/versions.ts` with fetch helpers:
       - `fetchVersions(datasetId: string): Promise<DatasetVersion[]>` -- calls /api/versions?datasetId=X
       - `fetchVersionDiff(datasetId: string, versionA: number, versionB: number, page?: number): Promise<VersionDiff>` -- calls /api/version-diff

    3. Create `src/app/api/versions/route.ts` (GET):
       - Auth check via createClient + getUser
       - Extract datasetId from searchParams
       - Forward to `${FASTAPI_URL}/api/v1/datasets/${datasetId}/versions`
       - Return JSON response
       - Set `maxDuration = 30`

    4. Create `src/app/api/version-diff/route.ts` (POST):
       - Auth check via createClient + getUser
       - Extract body: { datasetId, versionA, versionB, page, pageSize }
       - Forward to `${FASTAPI_URL}/api/v1/datasets/${body.datasetId}/diff`
       - Return JSON response
       - Set `maxDuration = 60` (diff computation can be slow for large files)

    5. Create `src/components/files/version-timeline.tsx`:
       - Props: `{ datasetId: string }`
       - Fetch versions on mount using fetchVersions
       - **Trend summary header** at top: total version count, issue trend (compare first and latest version issue counts, show "X -> Y, down/up Z%"), row count change between first and latest
       - **Vertical timeline** of version cards, newest first. Each card shows:
         - Version label (v1, v2, etc.) with a colored dot on the timeline line
         - Timestamp formatted as relative time (e.g., "2 hours ago") with full date on hover
         - Row count, column count, file size (human-readable KB/MB)
         - Issue count with severity breakdown badges (critical=red, warning=amber, info=blue)
         - Validation profile name if available (query validation_runs if needed, or omit if not in the version record -- Claude's discretion to keep simple)
       - **Checkbox selection** on each card for comparison. Track selected versions in state (max 2).
         - When exactly 2 selected, show a sticky footer bar with "Compare v{A} <-> v{B}" button
         - Button disabled until exactly 2 selected
         - Clicking Compare navigates to diff view (set a `comparing` state with the two version numbers)
       - **Empty state**: "No versions yet. Versions are created automatically when you run validation."
       - **Loading state**: Skeleton cards (3 placeholder cards)
       - Use Lucide icons: GitBranch for timeline, AlertTriangle for issues, FileText for file info

    6. Modify `src/components/files/file-detail-view.tsx`:
       - Import VersionTimeline from `./version-timeline`
       - Add `<TabsTrigger value="versions">Versions</TabsTrigger>` between the "preview" and "audit" TabsTrigger elements (between lines 534 and 535)
       - Add corresponding `<TabsContent value="versions"><VersionTimeline datasetId={dataset.id} /></TabsContent>` between the preview and audit TabsContent blocks (between lines 702 and 704)

    7. Extend `src/components/realtime-provider.tsx`:
       - Add a subscription to the `dataset_versions` table following the existing pattern for `job_runs`
       - On INSERT events for dataset_versions, trigger a refresh (use the existing invalidation pattern -- either call onDatasetUpdate callback or dispatch a custom event)
       - This ensures new versions appear in the timeline immediately after validation completes
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Versions tab appears in dataset detail page between Preview and Audit. Version timeline loads and displays version cards with metadata. Checkbox selection enables comparison. Realtime subscription set up for dataset_versions table. Build succeeds with no type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Version diff comparison view with summary and drill-down</name>
  <files>
    src/components/files/version-diff-view.tsx,
    src/components/files/version-timeline.tsx
  </files>
  <action>
    1. Create `src/components/files/version-diff-view.tsx`:
       - Props: `{ datasetId: string, versionA: number, versionB: number, onBack: () => void }`
       - Fetch diff on mount using fetchVersionDiff
       - **Back button** at top: "Back to version history" with ArrowLeft icon, calls onBack
       - **Summary card** (always visible, renders immediately):
         - Title: "Comparing v{A} and v{B}"
         - Stats grid (2x3 or responsive): Rows before/after, Added rows count (green), Removed rows count (red), Modified rows count (amber), Columns before/after
         - If issues data available from versions, show issues before/after
       - **Expandable sections** using shadcn Collapsible or Accordion:
         - "Added Rows ({count})" -- green accent. Each row shows row number and all column values
         - "Removed Rows ({count})" -- red accent. Each row shows row number and all column values
         - "Modified Rows ({count})" -- amber accent. Each row shows row number and inline changes: `column: old_value -> new_value` (only changed columns per row, using the ModifiedRow.changes format)
       - **Pagination**: Each section shows first page (50 rows). If has_more, show "Load more" button that fetches next page and appends results
       - **Loading state**: Skeleton for summary card + "Computing differences..." message
       - **Empty diff state**: "No differences found between v{A} and v{B}" with a check icon
       - **Error state**: Toast via sonner on fetch failure, display retry button
       - Style: Use Tailwind classes consistent with the existing dashboard card patterns. Rounded-2xl cards, subtle borders, proper spacing

    2. Update `src/components/files/version-timeline.tsx` to integrate the diff view:
       - Add state: `comparing: { versionA: number, versionB: number } | null`
       - When user clicks "Compare v{A} <-> v{B}" in the sticky footer, set comparing state
       - When comparing is set, render `<VersionDiffView>` instead of the timeline
       - Pass onBack handler that sets comparing back to null
       - Clear checkbox selections when returning from diff view

    3. Ensure all components follow the project design system:
       - Space Grotesk for any heading text, DM Sans for body (inherited from global styles)
       - Colored icon accents: use teal for version timeline, amber for modifications
       - Use cursor-pointer on all clickable elements
       - Accessible: proper aria-labels on checkboxes, focus states on interactive elements
       - Responsive: stack vertically on mobile, timeline line hidden on small screens if needed
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Diff view renders with summary card and expandable sections for added/removed/modified rows. Modified rows show inline before/after format. Pagination works with load-more. Back button returns to timeline. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete dataset versioning feature</name>
  <action>Human verification of the full versioning flow -- no automated work in this task.</action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms versioning feature works end-to-end: version history displays after validation, comparison shows correct diff, UI is responsive and accessible.</done>
  <what-built>Complete dataset versioning feature: backend snapshot creation on validation, version history timeline UI with trend summary, checkbox-based version comparison, and paginated diff view with summary-first drill-down</what-built>
  <how-to-verify>
    1. Apply the migration: run `supabase/migrations/20260411_dataset_versions.sql` against your database
    2. Start the FastAPI backend and Next.js frontend
    3. Navigate to any dataset detail page
    4. Verify a "Versions" tab appears between "Preview" and "Audit"
    5. Click "Versions" -- should show empty state if no validations have been run
    6. Run a validation on the dataset
    7. After validation completes, check the Versions tab -- a v1 entry should appear (may need Realtime or page refresh)
    8. Run validation again -- v2 should appear at the top of the timeline
    9. Verify each version card shows: version number, timestamp, row count, column count, file size, issue count with severity badges
    10. Verify the trend summary header shows total versions and issue trend
    11. Check the two version checkboxes -- a sticky footer should appear with "Compare v1 <-> v2"
    12. Click Compare -- verify summary card shows rows before/after, added/removed/modified counts
    13. Expand the Modified Rows section -- verify inline `column: old -> new` format
    14. Click "Back to version history" -- verify return to timeline with selections cleared
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx next build` succeeds with no errors
- `cd backend && python -m pytest tests/ -x` passes
- Versions tab visible in dataset detail page
- Version cards display correct metadata
- Comparison flow works end-to-end
- Realtime updates work for new versions
</verification>

<success_criteria>
- Versions tab shows between Preview and Audit in dataset detail page
- Vertical timeline displays version cards with all required metadata (version number, timestamp, row/column counts, file size, issue count with severity breakdown)
- Trend summary header shows version count, issue trend, and row count change
- Checkbox selection enables exactly-2 comparison with sticky footer bar
- Diff view shows summary card first, then expandable added/removed/modified sections
- Modified rows use inline before/after format showing only changed columns
- Pagination loads additional rows on demand
- Realtime subscription updates version list when new versions are created
</success_criteria>

<output>
After completion, create `.planning/phases/30-dataset-versioning/30-02-SUMMARY.md`
</output>
