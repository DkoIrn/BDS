---
phase: 30-dataset-versioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260411_dataset_versions.sql
  - backend/app/services/versioning.py
  - backend/app/routers/versions.py
  - backend/app/queue/tasks.py
  - backend/app/main.py
  - backend/tests/test_versioning.py
  - backend/tests/fixtures/version_test_data/v1_sample.csv
  - backend/tests/fixtures/version_test_data/v2_sample.csv
autonomous: true
requirements: [DVER-01, DVER-03, DVER-04, DVER-05]

must_haves:
  truths:
    - "After validation completes, a new version row exists in dataset_versions with correct metadata"
    - "When a dataset reaches 11 versions, the oldest version row is deleted before the new one is inserted"
    - "Comparing two version files returns a structured diff with summary stats and row-level changes"
    - "Each version record stores issue_count and severity_breakdown from its validation run"
  artifacts:
    - path: "supabase/migrations/20260411_dataset_versions.sql"
      provides: "dataset_versions table with RLS, indexes, Realtime publication"
      contains: "CREATE TABLE public.dataset_versions"
    - path: "backend/app/services/versioning.py"
      provides: "Snapshot creation, pruning, and diff computation logic"
      exports: ["create_version_snapshot", "compute_version_diff"]
    - path: "backend/app/routers/versions.py"
      provides: "GET versions list + POST diff endpoint"
      exports: ["router"]
    - path: "backend/tests/test_versioning.py"
      provides: "Unit tests for DVER-01 through DVER-05"
      min_lines: 80
  key_links:
    - from: "backend/app/queue/tasks.py"
      to: "backend/app/services/versioning.py"
      via: "create_version_snapshot call after step 8"
      pattern: "create_version_snapshot"
    - from: "backend/app/routers/versions.py"
      to: "backend/app/services/versioning.py"
      via: "compute_version_diff call in POST /diff endpoint"
      pattern: "compute_version_diff"
    - from: "backend/app/main.py"
      to: "backend/app/routers/versions.py"
      via: "app.include_router"
      pattern: "include_router.*versions"
---

<objective>
Backend infrastructure for dataset versioning: database migration, snapshot creation service with auto-pruning, diff computation endpoint, and integration into the validation task completion flow.

Purpose: Every validation run must create a traceable snapshot so users can see how their data changed over time. The backend provides the data layer and computation for the frontend to consume.
Output: dataset_versions table, versioning service, versions/diff API endpoints, unit tests
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-dataset-versioning/30-CONTEXT.md
@.planning/phases/30-dataset-versioning/30-RESEARCH.md
@.planning/phases/29-job-queue-infrastructure/29-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/app/queue/tasks.py (insertion point -- between step 8 and step 9, around line 247):
```python
# Step 8 updates dataset status to "validated" (line 240-245)
# Step 9 records successful job run (line 247-259)
# Step 10 dispatches webhooks (line 263-273)
# INSERT snapshot creation between step 8 and step 9

# Available variables at insertion point:
# supabase - SupabaseClient instance
# dataset_id - str
# dataset - dict (full dataset record including user_id, storage_path, file_name)
# run_id - str (validation_run_id)
# total_issues - int
# critical_count - int
# warning_count - int (derived from issues)
# info_count - int (derived from issues)
# df - pandas DataFrame (parsed dataset)
# file_bytes - bytes (raw file from storage)
# pass_rate - float
```

From backend/app/dependencies.py (SupabaseClient pattern):
```python
# Uses supabase.table("name").select/insert/update/delete().eq().execute()
# Uses supabase.storage.from_("datasets").download(path) / .upload(path, bytes)
```

From backend/app/main.py (router registration):
```python
app.include_router(jobs_router, tags=["jobs"])
# Add: app.include_router(versions_router, tags=["versions"])
```

From supabase/migrations/20260411_job_tracking.sql (migration pattern):
```sql
-- Table with UUID PK, RLS, Realtime publication
-- ALTER PUBLICATION supabase_realtime ADD TABLE table_name;
-- RLS policies for user access via org membership chain
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Database migration and versioning service with tests</name>
  <files>
    supabase/migrations/20260411_dataset_versions.sql,
    backend/app/services/versioning.py,
    backend/tests/test_versioning.py,
    backend/tests/fixtures/version_test_data/v1_sample.csv,
    backend/tests/fixtures/version_test_data/v2_sample.csv
  </files>
  <behavior>
    - test_create_snapshot: create_version_snapshot stores a version record with correct dataset_id, version_number=1, row_count, column_count, file_size, issue_count, severity_breakdown, and copies file to versioned storage path
    - test_create_snapshot_increments_version: second call creates version_number=2 (max existing + 1)
    - test_version_issue_count: severity_breakdown JSON contains critical/warning/info counts
    - test_auto_prune_at_limit: when 10 versions exist, creating an 11th deletes the oldest version row and schedules storage file cleanup
    - test_version_number_after_prune: after pruning version 1, next version is max(existing)+1 not len(versions)+1
    - test_compute_diff_added_rows: df_new has more rows than df_old, added_rows populated correctly
    - test_compute_diff_removed_rows: df_new has fewer rows than df_old, removed_rows populated correctly
    - test_compute_diff_modified_rows: same row count but different values, modified_rows shows column: old/new
    - test_compute_diff_summary: summary contains rows_before, rows_after, added_count, removed_count, modified_count
    - test_compute_diff_column_changes: handles columns added/removed between versions
  </behavior>
  <action>
    1. Create the SQL migration `supabase/migrations/20260411_dataset_versions.sql` exactly as specified in 30-RESEARCH.md (the full CREATE TABLE, index, Realtime publication, and RLS policies). Use the org membership chain pattern from 20260411_job_tracking.sql for the SELECT RLS policy. Add service_role full access policy.

    2. Create test fixtures:
       - `backend/tests/fixtures/version_test_data/v1_sample.csv`: 5 rows with columns KP, Depth, Status (sample pipeline data)
       - `backend/tests/fixtures/version_test_data/v2_sample.csv`: 6 rows (1 added), 1 modified Depth value, same columns

    3. Write tests FIRST in `backend/tests/test_versioning.py` covering the behaviors above. Mock supabase client (table operations and storage operations) following the existing test patterns in test_job_queue.py. Use the fixture CSV files for diff tests.

    4. Create `backend/app/services/versioning.py` with two functions:
       - `create_version_snapshot(supabase, dataset_id, user_id, validation_run_id, issue_count, critical_count, warning_count, info_count, row_count, column_count, file_size, storage_path)`: Implements the prune-then-insert pattern from 30-RESEARCH.md Pattern 2. MAX_VERSIONS = 10 constant. Version numbers use `v` prefix format (v1, v2...). Storage path: `{user_id}/{dataset_id}/versions/v{N}_{timestamp}.csv`. Downloads raw file from storage_path, uploads copy to versioned path. Deletes oldest version row(s) if at limit. Storage file cleanup for pruned versions happens after DB operations with logged warnings on failure.
       - `compute_version_diff(df_old, df_new)`: Position-based row comparison using the pattern from 30-RESEARCH.md Pattern 3. Returns dict with summary (rows_before, rows_after, added_count, removed_count, modified_count, columns_before, columns_after) and row-level detail arrays (added, removed, modified). For columns added between versions: show as `(new) value`. For columns removed: show as `value (removed)`.

    5. Run tests: `cd backend && python -m pytest tests/test_versioning.py -x`
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_versioning.py -x</automated>
  </verify>
  <done>All 10 versioning tests pass. Migration SQL exists. create_version_snapshot handles creation + pruning. compute_version_diff handles added/removed/modified rows and column changes.</done>
</task>

<task type="auto">
  <name>Task 2: Versions API router, task integration, and app registration</name>
  <files>
    backend/app/routers/versions.py,
    backend/app/queue/tasks.py,
    backend/app/main.py
  </files>
  <action>
    1. Create `backend/app/routers/versions.py` following the pattern from routers/compare.py:
       - `GET /api/v1/datasets/{dataset_id}/versions`: Query dataset_versions table ordered by version_number DESC. Return `{"versions": [...]}`.
       - `POST /api/v1/datasets/{dataset_id}/diff`: Accept JSON body `{"version_a": int, "version_b": int, "page": int (default 1), "page_size": int (default 50)}`. Look up both version records, download both files from storage, parse with pandas (csv or excel based on filename), call compute_version_diff, paginate the row-level detail arrays (added/removed/modified), return full summary + paginated details with total counts and has_more flag. Set a 60-second timeout. Handle errors: version not found (404), file download failure (502), diff computation error (500).

    2. Integrate snapshot creation into `backend/app/queue/tasks.py`:
       - Add `from app.services.versioning import create_version_snapshot` as a lazy import inside the validate_dataset function body (after the existing lazy imports block, around line 119).
       - Insert the snapshot creation call between step 8 (update dataset status, line 245) and step 9 (record job run, line 247). Wrap in try/except so snapshot failure does not break the validation flow -- log warning on failure.
       - Pass all required parameters: supabase, dataset_id, dataset["user_id"], run_id, total_issues, critical_count, warning_count (derive from issues list: `sum(1 for i in issues if i.severity.value == "warning")`), info_count (same pattern), len(df), len(df.columns), len(file_bytes), dataset["storage_path"].
       - Compute warning_count and info_count from the issues list before the snapshot call (they are already partially computed for the result_summary, extend if needed).

    3. Register the versions router in `backend/app/main.py`:
       - Import: `from app.routers.versions import router as versions_router`
       - Add: `app.include_router(versions_router, tags=["versions"])`

    4. Run full test suite to verify no regressions: `cd backend && python -m pytest tests/ -x`
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/ -x</automated>
  </verify>
  <done>Versions router registered with list and diff endpoints. Snapshot creation integrated into validate_dataset task between steps 8 and 9. Full backend test suite passes.</done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/test_versioning.py -x` -- all versioning tests pass
- `cd backend && python -m pytest tests/ -x` -- full suite passes (no regressions)
- Migration SQL file exists at `supabase/migrations/20260411_dataset_versions.sql`
- `grep -r "create_version_snapshot" backend/app/queue/tasks.py` -- snapshot integrated into task
- `grep -r "include_router.*versions" backend/app/main.py` -- router registered
</verification>

<success_criteria>
- dataset_versions table migration exists with correct schema, RLS, indexes, and Realtime publication
- create_version_snapshot creates versioned file copies and metadata rows with auto-pruning at 10
- compute_version_diff computes position-based row diffs with summary stats
- Versions API returns list and paginated diff results
- Snapshot creation integrated into validation task completion flow (non-blocking)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/30-dataset-versioning/30-01-SUMMARY.md`
</output>
