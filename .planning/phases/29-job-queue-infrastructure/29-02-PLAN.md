---
phase: 29-job-queue-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - src/app/api/validate/route.ts
  - src/app/api/v1/validate/route.ts
  - src/app/api/jobs/route.ts
  - src/app/api/jobs/[jobId]/route.ts
  - src/app/api/jobs/[jobId]/retry/route.ts
  - src/app/api/jobs/[jobId]/cancel/route.ts
  - src/lib/types/jobs.ts
  - src/components/jobs/job-progress-bar.tsx
  - src/components/jobs/job-error-display.tsx
  - src/components/jobs/job-history-table.tsx
  - src/components/realtime-provider.tsx
  - src/app/(dashboard)/dashboard/page.tsx
autonomous: false
requirements:
  - JOBQ-03
  - JOBQ-06

must_haves:
  truths:
    - "User sees a progress indicator with stage name during validation (not just a spinner)"
    - "User can view a history of recent jobs with status, duration, and outcome on the dashboard"
    - "Dashboard Recent Jobs section auto-refreshes via Supabase Realtime"
    - "Per-dataset job history is visible on the dataset detail page"
  artifacts:
    - path: "src/lib/types/jobs.ts"
      provides: "TypeScript types for job_runs table and progress"
      exports: ["JobRun", "JobStatus", "ValidationProgress"]
    - path: "src/components/jobs/job-progress-bar.tsx"
      provides: "Stage-based progress indicator component"
      exports: ["JobProgressBar"]
    - path: "src/components/jobs/job-error-display.tsx"
      provides: "Error state with retry button and expandable details"
      exports: ["JobErrorDisplay"]
    - path: "src/components/jobs/job-history-table.tsx"
      provides: "Recent jobs list with rich detail per entry"
      exports: ["JobHistoryTable"]
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Dashboard with Recent Jobs section"
  key_links:
    - from: "src/app/api/validate/route.ts"
      to: "FastAPI /api/v1/validate"
      via: "fetch POST, now receives job_id in response"
      pattern: "job_id"
    - from: "src/components/jobs/job-progress-bar.tsx"
      to: "datasets table"
      via: "Supabase Realtime subscription on validation_progress column"
      pattern: "validation_progress"
    - from: "src/components/jobs/job-history-table.tsx"
      to: "job_runs table"
      via: "Supabase Realtime subscription for live updates"
      pattern: "job_runs"
    - from: "src/components/jobs/job-error-display.tsx"
      to: "src/app/api/jobs/[jobId]/retry/route.ts"
      via: "POST /api/jobs/{jobId}/retry on Retry button click"
      pattern: "retry"
---

<objective>
Build the frontend job queue UI: progress indicator, error/retry display, job history table, and integrate with the Next.js API routes. Update the validate route to receive job_id from FastAPI and wire Supabase Realtime for live progress updates.

Purpose: Give users real-time visibility into validation processing status, clear error messages with retry capability, and a queryable history of all jobs.
Output: Working frontend with progress bar, error display, job history, and Realtime integration.
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-job-queue-infrastructure/29-CONTEXT.md
@.planning/phases/29-job-queue-infrastructure/29-RESEARCH.md
@.planning/phases/29-job-queue-infrastructure/29-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From backend/app/routers/jobs.py (created in Plan 01):
```
GET  /api/v1/jobs              -- list recent job_runs (query: dataset_id, status, limit)
GET  /api/v1/jobs/{job_id}     -- single job detail
POST /api/v1/jobs/{job_id}/retry  -- manual retry
POST /api/v1/jobs/{job_id}/cancel -- cancel running job
```

From supabase/migrations/20260411_job_tracking.sql (created in Plan 01):
```sql
-- datasets table gains:
validation_progress JSONB  -- {"stage": "validating", "detail": "3/7 checks"}

-- New table:
job_runs (
  id UUID PK,
  dataset_id UUID FK,
  job_type TEXT,
  procrastinate_job_id BIGINT,
  status TEXT CHECK IN ('queued','running','succeeded','failed','cancelled'),
  started_at TIMESTAMPTZ,
  completed_at TIMESTAMPTZ,
  duration_ms INTEGER,
  attempt_number INTEGER,
  max_attempts INTEGER,
  error_summary TEXT,
  error_detail TEXT,
  config_snapshot JSONB,
  result_summary JSONB,
  created_at TIMESTAMPTZ
)
-- Realtime enabled on job_runs table
```

From src/components/realtime-provider.tsx (existing):
```typescript
// Subscribes to datasets table UPDATE events
// Shows toast on status changes (validated, validation_error)
// Need to EXTEND for validation_progress updates
```

From src/app/(dashboard)/dashboard/page.tsx (existing):
```typescript
// Server component with dashboard layout
// Need to ADD Recent Jobs section
```

From existing design patterns:
- Rounded-2xl cards, Tailwind styling
- Status icons with colored accents (emerald=success, red=error, amber=warning, blue=running)
- sonner toast notifications
- DM Sans body font, Space Grotesk headings
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Job types, Next.js API routes, and UI components</name>
  <files>
    src/lib/types/jobs.ts,
    src/app/api/validate/route.ts,
    src/app/api/v1/validate/route.ts,
    src/app/api/jobs/route.ts,
    src/app/api/jobs/[jobId]/route.ts,
    src/app/api/jobs/[jobId]/retry/route.ts,
    src/app/api/jobs/[jobId]/cancel/route.ts,
    src/components/jobs/job-progress-bar.tsx,
    src/components/jobs/job-error-display.tsx,
    src/components/jobs/job-history-table.tsx
  </files>
  <action>
    1. Create `src/lib/types/jobs.ts`:
       - `JobStatus = "queued" | "running" | "succeeded" | "failed" | "cancelled"`
       - `ValidationProgress = { stage: string; detail: string }` -- stages: "starting", "downloading", "parsing", "validating", "storing_results", "complete", "error"
       - `JobRun` interface matching job_runs table columns (camelCase for frontend)
       - Export all types

    2. Update `src/app/api/validate/route.ts`:
       - After fire-and-forget POST to FastAPI, extract `job_id` from FastAPI response body
       - Return job_id in the 202 response to the client: `{ status: "accepted", datasetId, jobId }`
       - Do NOT set dataset status to "validating" anymore (the queue task handles it) -- remove the `supabase.from('datasets').update({ status: 'validating' })` call
       - Instead, set status to "queued" to indicate job is in queue

    3. Update `src/app/api/v1/validate/route.ts` (Enterprise API):
       - Same changes as above: extract job_id from FastAPI response, return it, set status to "queued" not "validating"

    4. Create Next.js API proxy routes for job operations:
       - `src/app/api/jobs/route.ts`: GET -- proxy to FastAPI GET /api/v1/jobs with query params (dataset_id, status, limit)
       - `src/app/api/jobs/[jobId]/route.ts`: GET -- proxy to FastAPI GET /api/v1/jobs/{jobId}
       - `src/app/api/jobs/[jobId]/retry/route.ts`: POST -- proxy to FastAPI POST /api/v1/jobs/{jobId}/retry
       - `src/app/api/jobs/[jobId]/cancel/route.ts`: POST -- proxy to FastAPI POST /api/v1/jobs/{jobId}/cancel
       - All routes: auth check, org role check, proxy to FASTAPI_URL

    5. Create `src/components/jobs/job-progress-bar.tsx`:
       - Props: `{ datasetId: string; initialProgress?: ValidationProgress }`
       - Subscribe to Supabase Realtime on datasets table filtered by dataset_id, watching validation_progress column
       - Render stage-based progress:
         - Show current stage name (e.g., "Validating (3/7 checks)")
         - Animated progress bar that advances through stages (map stages to approximate percentage: starting=5%, downloading=15%, parsing=30%, validating=40-80%, storing_results=90%, complete=100%)
         - Pulsing animation while in progress
       - Use blue accent color for in-progress, emerald for complete
       - Tailwind rounded-2xl card style

    6. Create `src/components/jobs/job-error-display.tsx`:
       - Props: `{ jobRun: JobRun; onRetry: () => void; retrying?: boolean }`
       - Display human-friendly error summary prominently
       - Show attempt count: "Failed after 3 attempts"
       - Expandable section (details/summary or collapsible) for technical error_detail (traceback)
       - Show timestamps for each attempt if available
       - Prominent "Retry" button (uses onRetry callback) with loading state
       - Red accent color, rounded-2xl card

    7. Create `src/components/jobs/job-history-table.tsx`:
       - Props: `{ datasetId?: string; limit?: number }` -- if datasetId provided, filter to that dataset; otherwise show all (dashboard mode)
       - Fetch job_runs from /api/jobs on mount (with optional dataset_id filter)
       - Subscribe to Supabase Realtime on job_runs table for live updates (INSERT and UPDATE events)
       - Each row shows: status icon (colored dot), dataset name, timestamp (relative), duration, issue count from result_summary, validation profile from config_snapshot, retry count, expandable error log
       - Status colors: queued=gray, running=blue, succeeded=emerald, failed=red, cancelled=amber
       - Sort by created_at DESC
       - Clicking a row expands to show full detail including error_summary/error_detail if failed
       - Tailwind table with rounded-2xl container
  </action>
  <verify>
    <automated>cd C:/Users/danie/OneDrive/Desktop/BDS && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - Job types exported from src/lib/types/jobs.ts
    - Validate routes return job_id and set status to "queued"
    - Job API proxy routes created (list, detail, retry, cancel)
    - Progress bar component renders stage-based progress with Realtime subscription
    - Error display component shows human-friendly error with expandable detail and retry button
    - Job history table fetches and live-updates via Realtime
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard integration and Realtime provider extension</name>
  <files>
    src/components/realtime-provider.tsx,
    src/app/(dashboard)/dashboard/page.tsx
  </files>
  <action>
    1. Extend `src/components/realtime-provider.tsx`:
       - Add subscription to job_runs table (INSERT and UPDATE events) alongside existing datasets subscription
       - On job_runs INSERT with status "running": show toast "Validation started for {dataset_name}"
       - On job_runs UPDATE to "succeeded": show toast "Validation complete for {dataset_name}" with issue count from result_summary (keep existing validated toast logic but route through job_runs)
       - On job_runs UPDATE to "failed": show toast.error "Validation failed for {dataset_name}" with error_summary
       - On datasets UPDATE with validation_progress change: NO toast (progress is shown in the progress bar component, not via toasts)
       - Keep existing dataset status change handling for backward compatibility with USE_JOB_QUEUE=false

    2. Update `src/app/(dashboard)/dashboard/page.tsx`:
       - Add a "Recent Jobs" section below the existing dashboard content
       - Use JobHistoryTable component in dashboard mode (no datasetId filter, limit=10)
       - Section header: "Recent Jobs" with a subtle refresh indicator
       - Card container with rounded-2xl, consistent with dashboard bento grid style
       - Show empty state: "No validation jobs yet" when no job_runs exist
  </action>
  <verify>
    <automated>cd C:/Users/danie/OneDrive/Desktop/BDS && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - Realtime provider subscribes to both datasets and job_runs tables
    - Toast notifications fire for job status changes
    - Dashboard shows Recent Jobs section with live-updating job history
    - TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete job queue UI and real-time behavior</name>
  <files>N/A</files>
  <action>
    Human verification of complete job queue frontend including progress bar, error/retry display, job history, and Realtime integration.
  </action>
  <verify>
    <automated>cd C:/Users/danie/OneDrive/Desktop/BDS && npx tsc --noEmit --pretty 2>&1 | head -5</automated>
  </verify>
  <done>User confirms all UI behaviors work correctly in browser</done>
  <what-built>
    Complete job queue frontend: progress bar during validation, error display with retry button, job history on dashboard and dataset detail, Realtime live updates, and toast notifications for job events.
  </what-built>
  <how-to-verify>
    1. Run `supabase db push` to apply migrations (procrastinate schema + job_tracking)
    2. Start the backend: `cd backend && USE_JOB_QUEUE=true uvicorn app.main:app --reload`
    3. Start the frontend: `npm run dev`
    4. Navigate to the dashboard -- verify "Recent Jobs" section appears (empty state)
    5. Upload a dataset and trigger validation:
       - Verify progress bar appears showing stage names (not just a spinner)
       - Verify progress bar updates in real-time as stages complete
    6. After validation completes:
       - Verify the job appears in "Recent Jobs" on dashboard with green status
       - Verify toast notification fires
       - Navigate to dataset detail and verify job appears in per-dataset history
    7. Force a failure (e.g., corrupt file or stop the worker mid-validation):
       - Verify error state appears with human-friendly message
       - Verify expandable technical details section works
       - Verify "Retry" button is visible and functional
    8. Test cancel: start a validation, click Cancel, verify job stops
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- Backend tests: `cd backend && python -m pytest tests/ -x -q`
- Job progress bar shows stage names during validation
- Job history table live-updates on dashboard
- Error display shows human-friendly message with expandable detail
- Retry button re-enqueues the job
- Cancel button stops a running job
</verification>

<success_criteria>
- User sees stage-based progress during validation (JOBQ-03)
- User sees clear error with retry after 3 failures (JOBQ-04)
- User can view job history on dashboard and per-dataset (JOBQ-06)
- Dashboard auto-refreshes via Supabase Realtime
- All UI components follow existing design patterns (rounded-2xl, color accents, DM Sans/Space Grotesk)
</success_criteria>

<output>
After completion, create `.planning/phases/29-job-queue-infrastructure/29-02-SUMMARY.md`
</output>
