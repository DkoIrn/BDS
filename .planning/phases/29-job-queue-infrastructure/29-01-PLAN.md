---
phase: 29-job-queue-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/app/queue/__init__.py
  - backend/app/queue/tasks.py
  - backend/app/queue/worker.py
  - backend/app/main.py
  - backend/app/routers/validation.py
  - backend/app/routers/jobs.py
  - supabase/migrations/20260411_procrastinate_schema.sql
  - supabase/migrations/20260411_job_tracking.sql
  - backend/tests/test_job_queue.py
autonomous: true
requirements:
  - JOBQ-01
  - JOBQ-02
  - JOBQ-04
  - JOBQ-05

must_haves:
  truths:
    - "Validation jobs are stored in PostgreSQL and survive server restarts"
    - "Failed jobs retry automatically 3 times with exponential backoff"
    - "Retrying a job cleans up previous results first (no duplicates)"
    - "After 3 failed retries, the dataset status shows validation_error with error details persisted"
    - "Worker runs as a standalone process consuming from the queue"
  artifacts:
    - path: "backend/app/queue/__init__.py"
      provides: "Procrastinate App instance with PsycopgConnector"
      exports: ["app"]
    - path: "backend/app/queue/tasks.py"
      provides: "validate_dataset task with retry strategy"
      exports: ["validate_dataset"]
    - path: "backend/app/queue/worker.py"
      provides: "Standalone worker entry point for Railway"
    - path: "backend/app/routers/validation.py"
      provides: "Refactored validation endpoint using defer_async"
    - path: "backend/app/routers/jobs.py"
      provides: "Job status and history API endpoints"
      exports: ["router"]
    - path: "supabase/migrations/20260411_procrastinate_schema.sql"
      provides: "Procrastinate internal tables"
    - path: "supabase/migrations/20260411_job_tracking.sql"
      provides: "job_runs table and datasets.validation_progress column"
    - path: "backend/tests/test_job_queue.py"
      provides: "Unit tests for queue infrastructure"
  key_links:
    - from: "backend/app/routers/validation.py"
      to: "backend/app/queue/tasks.py"
      via: "validate_dataset.defer_async()"
      pattern: "defer_async"
    - from: "backend/app/queue/tasks.py"
      to: "backend/app/services/validation.py"
      via: "run_validation_pipeline()"
      pattern: "run_validation_pipeline"
    - from: "backend/app/main.py"
      to: "backend/app/queue/__init__.py"
      via: "lifespan context manager opens procrastinate app"
      pattern: "open_async"
---

<objective>
Set up the procrastinate job queue backend: install procrastinate, create DB migrations, define the validate_dataset task with retry/idempotency, refactor the validation router from BackgroundTasks to defer_async, add a job history API, and create the worker entry point.

Purpose: Replace fire-and-forget processing with persistent, retry-capable job queue so no validation job is ever silently lost.
Output: Working queue backend with tests, migrations, task definitions, and job status API.
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-job-queue-infrastructure/29-RESEARCH.md
@.planning/phases/29-job-queue-infrastructure/29-CONTEXT.md

<interfaces>
<!-- Existing code the executor needs to understand -->

From backend/app/routers/validation.py:
```python
# Current endpoint to REPLACE (line 173-192):
@router.post("/validate", status_code=202)
def validate_dataset(request: ValidateRequest, background_tasks: BackgroundTasks):
    # ... uses background_tasks.add_task(run_validation_background, ...)
    return {"status": "accepted", "dataset_id": request.dataset_id}

# Current background function to REFACTOR into queue task (line 20-171):
def run_validation_background(dataset_id: str, config: ProfileConfig | None) -> None:
    # Downloads file, parses, runs validators, stores results, updates status
```

From backend/app/main.py:
```python
# No lifespan currently -- need to ADD one for procrastinate
app = FastAPI(title="SurveyQC Validation API", ...)
```

From backend/app/dependencies.py:
```python
def get_supabase_client():  # Returns configured Supabase client
```

From backend/app/models/schemas.py:
```python
class ValidateRequest(BaseModel):
    dataset_id: str
    config: ProfileConfig | None = None

class ProfileConfig(BaseModel):
    # Validation profile configuration
```

From backend/app/services/webhooks.py:
```python
# Reference retry pattern: MAX_ATTEMPTS=3, BACKOFF_DELAYS=[5,30]
def dispatch_webhooks(dataset_id, event_type, payload): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Procrastinate queue infrastructure and migrations</name>
  <files>
    backend/requirements.txt,
    backend/app/queue/__init__.py,
    backend/app/queue/tasks.py,
    backend/app/queue/worker.py,
    backend/app/main.py,
    supabase/migrations/20260411_procrastinate_schema.sql,
    supabase/migrations/20260411_job_tracking.sql,
    backend/tests/test_job_queue.py
  </files>
  <behavior>
    - test_procrastinate_app_configured: App instance has correct import_paths and connector type
    - test_validate_task_registered: validate_dataset task is registered with name "validate_dataset", queue "validation", max_attempts=3
    - test_retry_strategy: RetryStrategy uses exponential_wait=10, max_attempts=3
    - test_idempotent_cleanup: cleanup_previous_run deletes existing validation_issues and validation_runs for a dataset_id before re-running
    - test_job_persisted: Deferring a job via InMemoryConnector creates a job record with status "todo"
    - test_progress_update_format: update_progress writes correct JSONB shape {stage, detail} to datasets table
  </behavior>
  <action>
    1. Add `procrastinate[psycopg]>=3.7,<4` to backend/requirements.txt

    2. Create `backend/app/queue/__init__.py`:
       - Create procrastinate.App with PsycopgConnector
       - conninfo from env var `DATABASE_URL` (direct connection or session-mode Supavisor port 5432, NOT transaction pooler port 6543)
       - import_paths=["app.queue.tasks"]
       - Add USE_JOB_QUEUE env var check (feature flag for transition, per Pitfall 3 in RESEARCH.md)

    3. Create `backend/app/queue/tasks.py`:
       - Define `validate_dataset` task with:
         - queue="validation"
         - retry=RetryStrategy(max_attempts=3, exponential_wait=10) -- delays ~10s, ~100s, ~1000s
         - retry_exceptions={Exception}
       - Task body wraps existing run_validation_background logic:
         a. Call cleanup_previous_run() first (delete validation_issues then validation_runs for dataset_id) -- JOBQ-05 idempotency
         b. Update validation_progress to {"stage": "starting", "detail": "Preparing validation"}
         c. Run the existing validation pipeline (download, parse, validate, store results)
         d. Between each major step, check context.should_abort() for cancellation support
         e. Update validation_progress at each stage: "downloading", "parsing", "validating (N/M checks)", "storing_results", "complete"
         f. On success: update dataset status to "validated", write job_runs record with status "succeeded"
         g. On failure: update validation_progress with error stage, write job_runs record with error_summary (human-friendly) and error_detail (traceback), re-raise for procrastinate retry
         h. After max retries exhausted (procrastinate marks as failed): a separate on-failure hook or the task's except block should set dataset status to "validation_error"
       - Helper function cleanup_previous_run(supabase, dataset_id)
       - Helper function update_progress(supabase, dataset_id, stage, detail)
       - Helper function record_job_run(supabase, dataset_id, job_id, status, ...) to insert/update job_runs table

    4. Create `backend/app/queue/worker.py`:
       - Entry point: app.run_worker(queues=["validation", "reports", "exports"], concurrency=2, install_signal_handlers=True, listen_notify=True)
       - Runnable via `python -m app.queue.worker`

    5. Update `backend/app/main.py`:
       - Add asynccontextmanager lifespan that calls procrastinate_app.open_async()
       - Pass lifespan to FastAPI constructor
       - Keep all existing router includes

    6. Create `supabase/migrations/20260411_procrastinate_schema.sql`:
       - Use the output of `procrastinate schema --sql` (the full schema with tables, enums, functions, triggers)
       - Since we cannot run the CLI, embed the known procrastinate 3.7.x schema SQL from the research docs
       - Comment at top: "Generated via procrastinate schema --sql -- regenerate if upgrading procrastinate version"

    7. Create `supabase/migrations/20260411_job_tracking.sql`:
       - ALTER TABLE datasets ADD COLUMN IF NOT EXISTS validation_progress JSONB DEFAULT NULL
       - CREATE TABLE job_runs with columns: id (UUID PK), dataset_id (FK datasets ON DELETE CASCADE), job_type (TEXT default 'validation'), procrastinate_job_id (BIGINT), status (TEXT CHECK IN queued/running/succeeded/failed/cancelled), started_at, completed_at, duration_ms, attempt_number, max_attempts, error_summary, error_detail, config_snapshot (JSONB), result_summary (JSONB), created_at
       - Indexes: idx_job_runs_dataset (dataset_id, created_at DESC), idx_job_runs_status (status WHERE IN queued/running)
       - Enable Realtime: ALTER PUBLICATION supabase_realtime ADD TABLE job_runs
       - Add RLS policies for job_runs matching the org-scoped pattern used by other tables (users can read job_runs for datasets in their org's projects)

    8. Create `backend/tests/test_job_queue.py`:
       - Use procrastinate's InMemoryConnector for testing (no real DB needed)
       - Test fixtures in conftest or inline: create test app with InMemoryConnector
       - Tests per behavior block above
       - Mock supabase client calls with unittest.mock.patch
  </action>
  <verify>
    <automated>cd backend && pip install "procrastinate[psycopg]>=3.7,<4" && python -m pytest tests/test_job_queue.py -x -q</automated>
  </verify>
  <done>
    - procrastinate installed and in requirements.txt
    - Queue app, task, and worker modules exist and are importable
    - FastAPI main.py has lifespan for procrastinate
    - Two migration files exist with correct SQL
    - All unit tests pass using InMemoryConnector
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor validation router and add jobs API</name>
  <files>
    backend/app/routers/validation.py,
    backend/app/routers/jobs.py,
    backend/app/main.py
  </files>
  <action>
    1. Refactor `backend/app/routers/validation.py`:
       - Replace BackgroundTasks import and parameter with procrastinate defer
       - Keep the existing `run_validation_background()` function BUT rename it to `_legacy_validation_background()` and gate it behind `USE_JOB_QUEUE=false` env var (feature flag for safe transition per Pitfall 3)
       - New flow when USE_JOB_QUEUE=true:
         a. Validate dataset exists (keep existing check)
         b. Insert a job_runs record with status "queued"
         c. Call `validate_dataset.defer_async(dataset_id=..., config_json=...)`
         d. Update job_runs record with procrastinate_job_id from returned job object
         e. Return {"status": "accepted", "dataset_id": ..., "job_id": job.id}
       - When USE_JOB_QUEUE=false: use legacy BackgroundTasks path (unchanged)
       - IMPORTANT: Do NOT set dataset status to "validating" in the router anymore -- the task does it after successful enqueue. This avoids Pitfall 5 (stuck "validating" status).

    2. Create `backend/app/routers/jobs.py`:
       - GET /api/v1/jobs -- list recent job_runs (default last 20, filterable by dataset_id, status)
       - GET /api/v1/jobs/{job_id} -- get single job_run detail
       - POST /api/v1/jobs/{job_id}/retry -- manual retry: enqueue a new validation job for the same dataset, mark old job as superseded
       - POST /api/v1/jobs/{job_id}/cancel -- cancel a running job via procrastinate_app.job_manager.cancel_job_by_id()
       - All endpoints return JSON matching the job_runs table schema

    3. Register jobs router in `backend/app/main.py`:
       - Import and include jobs_router

    Note: The Enterprise API route (`src/app/api/v1/validate/route.ts`) will be updated in Plan 02 alongside the standard validate route.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_job_queue.py -x -q</automated>
  </verify>
  <done>
    - Validation router uses defer_async when USE_JOB_QUEUE=true, legacy BackgroundTasks when false
    - Jobs API provides list, detail, retry, and cancel endpoints
    - Jobs router registered in main.py
    - Existing tests still pass
  </done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/ -x -q` -- full backend test suite passes
- `python -c "from app.queue import app; from app.queue.tasks import validate_dataset; print('imports ok')"` -- queue modules importable
- Migration SQL files are valid (no syntax errors)
- Feature flag USE_JOB_QUEUE controls which code path is active
</verification>

<success_criteria>
- Procrastinate app instance configured with PsycopgConnector
- validate_dataset task registered with retry strategy (3 attempts, exponential backoff)
- Worker entry point runnable as standalone process
- Validation router refactored with feature flag for safe transition
- Job status API with list/detail/retry/cancel endpoints
- Migrations for procrastinate schema and job_runs table
- Idempotent cleanup before every validation attempt
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/29-job-queue-infrastructure/29-01-SUMMARY.md`
</output>
