---
phase: 11-file-format-parsers
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/app/parsers/__init__.py
  - backend/app/parsers/base.py
  - backend/app/parsers/geojson_parser.py
  - backend/app/parsers/shapefile_parser.py
  - backend/app/parsers/kml_parser.py
  - backend/tests/parsers/__init__.py
  - backend/tests/parsers/test_geojson_parser.py
  - backend/tests/parsers/test_shapefile_parser.py
  - backend/tests/parsers/test_kml_parser.py
  - backend/tests/fixtures/sample.geojson
  - backend/tests/fixtures/sample.kml
  - backend/requirements.txt
autonomous: true
requirements:
  - FMT-01
  - FMT-02
  - FMT-03

must_haves:
  truths:
    - "GeoJSON FeatureCollection with Point/LineString/Polygon features produces rows with geometry columns and properties"
    - "Shapefile ZIP archive with .shp/.dbf/.shx extracts attribute rows with easting/northing columns"
    - "KML file with placemarks extracts name, description, and coordinate columns"
    - "KMZ file is decompressed and the inner KML is parsed identically"
    - "All three parsers return ParseResult with headers, rows (string[][]), total_rows, metadata, and source_format"
  artifacts:
    - path: "backend/app/parsers/base.py"
      provides: "ParseResult dataclass and flatten_geometry helper"
      contains: "class ParseResult"
    - path: "backend/app/parsers/geojson_parser.py"
      provides: "parse_geojson function"
      exports: ["parse_geojson"]
    - path: "backend/app/parsers/shapefile_parser.py"
      provides: "parse_shapefile_zip function"
      exports: ["parse_shapefile_zip"]
    - path: "backend/app/parsers/kml_parser.py"
      provides: "parse_kml, parse_kmz functions"
      exports: ["parse_kml", "parse_kmz"]
  key_links:
    - from: "backend/app/parsers/geojson_parser.py"
      to: "backend/app/parsers/base.py"
      via: "imports ParseResult, flatten_geometry"
      pattern: "from app\\.parsers\\.base import"
    - from: "backend/app/parsers/shapefile_parser.py"
      to: "backend/app/parsers/base.py"
      via: "imports ParseResult"
      pattern: "from app\\.parsers\\.base import"
    - from: "backend/app/parsers/kml_parser.py"
      to: "backend/app/parsers/base.py"
      via: "imports ParseResult"
      pattern: "from app\\.parsers\\.base import"
---

<objective>
Implement ParseResult base contract, GeoJSON, Shapefile, and KML/KMZ parsers with TDD on the FastAPI backend.

Purpose: These three formats (plus the two in plan 02) are the core geospatial file parsers that extend the platform beyond CSV/Excel. Each parser converts a geospatial format into normalized string[][] tabular output compatible with the existing column detection and validation pipeline.

Output: Three working parsers with full test coverage, shared ParseResult dataclass, test fixtures, and updated requirements.txt with new dependencies (pyshp, fastkml, lxml).
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-file-format-parsers/11-RESEARCH.md

@backend/requirements.txt
@backend/app/dependencies.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ParseResult base, test fixtures, and GeoJSON parser (TDD)</name>
  <files>
    backend/app/parsers/__init__.py,
    backend/app/parsers/base.py,
    backend/app/parsers/geojson_parser.py,
    backend/tests/parsers/__init__.py,
    backend/tests/parsers/test_geojson_parser.py,
    backend/tests/fixtures/sample.geojson,
    backend/requirements.txt
  </files>
  <behavior>
    - ParseResult has fields: headers (list[str]), rows (list[list[str]]), total_rows (int), metadata (dict), warnings (list[str]), source_format (str)
    - flatten_geometry("Point", [1.0, 2.0, 3.0]) returns [{"longitude": "1.0", "latitude": "2.0", "elevation": "3.0"}]
    - flatten_geometry("LineString", [[1,2],[3,4]]) returns 2 row dicts with point_index, longitude, latitude
    - flatten_geometry("Polygon", [[[0,0],[1,0],[1,1],[0,0]]]) returns rows with ring, point_index, longitude, latitude
    - parse_geojson on FeatureCollection with 3 Point features returns ParseResult with 3 rows, headers include geometry_type + property keys
    - parse_geojson on single Feature (not FeatureCollection) returns 1+ rows
    - parse_geojson on FeatureCollection with mixed geometry types includes geometry_type column
    - parse_geojson metadata includes feature_count
    - parse_geojson source_format is "geojson"
    - All row values are strings (not floats/ints)
  </behavior>
  <action>
    RED: Create backend/tests/parsers/__init__.py (empty). Create backend/tests/fixtures/sample.geojson with a FeatureCollection containing 3 Point features with properties (name, depth, description). Create backend/tests/parsers/test_geojson_parser.py with tests for ParseResult construction, flatten_geometry for Point/LineString/Polygon, and parse_geojson for FeatureCollection, single Feature, mixed geometry types. Run tests -- they must fail.

    GREEN: Create backend/app/parsers/__init__.py (empty for now). Create backend/app/parsers/base.py with ParseResult dataclass (headers, rows, total_rows, metadata, warnings, source_format) and flatten_geometry helper handling Point, LineString, Polygon, MultiPoint, MultiLineString, MultiPolygon, and GeometryCollection. Create backend/app/parsers/geojson_parser.py with parse_geojson(file_bytes: bytes) -> ParseResult. Use stdlib json only. Headers: ["geometry_type", "longitude", "latitude", "elevation"] + sorted property keys. All values converted to str. Run tests -- they must pass.

    Also update backend/requirements.txt to add: pyshp>=2.3, ezdxf>=1.4, fastkml>=1.0, lxml>=5.0 (install all deps once for both plans).

    Run: cd backend && pip install -r requirements.txt
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/parsers/test_geojson_parser.py -x -q</automated>
  </verify>
  <done>ParseResult dataclass and flatten_geometry helper tested and working. GeoJSON parser handles FeatureCollection, single Feature, and mixed geometry types. All values are strings. requirements.txt updated with all Phase 11 deps installed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shapefile ZIP parser (TDD)</name>
  <files>
    backend/app/parsers/shapefile_parser.py,
    backend/tests/parsers/test_shapefile_parser.py,
    backend/tests/fixtures/sample_shapefile.zip
  </files>
  <behavior>
    - parse_shapefile_zip on valid ZIP with .shp/.dbf/.shx returns ParseResult with attribute columns + easting/northing
    - parse_shapefile_zip raises ValueError when ZIP has no .shp file
    - parse_shapefile_zip handles Point geometry (easting/northing from shape points)
    - parse_shapefile_zip includes all .dbf attribute fields as columns
    - parse_shapefile_zip metadata includes shape_type and record_count
    - parse_shapefile_zip source_format is "shapefile"
    - All row values are strings
  </behavior>
  <action>
    RED: Create test fixture backend/tests/fixtures/sample_shapefile.zip programmatically in a conftest.py or test setup using pyshp's Writer to create a minimal shapefile with 3 Point records and 2 attribute fields (name: str, depth: float), then zip the .shp/.dbf/.shx into sample_shapefile.zip. Alternatively, generate the ZIP in the test itself using an in-memory approach. Write tests for valid ZIP parsing, missing .shp error, attribute extraction, and string conversion. Run tests -- they must fail.

    GREEN: Create backend/app/parsers/shapefile_parser.py with parse_shapefile_zip(file_bytes: bytes) -> ParseResult. Use pyshp (import shapefile). Open ZIP with zipfile, find .shp/.dbf/.shx, create shapefile.Reader from BytesIO objects. Extract field names from sf.fields[1:] (skip DeletionFlag). For each shape+record pair, flatten geometry coordinates to easting/northing columns and append attribute values. Handle encoding: try UTF-8 first via .cpg file in ZIP, fall back to latin-1. Run tests -- they must pass.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/parsers/test_shapefile_parser.py -x -q</automated>
  </verify>
  <done>Shapefile parser extracts attributes and coordinates from ZIP archives. Handles missing .shp gracefully. All values are strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: KML and KMZ parser (TDD)</name>
  <files>
    backend/app/parsers/kml_parser.py,
    backend/tests/parsers/test_kml_parser.py,
    backend/tests/fixtures/sample.kml
  </files>
  <behavior>
    - parse_kml on KML with 3 Point placemarks returns ParseResult with 3 rows
    - parse_kml headers include name, description, longitude, latitude, elevation
    - parse_kml extracts extended data fields as additional columns
    - parse_kmz decompresses KMZ and delegates to parse_kml
    - parse_kmz raises ValueError when KMZ has no .kml file
    - parse_kml metadata includes placemark_count
    - parse_kml source_format is "kml"
    - All row values are strings
  </behavior>
  <action>
    RED: Create backend/tests/fixtures/sample.kml with 3 Point placemarks including name, description, and coordinates. Write tests for KML parsing (placemarks to rows, header extraction, extended data), KMZ decompression (create in-memory KMZ from sample.kml bytes), and error cases. Run tests -- they must fail.

    GREEN: Create backend/app/parsers/kml_parser.py with parse_kml(file_bytes: bytes) -> ParseResult and parse_kmz(file_bytes: bytes) -> ParseResult. For KML: use fastkml to parse the document. If fastkml API is problematic, fall back to lxml with KML namespace-aware XPath queries (namespace: http://www.opengis.net/kml/2.2). Extract Placemark name, description, coordinates. For extended data, extract Data/SimpleData elements as additional columns. For KMZ: use stdlib zipfile to extract .kml, then call parse_kml. Run tests -- they must pass.

    REFACTOR if needed: Ensure consistent header ordering across placemarks with varying extended data keys.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/parsers/test_kml_parser.py -x -q</automated>
  </verify>
  <done>KML parser extracts placemarks with name, description, coordinates, and extended data. KMZ decompression works. All values are strings.</done>
</task>

</tasks>

<verification>
All parser tests pass:
```bash
cd backend && python -m pytest tests/parsers/ -x -q
```
</verification>

<success_criteria>
- ParseResult dataclass is the shared contract across all parsers
- GeoJSON parser handles FeatureCollection, single Feature, mixed geometry types
- Shapefile parser handles ZIP archives with attribute + coordinate extraction
- KML parser handles placemarks with extended data; KMZ decompresses correctly
- All parsers return string[][] rows (no numeric types in row data)
- All tests green
- New Python deps (pyshp, fastkml, lxml, ezdxf) installed
</success_criteria>

<output>
After completion, create `.planning/phases/11-file-format-parsers/11-01-SUMMARY.md`
</output>
