---
phase: 11-file-format-parsers
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/app/parsers/landxml_parser.py
  - backend/app/parsers/dxf_parser.py
  - backend/app/parsers/__init__.py
  - backend/tests/parsers/test_landxml_parser.py
  - backend/tests/parsers/test_dxf_parser.py
  - backend/tests/parsers/test_parse_dispatch.py
  - backend/tests/fixtures/sample.xml
  - backend/tests/fixtures/sample.dxf
autonomous: true
requirements:
  - FMT-04
  - FMT-05

must_haves:
  truths:
    - "LandXML file with CgPoints extracts survey point coordinates as tabular rows"
    - "LandXML file with Alignment extracts centerline coordinate data"
    - "DXF file with POINT, LINE, and LWPOLYLINE entities extracts coordinate rows with entity type and layer"
    - "DXF block references (INSERT entities) are expanded via virtual_entities"
    - "Parser dispatch routes files to correct parser by extension"
  artifacts:
    - path: "backend/app/parsers/landxml_parser.py"
      provides: "parse_landxml function"
      exports: ["parse_landxml"]
    - path: "backend/app/parsers/dxf_parser.py"
      provides: "parse_dxf function"
      exports: ["parse_dxf"]
    - path: "backend/app/parsers/__init__.py"
      provides: "PARSER_REGISTRY dict and dispatch_parser function"
      exports: ["dispatch_parser", "PARSER_REGISTRY"]
  key_links:
    - from: "backend/app/parsers/__init__.py"
      to: "backend/app/parsers/geojson_parser.py"
      via: "imports parse_geojson into registry"
      pattern: "from app\\.parsers\\.geojson_parser import"
    - from: "backend/app/parsers/__init__.py"
      to: "backend/app/parsers/dxf_parser.py"
      via: "imports parse_dxf into registry"
      pattern: "from app\\.parsers\\.dxf_parser import"
---

<objective>
Implement LandXML and DXF parsers with TDD, then build the parser dispatch registry that routes files to the correct parser by extension.

Purpose: Completes the five-format parser set. The dispatch registry is the single entry point used by the FastAPI endpoint (plan 03) to route any supported file to its parser.

Output: Two working parsers with tests, a dispatch_parser function with PARSER_REGISTRY mapping extensions to parser functions, and test fixtures for LandXML and DXF.
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-file-format-parsers/11-RESEARCH.md

@backend/app/parsers/base.py (created by plan 01 -- ParseResult dataclass, flatten_geometry)

<interfaces>
<!-- From plan 01 -- these will exist when plan 02 runs (both wave 1, but plan 02 needs base.py) -->
<!-- NOTE: Plans 01 and 02 are both wave 1. Plan 02 MUST NOT run until plan 01's Task 1 (which creates base.py) is complete. The dispatch registry in __init__.py imports all parsers, so plan 02 should run after plan 01 finishes. -->

From backend/app/parsers/base.py (created by plan 01 Task 1):
```python
@dataclass
class ParseResult:
    headers: list[str]
    rows: list[list[str]]
    total_rows: int
    metadata: dict
    warnings: list[str] = field(default_factory=list)
    source_format: str = ""

def flatten_geometry(geom_type: str, coords) -> list[dict]: ...
```

From backend/app/parsers/geojson_parser.py:
```python
def parse_geojson(file_bytes: bytes) -> ParseResult: ...
```

From backend/app/parsers/shapefile_parser.py:
```python
def parse_shapefile_zip(file_bytes: bytes) -> ParseResult: ...
```

From backend/app/parsers/kml_parser.py:
```python
def parse_kml(file_bytes: bytes) -> ParseResult: ...
def parse_kmz(file_bytes: bytes) -> ParseResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LandXML parser (TDD)</name>
  <files>
    backend/app/parsers/landxml_parser.py,
    backend/tests/parsers/test_landxml_parser.py,
    backend/tests/fixtures/sample.xml
  </files>
  <behavior>
    - parse_landxml on LandXML with CgPoints returns ParseResult with rows containing point name, northing, easting, elevation
    - parse_landxml on LandXML with Alignment/CoordGeom extracts coordinate points along the alignment
    - parse_landxml on LandXML with both CgPoints and Alignment returns combined rows with a "source" column distinguishing them
    - parse_landxml handles LandXML 1.2 namespace (http://www.landxml.org/schema/LandXML-1.2)
    - parse_landxml raises ValueError on non-LandXML XML files
    - parse_landxml metadata includes element counts (cgpoint_count, alignment_count)
    - parse_landxml source_format is "landxml"
    - All row values are strings
  </behavior>
  <action>
    RED: Create backend/tests/fixtures/sample.xml -- a minimal LandXML 1.2 file with a CgPoints collection (3 points: name, northing, easting, elevation as space-separated text content) and an Alignment with CoordGeom containing a Line element (start/end points). Write tests covering CgPoint extraction, Alignment extraction, combined output, namespace handling, non-LandXML rejection. Run tests -- they must fail.

    GREEN: Create backend/app/parsers/landxml_parser.py with parse_landxml(file_bytes: bytes) -> ParseResult. Use lxml.etree to parse. Register LandXML namespaces (1.0, 1.1, 1.2 -- try each). Use XPath to find CgPoint elements (text content is "northing easting elevation" space-separated). Use XPath to find Alignment/CoordGeom child elements (Line with Start/End points). Headers: ["source", "point_name", "easting", "northing", "elevation"]. Validate root tag contains "LandXML" -- raise ValueError otherwise. Run tests -- they must pass.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/parsers/test_landxml_parser.py -x -q</automated>
  </verify>
  <done>LandXML parser extracts CgPoints and Alignment coordinates. Handles LandXML namespaces. Rejects non-LandXML XML. All values are strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DXF parser and dispatch registry (TDD)</name>
  <files>
    backend/app/parsers/dxf_parser.py,
    backend/app/parsers/__init__.py,
    backend/tests/parsers/test_dxf_parser.py,
    backend/tests/parsers/test_parse_dispatch.py,
    backend/tests/fixtures/sample.dxf
  </files>
  <behavior>
    - parse_dxf on DXF with POINT entities returns rows with entity_type, layer, easting, northing, elevation
    - parse_dxf on DXF with LINE entities returns 2 rows per line (start and end points)
    - parse_dxf on DXF with LWPOLYLINE returns one row per vertex
    - parse_dxf handles INSERT entities by expanding via virtual_entities
    - parse_dxf metadata includes layer list
    - parse_dxf source_format is "dxf"
    - dispatch_parser routes .geojson to parse_geojson
    - dispatch_parser routes .zip to parse_shapefile_zip
    - dispatch_parser routes .kml to parse_kml, .kmz to parse_kmz
    - dispatch_parser routes .xml to parse_landxml
    - dispatch_parser routes .dxf to parse_dxf
    - dispatch_parser raises ValueError for unsupported extensions
  </behavior>
  <action>
    RED: Create backend/tests/fixtures/sample.dxf programmatically using ezdxf: create a new DXF document, add to modelspace a POINT, a LINE, and an LWPOLYLINE on different layers. Save to BytesIO, write to fixtures. Write tests for DXF entity extraction and dispatch_parser routing for all extensions. Run tests -- they must fail.

    GREEN: Create backend/app/parsers/dxf_parser.py with parse_dxf(file_bytes: bytes) -> ParseResult. Use ezdxf.read(io.BytesIO(file_bytes)). Iterate modelspace entities. Handle POINT (dxf.location), LINE (dxf.start + dxf.end as 2 rows), LWPOLYLINE (get_points format="xyb"), CIRCLE (dxf.center as 1 row), ARC (dxf.center as 1 row), 3DFACE (4 corner points), INSERT (expand with virtual_entities, recurse). Headers: ["entity_type", "layer", "easting", "northing", "elevation"]. Metadata: {"layers": list of unique layer names}.

    Update backend/app/parsers/__init__.py with PARSER_REGISTRY dict mapping extensions to parser functions, and dispatch_parser(file_bytes, filename) function. Also export ParseResult from __init__.py for convenience. Registry entries: .geojson -> parse_geojson, .json -> parse_geojson, .zip -> parse_shapefile_zip, .kml -> parse_kml, .kmz -> parse_kmz, .xml -> parse_landxml, .dxf -> parse_dxf.

    Write backend/tests/parsers/test_parse_dispatch.py testing dispatch routing and unsupported extension error.

    Run all tests -- they must pass.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/parsers/ -x -q</automated>
  </verify>
  <done>DXF parser handles POINT, LINE, LWPOLYLINE, and INSERT entities. Dispatch registry routes all 5 formats correctly. All parser tests green.</done>
</task>

</tasks>

<verification>
Full parser test suite:
```bash
cd backend && python -m pytest tests/parsers/ -x -q
```

Plus ensure existing backend tests still pass:
```bash
cd backend && python -m pytest -x -q
```
</verification>

<success_criteria>
- LandXML parser extracts CgPoints and Alignment data with namespace support
- DXF parser handles POINT, LINE, LWPOLYLINE, INSERT entities
- dispatch_parser routes all 7 extensions to correct parsers
- All parser tests pass alongside existing backend tests
</success_criteria>

<output>
After completion, create `.planning/phases/11-file-format-parsers/11-02-SUMMARY.md`
</output>
