---
phase: 22-ai-issue-prioritisation-smart-grouping
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/ai/types.ts
  - src/lib/ai/cluster-issues.ts
  - src/lib/ai/cluster-issues.test.ts
autonomous: true
requirements:
  - AIFR-03

must_haves:
  truths:
    - "Issues with the same rule_type and column_name are grouped into a single cluster"
    - "Clusters are sorted by priority score (severity_weight * count)"
    - "Each cluster has a human-readable label with issue count and KP range when available"
    - "Top 3 blockers are the first 3 clusters in the sorted output"
    - "Pipeline ValidationIssue type adapts to ClusterInput format via adapter function"
    - "Supabase ValidationIssue type adapts to ClusterInput format via adapter function"
  artifacts:
    - path: "src/lib/ai/types.ts"
      provides: "Shared types for clustering and AI summary"
      exports: ["ClusterInput", "IssueCluster", "AISummaryResponse"]
    - path: "src/lib/ai/cluster-issues.ts"
      provides: "Deterministic clustering, scoring, adapters"
      exports: ["clusterIssues", "getTopBlockers", "adaptServerIssues", "adaptPipelineIssues"]
    - path: "src/lib/ai/cluster-issues.test.ts"
      provides: "TDD tests for clustering logic"
      min_lines: 80
  key_links:
    - from: "src/lib/ai/cluster-issues.ts"
      to: "src/lib/ai/types.ts"
      via: "imports ClusterInput, IssueCluster"
      pattern: "import.*from.*types"
---

<objective>
Build the deterministic issue clustering and prioritisation engine with TDD.

Purpose: This is the core intelligence layer that groups similar validation issues (e.g. "23 KP gaps in DOB between KP 102-118") and ranks them by impact. It runs synchronously and produces the data structures consumed by both the UI and the AI summary API route in Plan 02.

Output: Pure TypeScript clustering library with full test coverage, type definitions, and adapter functions for both validation flows.
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ai-issue-prioritisation-smart-grouping/22-RESEARCH.md

<interfaces>
<!-- Supabase ValidationIssue (server-side, from validation_issues table) -->
From src/lib/types/validation.ts:
```typescript
export type ValidationSeverity = 'critical' | 'warning' | 'info'

export interface ValidationIssue {
  id: string
  run_id: string
  dataset_id: string
  row_number: number
  column_name: string
  rule_type: string
  severity: ValidationSeverity
  message: string
  expected: string | null
  actual: string | null
  kp_value: number | null
  created_at: string
}
```

<!-- Pipeline ValidationIssue (client-side, from client-validate.ts) -->
From src/app/(dashboard)/pipeline/lib/client-validate.ts:
```typescript
export interface ValidationIssue {
  type: "missing" | "duplicate" | "outlier" | "kp_gap" | "kp_monotonicity"
  severity: "critical" | "warning" | "info"
  row?: number
  column?: string
  message: string
  detail?: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Types and clustering engine with TDD</name>
  <files>src/lib/ai/types.ts, src/lib/ai/cluster-issues.ts, src/lib/ai/cluster-issues.test.ts</files>
  <behavior>
    - Test: clusterIssues groups issues with same rule_type + column_name into one cluster
    - Test: clusterIssues returns clusters sorted by priority (critical*10*count > warning*3*count > info*1*count)
    - Test: cluster label includes count and KP range when kp_value present (e.g. "5 KP gaps in "DOB" between KP 10.0-50.0")
    - Test: cluster label works without KP values (e.g. "3 missing data in "DOC"")
    - Test: getTopBlockers returns first 3 clusters from sorted output
    - Test: getTopBlockers returns fewer than 3 if fewer clusters exist
    - Test: adaptServerIssues maps Supabase ValidationIssue[] to ClusterInput[]
    - Test: adaptPipelineIssues maps pipeline ValidationIssue[] to ClusterInput[] (type->rule_type, optional row/column handled)
    - Test: single-issue cluster has count=1 and singular label
    - Test: empty issues array returns empty clusters array
  </behavior>
  <action>
    RED: Create src/lib/ai/cluster-issues.test.ts with all tests above. Create src/lib/ai/types.ts with:
    - ClusterInput: { id: string, rule_type: string, severity: 'critical'|'warning'|'info', row_number: number, column_name: string, message: string, kp_value: number|null }
    - IssueCluster: { id: string, rule_type: string, column_name: string, severity: ValidationSeverity, count: number, label: string, kp_range: {min:number,max:number}|null, row_range: {min:number,max:number}, issues: ClusterInput[], priorityScore: number }
    - AISummaryResponse: { summary: string, topBlockers: string[], recommendation: 'accept'|'reject', confidence: number, reasoning: string }
    Create stub src/lib/ai/cluster-issues.ts exporting empty functions. Run tests -- all MUST fail.

    GREEN: Implement in src/lib/ai/cluster-issues.ts:
    - clusterIssues(issues: ClusterInput[]): IssueCluster[] -- group by rule_type+column_name, compute severity as max in group, generate label via generateClusterLabel helper, sort by priorityScore (SEVERITY_WEIGHT[severity]*count where critical=10, warning=3, info=1)
    - getTopBlockers(clusters: IssueCluster[], n=3): IssueCluster[] -- return first n from sorted clusters
    - adaptServerIssues(issues: ServerValidationIssue[]): ClusterInput[] -- map id, rule_type, severity, row_number, column_name, message, kp_value directly
    - adaptPipelineIssues(issues: PipelineValidationIssue[]): ClusterInput[] -- map type->rule_type, row??0->row_number, column??'unknown'->column_name, generate id as pipeline-{index}
    - SEVERITY_WEIGHT constant: { critical: 10, warning: 3, info: 1 }
    - RULE_LABELS constant: { missing_data: "missing data", duplicate_rows: "duplicate row", near_duplicate_kp: "near-duplicate KP", outliers_zscore: "z-score outlier", outliers_iqr: "IQR outlier", kp_gaps: "KP gap", monotonicity: "monotonicity violation", range_check: "range violation", cross_column: "cross-column mismatch", spike_detection: "spike", coordinate_sanity: "coordinate issue", event_listing: "event mismatch", position_consistency: "position inconsistency", kp_drift: "KP drift", segment_continuity: "segment discontinuity", missing: "missing data", duplicate: "duplicate row", outlier: "outlier", kp_gap: "KP gap", kp_monotonicity: "monotonicity violation" }
    Run tests -- all MUST pass.

    Import types for adapters: use typeof imports with descriptive type aliases (ServerValidationIssue, PipelineValidationIssue) to avoid name collision since both flows export "ValidationIssue".
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai/cluster-issues.test.ts</automated>
  </verify>
  <done>All 10 clustering tests pass. Types exported. Adapter functions handle both validation flows.</done>
</task>

</tasks>

<verification>
npx vitest run src/lib/ai/
</verification>

<success_criteria>
- clusterIssues correctly groups, labels, and sorts issue clusters
- Adapter functions bridge both server and pipeline ValidationIssue types
- All tests pass with no imports from external dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/22-ai-issue-prioritisation-smart-grouping/22-01-SUMMARY.md`
</output>
