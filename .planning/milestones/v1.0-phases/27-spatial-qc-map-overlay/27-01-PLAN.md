---
phase: 27-spatial-qc-map-overlay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/spatial-qc/lib/types.ts
  - src/components/spatial-qc/lib/coordinate-extractor.ts
  - src/components/spatial-qc/lib/coordinate-extractor.test.ts
  - src/components/spatial-qc/lib/severity-colors.ts
  - src/components/spatial-qc/lib/severity-colors.test.ts
  - src/components/spatial-qc/lib/dataset-comparison.ts
  - src/components/spatial-qc/lib/dataset-comparison.test.ts
  - src/components/spatial-qc/lib/leaflet-heat.d.ts
autonomous: true
requirements: [SQCV-01, SQCV-02, SQCV-03]

must_haves:
  truths:
    - "Coordinate extractor resolves lat/lon from parsedData rows using column mappings"
    - "Coordinate extractor handles both lat/lon and easting/northing column types"
    - "hasSpatialColumns guard returns true only when coordinate columns exist in mappings"
    - "Severity colors map to correct marker fill/stroke and heatmap intensity"
    - "Dataset comparison matches rows by nearest KP and computes deviation distances"
  artifacts:
    - path: "src/components/spatial-qc/lib/types.ts"
      provides: "SpatialIssue, SpatialDataPoint, ComparisonResult interfaces"
      exports: ["SpatialIssue", "SpatialDataPoint", "ComparisonPair", "ComparisonResult"]
    - path: "src/components/spatial-qc/lib/coordinate-extractor.ts"
      provides: "Coordinate extraction from issues + parsedData + columnMappings"
      exports: ["extractSpatialIssues", "extractSpatialPoints", "hasSpatialColumns"]
    - path: "src/components/spatial-qc/lib/severity-colors.ts"
      provides: "Severity to color and heatmap intensity mappings"
      exports: ["SEVERITY_MARKER_COLORS", "SEVERITY_HEAT_INTENSITY", "getSeverityColor"]
    - path: "src/components/spatial-qc/lib/dataset-comparison.ts"
      provides: "KP-based matching and deviation computation between two datasets"
      exports: ["matchDatasetsByKP", "computeDeviations"]
    - path: "src/components/spatial-qc/lib/leaflet-heat.d.ts"
      provides: "TypeScript type declarations for leaflet.heat plugin"
  key_links:
    - from: "src/components/spatial-qc/lib/coordinate-extractor.ts"
      to: "src/lib/types/validation.ts"
      via: "imports ValidationIssue"
      pattern: "import.*ValidationIssue.*from.*validation"
    - from: "src/components/spatial-qc/lib/coordinate-extractor.ts"
      to: "src/lib/parsing/types.ts"
      via: "imports ColumnMapping"
      pattern: "import.*ColumnMapping.*from.*parsing/types"
---

<objective>
Build the core library modules for spatial QC visualization: type definitions, coordinate extraction logic, severity color mappings, dataset comparison engine, and leaflet.heat type declarations.

Purpose: These pure-function modules are the foundation for the map components in Plan 02. Coordinate extraction is the only genuinely new logic -- it cross-references validation issue row numbers with parsed data and column mappings to resolve geographic coordinates.

Output: Tested library modules under src/components/spatial-qc/lib/ with full test coverage.
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-spatial-qc-map-overlay/27-RESEARCH.md

<interfaces>
<!-- Key types the executor needs from existing codebase -->

From src/lib/types/validation.ts:
```typescript
export type ValidationSeverity = 'critical' | 'warning' | 'info'

export interface ValidationIssue {
  id: string
  run_id: string
  dataset_id: string
  row_number: number
  column_name: string
  rule_type: string
  severity: ValidationSeverity
  message: string
  expected: string | null
  actual: string | null
  kp_value: number | null
  created_at: string
}
```

From src/lib/parsing/types.ts:
```typescript
export type SurveyColumnType =
  | 'kp' | 'easting' | 'northing' | 'depth' | 'dob' | 'doc' | 'top'
  | 'elevation' | 'event' | 'description' | 'date' | 'time'
  | 'latitude' | 'longitude'

export interface ColumnMapping {
  index: number
  originalName: string
  mappedType: SurveyColumnType | null
  ignored: boolean
}
```

From pipeline client-validate (client-side ValidationIssue -- different shape):
```typescript
export interface ValidationIssue {
  type: "missing" | "duplicate" | "outlier" | "kp_gap" | "kp_monotonicity"
  severity: "critical" | "warning" | "info"
  row?: number
  column?: string
  message: string
  detail?: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type definitions, coordinate extractor, and severity colors with TDD</name>
  <files>
    src/components/spatial-qc/lib/types.ts,
    src/components/spatial-qc/lib/coordinate-extractor.ts,
    src/components/spatial-qc/lib/coordinate-extractor.test.ts,
    src/components/spatial-qc/lib/severity-colors.ts,
    src/components/spatial-qc/lib/severity-colors.test.ts,
    src/components/spatial-qc/lib/leaflet-heat.d.ts
  </files>
  <behavior>
    - extractSpatialIssues with lat/lon columns: returns SpatialIssue[] with correct coords from row_number lookup
    - extractSpatialIssues with easting/northing columns (no lat/lon): returns SpatialIssue[] using easting/northing as lat/lng (caller must convert)
    - extractSpatialIssues with no spatial columns: returns empty array
    - extractSpatialIssues skips issues where row_number is out of bounds or coords are NaN
    - extractSpatialIssues works with server ValidationIssue (row_number field)
    - extractSpatialPoints: extracts all data points as SpatialDataPoint[] (not just issue rows)
    - hasSpatialColumns: returns true when lat+lon mapped, true when easting+northing mapped, false when neither
    - SEVERITY_MARKER_COLORS: critical=red, warning=amber, info=blue with fill and stroke
    - SEVERITY_HEAT_INTENSITY: critical=1.0, warning=0.6, info=0.3
    - getSeverityColor: returns correct fill/stroke for each severity level
  </behavior>
  <action>
    1. Create src/components/spatial-qc/lib/types.ts with:
       - SpatialIssue: { issue: ValidationIssue; lat: number; lng: number }
       - SpatialDataPoint: { rowIndex: number; lat: number; lng: number; kp?: number }
       - ComparisonPair: { pointA: SpatialDataPoint; pointB: SpatialDataPoint; deviationMeters: number }
       - ComparisonResult: { pairs: ComparisonPair[]; unmatchedA: SpatialDataPoint[]; unmatchedB: SpatialDataPoint[] }
       Import ValidationIssue from @/lib/types/validation

    2. Create src/components/spatial-qc/lib/severity-colors.ts:
       - SEVERITY_MARKER_COLORS const map: critical={fill:'#ef4444',stroke:'#991b1b'}, warning={fill:'#f59e0b',stroke:'#b45309'}, info={fill:'#3b82f6',stroke:'#1d4ed8'}
       - SEVERITY_HEAT_INTENSITY const map: critical=1.0, warning=0.6, info=0.3
       - getSeverityColor(severity: ValidationSeverity): returns {fill, stroke} from SEVERITY_MARKER_COLORS

    3. Create src/components/spatial-qc/lib/coordinate-extractor.ts:
       - hasSpatialColumns(mappings: ColumnMapping[] | null): boolean -- checks for lat+lon or easting+northing pairs
       - extractSpatialIssues(issues: ValidationIssue[], parsedData: string[][], columnMappings: ColumnMapping[]): SpatialIssue[]
         - Find lat/lon column indices from mappings (mappedType === 'latitude'/'longitude')
         - Fall back to easting/northing if no lat/lon
         - For each issue with valid row_number (1-indexed, header=row 0), look up coordinates from parsedData[row_number]
         - Skip issues where coords are NaN or row out of bounds
         - Return SpatialIssue[] with the original issue object + resolved coordinates
       - extractSpatialPoints(parsedData: string[][], columnMappings: ColumnMapping[]): SpatialDataPoint[]
         - Extract coordinates for ALL data rows (not just issue rows), used by comparison feature
         - Include kp value if KP column is mapped

    4. Create leaflet-heat.d.ts following existing pattern in src/app/(public)/tools/visualize/lib/leaflet-screenshoter.d.ts:
       - Declare module 'leaflet' augmentation with heatLayer function
       - Options: radius, blur, maxZoom, max, minOpacity, gradient

    5. Write tests RED first, then implement GREEN. Test files:
       - coordinate-extractor.test.ts: test all behaviors listed above with mock parsedData and column mappings
       - severity-colors.test.ts: test all severity mappings and getSeverityColor function

    Install leaflet.heat: `npm install leaflet.heat`
  </action>
  <verify>
    <automated>cd C:/Users/danie/OneDrive/Desktop/BDS && npx vitest run src/components/spatial-qc/lib/coordinate-extractor.test.ts src/components/spatial-qc/lib/severity-colors.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - All coordinate extraction tests pass (lat/lon, easting/northing, no spatial cols, out-of-bounds, NaN)
    - All severity color tests pass
    - hasSpatialColumns correctly detects lat+lon and easting+northing pairs
    - leaflet.heat installed and type declaration file exists
    - Types exported from types.ts
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dataset comparison engine with TDD</name>
  <files>
    src/components/spatial-qc/lib/dataset-comparison.ts,
    src/components/spatial-qc/lib/dataset-comparison.test.ts
  </files>
  <behavior>
    - matchDatasetsByKP: matches points from two datasets by nearest KP value within tolerance (default 0.1)
    - matchDatasetsByKP: returns matched pairs and unmatched points from both datasets
    - matchDatasetsByKP: with identical KP values returns all matched pairs with 0 deviation
    - matchDatasetsByKP: with no KP overlap returns all points as unmatched
    - computeDeviations: computes Haversine distance in meters between matched pairs
    - computeDeviations with co-located points: returns ~0 meters deviation
    - computeDeviations with known offset: returns expected distance (within 1% tolerance for Haversine approximation)
  </behavior>
  <action>
    1. Create src/components/spatial-qc/lib/dataset-comparison.ts:
       - matchDatasetsByKP(pointsA: SpatialDataPoint[], pointsB: SpatialDataPoint[], kpTolerance?: number): ComparisonResult
         - Sort both arrays by kp value
         - For each point in A, find nearest KP match in B within tolerance
         - Each B point can only be matched once (greedy nearest-first)
         - Compute Haversine deviation in meters for each pair
         - Return { pairs, unmatchedA, unmatchedB }
       - computeDeviations(pairs: Array<{pointA: SpatialDataPoint, pointB: SpatialDataPoint}>): ComparisonPair[]
         - Haversine formula between (lat,lng) of pointA and pointB
         - Return distance in meters
       - Internal haversineDistance(lat1, lon1, lat2, lon2): number (meters)

    2. Write tests RED first, then implement GREEN:
       - dataset-comparison.test.ts: test matching, unmatched handling, deviation computation
       - Use concrete test data: e.g. two survey lines with known KP values and slightly offset coordinates
  </action>
  <verify>
    <automated>cd C:/Users/danie/OneDrive/Desktop/BDS && npx vitest run src/components/spatial-qc/lib/dataset-comparison.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - KP matching correctly pairs points within tolerance
    - Unmatched points correctly identified in both datasets
    - Haversine deviation computation returns accurate distances
    - All tests pass
  </done>
</task>

</tasks>

<verification>
```bash
cd C:/Users/danie/OneDrive/Desktop/BDS && npx vitest run src/components/spatial-qc/ --reporter=verbose
```
All spatial-qc library tests pass. leaflet.heat is installed. Type declaration exists.
</verification>

<success_criteria>
- coordinate-extractor resolves lat/lng from parsedData using column mappings for both lat/lon and easting/northing
- hasSpatialColumns guard returns correct boolean
- severity color/intensity mappings are correct
- dataset comparison matches by KP with Haversine deviation
- All test files pass
- leaflet.heat installed with .d.ts type declaration
</success_criteria>

<output>
After completion, create `.planning/phases/27-spatial-qc-map-overlay/27-01-SUMMARY.md`
</output>
