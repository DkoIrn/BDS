---
phase: 25-multi-user-roles
plan: 03
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - src/lib/actions/comments.ts
  - src/lib/actions/approval.ts
  - src/components/comments/issue-comments.tsx
  - src/components/approval/approval-workflow.tsx
  - src/components/results/expandable-issue-row.tsx
  - src/app/(dashboard)/projects/[projectId]/jobs/[jobId]/files/[fileId]/page.tsx
autonomous: true
requirements: [TEAM-05]

must_haves:
  truths:
    - "Users can add comments on individual validation issues"
    - "Comments show author name and timestamp"
    - "Users can delete their own comments"
    - "Datasets can be transitioned through draft -> reviewed -> approved -> issued"
    - "Only Reviewers and Admins can change approval status"
    - "Approval status transitions follow the defined state machine"
    - "Approval status changes are logged in the audit trail"
  artifacts:
    - path: "src/lib/actions/comments.ts"
      provides: "Comment CRUD server actions"
      exports: ["addComment", "getIssueComments", "deleteComment"]
    - path: "src/lib/actions/approval.ts"
      provides: "Approval status transition server action"
      exports: ["updateApprovalStatus", "getApprovalStatus"]
    - path: "src/components/comments/issue-comments.tsx"
      provides: "Comment thread UI for validation issues"
    - path: "src/components/approval/approval-workflow.tsx"
      provides: "Approval status badge and transition buttons"
  key_links:
    - from: "src/components/comments/issue-comments.tsx"
      to: "src/lib/actions/comments.ts"
      via: "addComment server action"
      pattern: "addComment"
    - from: "src/components/approval/approval-workflow.tsx"
      to: "src/lib/actions/approval.ts"
      via: "updateApprovalStatus server action"
      pattern: "updateApprovalStatus"
    - from: "src/components/results/expandable-issue-row.tsx"
      to: "src/components/comments/issue-comments.tsx"
      via: "Rendered inside expanded issue detail"
      pattern: "IssueComments"
---

<objective>
Add issue commenting and dataset approval workflow. Users can discuss individual validation issues with threaded comments, and datasets can progress through a draft -> reviewed -> approved -> issued lifecycle managed by Reviewers/Admins.

Purpose: Enables team collaboration on QC results -- discussion on specific issues and formal sign-off workflow for client deliverables.
Output: Comment server actions + UI, approval status actions + UI, integration into existing results views
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-multi-user-roles/25-RESEARCH.md
@.planning/phases/25-multi-user-roles/25-01-SUMMARY.md
@src/components/results/expandable-issue-row.tsx
@src/app/(dashboard)/projects/[projectId]/jobs/[jobId]/files/[fileId]/page.tsx

<interfaces>
<!-- From Plan 01 outputs -->

From src/lib/types/organisations.ts:
```typescript
export type OrgRole = 'admin' | 'reviewer' | 'viewer'
export type ApprovalStatus = 'draft' | 'reviewed' | 'approved' | 'issued'
export const APPROVAL_TRANSITIONS: Record<ApprovalStatus, ApprovalStatus[]>
export interface IssueComment {
  id: string; issue_id: string; user_id: string; content: string;
  created_at: string; updated_at: string;
  profiles?: { full_name: string | null }
}
```

From src/lib/permissions.ts:
```typescript
export function hasPermission(role: OrgRole, action: PermissionAction): boolean
export async function requireOrgRole(supabase: SupabaseClient, userId: string, minRole?: OrgRole): Promise<{ orgId: string; role: OrgRole } | { error: string }>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comment and approval server actions</name>
  <files>src/lib/actions/comments.ts, src/lib/actions/approval.ts</files>
  <action>
1. **Create `src/lib/actions/comments.ts`:**
   - `addComment(issueId: string, content: string)`: Server action. getUser + requireOrgRole('viewer') (all roles can comment per permission matrix). Insert into issue_comments with user_id for attribution. Revalidate the file detail path. Return { success: true, comment } or { error }.
   - `getIssueComments(issueId: string)`: Server action. getUser + requireOrgRole('viewer'). Select from issue_comments where issue_id matches, join profiles for full_name, order by created_at ASC. Return IssueComment[].
   - `deleteComment(commentId: string)`: Server action. getUser + requireOrgRole('viewer'). Delete from issue_comments where id = commentId AND user_id = current user (can only delete own comments). Return { success } or { error }.

2. **Create `src/lib/actions/approval.ts`:**
   - `getApprovalStatus(datasetId: string)`: Server action. getUser + requireOrgRole('viewer'). Select approval_status from datasets where id = datasetId. Return the status string.
   - `updateApprovalStatus(datasetId: string, newStatus: ApprovalStatus)`: Server action. getUser + requireOrgRole('reviewer'). Fetch current approval_status. Validate transition using APPROVAL_TRANSITIONS (imported from types/organisations). If invalid transition, return { error: 'Invalid status transition from X to Y' }. Update datasets set approval_status = newStatus. Log in audit_logs: action = 'approval_change', metadata = { from: oldStatus, to: newStatus, changed_by: user.id }. Revalidate path. Return { success: true }.
   - `canTransition(current: ApprovalStatus, next: ApprovalStatus)`: Pure function (not server action). Returns boolean. Export for use in UI to disable invalid transition buttons.

IMPORTANT: Use the APPROVAL_TRANSITIONS constant from src/lib/types/organisations.ts for valid state transitions. Do not duplicate the definition.
  </action>
  <verify>
    <automated>cd C:/Users/danie/OneDrive/Desktop/BDS && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Comment CRUD actions work with org role checks. Approval status transitions validate against state machine. Audit logging on approval changes. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Comment thread UI and approval workflow component</name>
  <files>src/components/comments/issue-comments.tsx, src/components/approval/approval-workflow.tsx, src/components/results/expandable-issue-row.tsx, src/app/(dashboard)/projects/[projectId]/jobs/[jobId]/files/[fileId]/page.tsx</files>
  <action>
1. **Create `src/components/comments/issue-comments.tsx`** (client component):
   - Props: `issueId: string, currentUserId: string`
   - On mount, fetch comments via getIssueComments(issueId).
   - Display comment thread: each comment shows author name (from profiles.full_name, fallback to "Unknown"), timestamp (relative: "2 hours ago"), and content.
   - Add comment form at bottom: textarea + "Post" button. Uses addComment server action. Clear textarea on success. Optimistic update: add to local list immediately.
   - Delete button (trash icon) on own comments only (comment.user_id === currentUserId). Calls deleteComment, removes from local list.
   - Empty state: "No comments yet. Be the first to comment." in muted text.
   - Style: compact thread style, small text, subtle borders between comments. Use existing Card/CardContent pattern.

2. **Create `src/components/approval/approval-workflow.tsx`** (client component):
   - Props: `datasetId: string, currentStatus: ApprovalStatus, userRole: OrgRole`
   - Display current status as a colored badge: draft=gray, reviewed=blue, approved=emerald, issued=violet.
   - Show transition buttons for valid next states (from APPROVAL_TRANSITIONS). Only render buttons if userRole is 'reviewer' or 'admin' (use hasPermission('approve_datasets')).
   - Each button calls updateApprovalStatus. Show loading state during transition.
   - Success toast: "Status updated to {newStatus}".
   - Error toast on invalid transition.
   - Visual: horizontal flow showing status progression with the current step highlighted. Simple pill badges with arrows between them.

3. **Update `src/components/results/expandable-issue-row.tsx`:**
   - Import IssueComments component.
   - In the expanded detail section (below the issue explanation), add a collapsible "Comments" section.
   - Use a disclosure/accordion pattern: "Comments (N)" header that expands to show IssueComments.
   - Pass issueId and currentUserId props.

4. **Update file detail page** (`src/app/(dashboard)/projects/[projectId]/jobs/[jobId]/files/[fileId]/page.tsx`):
   - Import ApprovalWorkflow component.
   - Fetch current user's org role (use getUserOrgRole from team actions or derive from requireOrgRole).
   - Render ApprovalWorkflow component in the file detail header area, next to or below the file status. Pass datasetId, current approval_status from dataset, and userRole.
   - Pass currentUserId down to the results tab area so expandable-issue-row can forward it to IssueComments.
  </action>
  <verify>
    <automated>cd C:/Users/danie/OneDrive/Desktop/BDS && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Issue comments render in expanded issue rows with add/delete functionality. Approval workflow badge and transition buttons visible on file detail page. Viewers can comment but not change approval status. Reviewers/Admins can transition approval status. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Comments component renders in expanded issue rows
- Approval workflow component renders on file detail page
- Approval transitions follow the state machine (draft->reviewed->approved->issued)
- Comment deletion only allowed for own comments
</verification>

<success_criteria>
- Users can add, view, and delete (own) comments on validation issues
- Datasets show approval status badge with transition controls
- Only Reviewers/Admins can change approval status
- Invalid transitions are rejected with clear error messages
- Approval changes logged in audit trail
</success_criteria>

<output>
After completion, create `.planning/phases/25-multi-user-roles/25-03-SUMMARY.md`
</output>
