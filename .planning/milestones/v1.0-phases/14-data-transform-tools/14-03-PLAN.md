---
phase: 14-data-transform-tools
plan: 03
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - src/app/(dashboard)/tools/transform/split/page.tsx
  - src/app/(dashboard)/tools/transform/split/split-tool.tsx
  - src/app/api/transform/split/route.ts
autonomous: false
requirements:
  - XFRM-10
  - XFRM-11

must_haves:
  truths:
    - "Split tool lets user upload a file, choose split mode (KP range or column value), configure split parameters, and download results"
    - "Split by column value produces a ZIP with one file per unique value"
    - "Split by KP range produces a ZIP with one file per range"
    - "Individual file downloads and Download All as ZIP both work"
    - "Split done state shows preview table of first ~20 rows for the selected/first output file"
    - "Output format defaults to same-as-input with GeoJSON fallback"
  artifacts:
    - path: "src/app/(dashboard)/tools/transform/split/split-tool.tsx"
      provides: "Split dataset tool UI with two modes"
      contains: "useDropzone"
    - path: "src/app/api/transform/split/route.ts"
      provides: "Auth proxy for split endpoint"
      exports: ["POST"]
  key_links:
    - from: "src/app/(dashboard)/tools/transform/split/split-tool.tsx"
      to: "/api/transform/split"
      via: "fetch POST with FormData"
      pattern: "fetch.*api/transform/split"
    - from: "src/app/api/transform/split/route.ts"
      to: "FASTAPI_URL/api/v1/transform/split"
      via: "raw body forwarding"
      pattern: "fastApiUrl.*transform/split"
    - from: "src/app/(dashboard)/tools/transform/split/split-tool.tsx"
      to: "src/components/transform/output-preview-table.tsx"
      via: "import OutputPreviewTable"
      pattern: "OutputPreviewTable"
---

<objective>
Build the split dataset tool with KP range and column value split modes, individual file downloads, and end-to-end human verification of all three transform tools.

Purpose: Completes the transform tools suite. Split is the most complex UI (two modes, multiple outputs, individual + ZIP download) so it gets its own plan. Includes verification checkpoint for all three tools.
Output: Split tool at /tools/transform/split with both split modes, individual file downloads, API proxy route, and verified end-to-end functionality.
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-data-transform-tools/14-CONTEXT.md
@.planning/phases/14-data-transform-tools/14-RESEARCH.md
@.planning/phases/14-data-transform-tools/14-01-SUMMARY.md
@.planning/phases/14-data-transform-tools/14-02-SUMMARY.md

<interfaces>
<!-- Auth proxy pattern from Plan 02 -->
From src/app/api/transform/crs/route.ts:
```typescript
export async function POST(request: Request) {
  // Auth check -> raw body forward to ${fastApiUrl}/api/v1/transform/... -> relay response
}
```

<!-- Split endpoint from Plan 01 -->
From backend/app/routers/transform.py (Plan 01):
- POST /api/v1/transform/split
- Form fields: file, mode ("kp"|"column"), target_format, ranges (JSON for kp), column_name (for column)
- Returns: application/zip with X-Split-Files header listing filenames
- INDIVIDUAL FILE SUPPORT: pass single_value (for column mode) or single_range (JSON [start,end] for kp mode) to get a single file instead of ZIP

<!-- Shared preview table from Plan 02 -->
From src/components/transform/output-preview-table.tsx:
```typescript
export function OutputPreviewTable({ csvText, maxRows }: { csvText: string; maxRows?: number }): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split tool page and API proxy</name>
  <files>
    src/app/(dashboard)/tools/transform/split/page.tsx,
    src/app/(dashboard)/tools/transform/split/split-tool.tsx,
    src/app/api/transform/split/route.ts
  </files>
  <action>
    1. Create src/app/api/transform/split/route.ts:
       - Same auth proxy pattern as CRS/merge routes.
       - Forward to `${fastApiUrl}/api/v1/transform/split`.
       - Forward headers: Content-Type, Content-Disposition, X-Transform-Warnings, X-Split-Files, X-Input-Format.
       - For ZIP responses, forward the blob directly (application/zip content type).
       - For single-file responses (when single_value/single_range used), forward as normal file response.

    2. Create src/app/(dashboard)/tools/transform/split/page.tsx:
       - Server component wrapper with title "Split Dataset" and back link to /tools/transform.

    3. Create src/app/(dashboard)/tools/transform/split/split-tool.tsx ("use client"):
       - State machine: upload -> configure -> splitting -> done -> error.

       Step 1 (Upload): Single-file dropzone (same extensions, 50MB limit). Show file info after selection.

       Step 2 (Configure):
       - Mode toggle: "By KP Range" or "By Column Value" (two buttons or radio group).

       KP Range mode:
       - Dynamic list of range inputs. Each range: start KP (number input) and end KP (number input).
       - "Add Range" button to add more ranges. Minimum 1 range.
       - Remove button on each range (if >1).

       Column Value mode:
       - After file selected, read first line client-side to extract headers (for CSV). For non-CSV files, show a text input for column name.
       - Show dropdown of detected column headers for CSV/Excel files.

       Output format selector: CSV, GeoJSON, KML.
       DEFAULT OUTPUT FORMAT: Detect the extension of the uploaded file. If it matches a supported output format, pre-select it. Otherwise default to GeoJSON. Per user decision: "same as input, fallback to GeoJSON."
       "Split Dataset" button to proceed. If user has not changed format, send target_format as empty string for backend default.

       Step 3 (Splitting): Spinner with "Splitting dataset..." status text.
       POST to /api/transform/split with FormData: file, mode ("kp" or "column"), target_format, ranges (JSON.stringify for KP mode), column_name (for column mode).

       Step 4 (Done):
       - Parse X-Split-Files header to get list of filenames (comma-separated).
       - Show file list with individual download buttons for each file.
       - INDIVIDUAL FILE DOWNLOADS: When user clicks a single file's download button, make a second fetch to /api/transform/split with the same file and parameters PLUS single_value (for column mode: the specific column value for that file, extracted from the filename label) or single_range (for kp mode: the specific [start, end] range). The backend returns a single file (not ZIP). Create blob URL and trigger download.
       - "Download All as ZIP" button: uses the original ZIP blob URL from the initial split response.
       - Show stats: number of output files, warnings from X-Transform-Warnings.
       - Import and render OutputPreviewTable from src/components/transform/output-preview-table.tsx. For the preview: after initial split, request the first individual file as text (using single_value/single_range for the first split result) and pass to OutputPreviewTable with maxRows=20. When user clicks a different file's download button, update the preview to show that file's content.
       - "Split Another" reset button.

       Error state: Same inline error pattern.

       Client-side CSV header extraction:
       ```typescript
       async function extractCsvHeaders(file: File): Promise<string[]> {
         const text = await file.slice(0, 4096).text()
         const firstLine = text.split('\n')[0]
         return firstLine.split(',').map(h => h.trim().replace(/^"|"$/g, ''))
       }
       ```
       For non-CSV files, show text input instead of dropdown.
  </action>
  <verify>
    <automated>cd "C:/Users/danie/OneDrive/Desktop/BDS" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Split tool page renders with upload, mode selection (KP range / column value), configuration panels, individual file download buttons, Download All as ZIP button, and preview table showing first ~20 rows. Output format defaults to same-as-input with GeoJSON fallback. API proxy route forwards to FastAPI correctly. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all transform tools end-to-end</name>
  <action>Human verification of all three transform tools built across Plans 01-03.</action>
  <what-built>All three transform tools: CRS conversion at /tools/transform/crs, merge at /tools/transform/merge, split at /tools/transform/split. Landing page with clickable cards and Coming Soon badge on Auto-Clean. Shared preview table showing first ~20 rows of output. Individual file downloads for split tool. Default output format matching input file type.</what-built>
  <how-to-verify>
    1. Start the dev servers: `npm run dev` and `cd backend && uvicorn app.main:app --reload`
    2. Navigate to /tools/transform -- verify cards are clickable, Auto-Clean shows "Coming Soon" badge
    3. CRS Conversion (/tools/transform/crs):
       - Upload a CSV with longitude/latitude columns
       - Verify source CRS auto-detected as WGS84
       - Verify output format defaults to CSV (same as input)
       - Select target CRS (e.g., OSGB36)
       - Click transform and verify preview table shows first ~20 rows of converted data
       - Download result and verify coordinates are in British National Grid format
    4. Merge (/tools/transform/merge):
       - Upload 2+ CSV files
       - Verify file list shows all uploaded files
       - Verify output format defaults to CSV (same as input)
       - Merge and verify preview table shows first ~20 rows of merged output
       - Download and verify merged file has all rows from both files
    5. Split (/tools/transform/split):
       - Upload a CSV with a categorical column
       - Verify output format defaults to CSV (same as input)
       - Select "By Column Value" mode
       - Pick the column and split
       - Verify file list shows individual download buttons per file
       - Click an individual file download and verify it downloads just that file
       - Click "Download All as ZIP" and verify it contains all files
       - Verify preview table shows first ~20 rows of the selected file
       - Test "By KP Range" mode with a CSV containing a KP column
    6. Test GeoJSON fallback: Upload a .xlsx file to CRS tool -> verify output format defaults to GeoJSON (not CSV)
  </how-to-verify>
  <verify>Human confirms all tools work correctly</verify>
  <done>All three transform tools verified functional. Landing page links work. CRS converts coordinates correctly. Merge combines files. Split produces individual downloads AND ZIP output. Preview tables show first ~20 rows. Output format defaults to same-as-input with GeoJSON fallback.</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
All three transform tools functional end-to-end. Backend tests pass. Frontend builds. User verifies visual and functional correctness. Individual file downloads work for split tool. Preview tables show in done state for all tools.
</verification>

<success_criteria>
- Split tool supports both KP range and column value split modes
- Split output provides individual file download buttons AND Download All as ZIP
- Preview table shows first ~20 rows of output for all three tools
- Output format defaults to same-as-input with GeoJSON fallback
- All three tools follow consistent three-step UI pattern
- Landing page correctly links to all tools with Coming Soon on Auto-Clean
- Human verification confirms end-to-end functionality
</success_criteria>

<output>
After completion, create `.planning/phases/14-data-transform-tools/14-03-SUMMARY.md`
</output>
